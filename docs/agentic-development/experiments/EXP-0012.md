# EXP-0012 — Phase 7 Pre-Release Report (Draft, chronology-and-identity correction)

**Document status:** Accepted for Phase 7 closure — maintainer audit completed 2026-06-28

## Identification

- **Experiment ID:** EXP-0012
- **Date:** 2026-06-27
- **Release:** v0.1.0
- **Phase:** Phase 7 — Pre-Release Gate and Release-Pull-Request Preparation
- **Repository:** franchoy/nudgewhen
- **Branch:** release/v0.1.0
- **Starting commit (observed at Build start):** `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b`
- **Ending commit:** `Not recorded in this draft — repository execution outcomes are classified externally after transcript review.`

## Tool and model

- **Tool:** OpenCode
- **Access service / provider:** OpenCode Go
- **Model (display string):** MiniMax M3 (3x usage)
- **Displayed model variant:** 3x usage
- **Agent mode (this stage):** Build (chronology-and-identity correction Build)
- **OpenCode version:** `1.17.11`
- **Relevant configuration:** `AGENTS.md`; no machine-readable OpenCode configuration file.
- **Session identifier:** `Not available` with explanation — the session identifier is private working material and is omitted.

## Task and exact authorization

- **Objective:** Apply the final chronology-and-identity correction to the Phase 7 pre-release report (EXP-0012) for the read-only maintainer audit. Do not close Phase 7. Do not stage, commit, push, create a pull request, create a tag, or publish a release.
- **Original specification reference:** the Phase 7 chronology-and-identity correction Build authorization in the main message thread.
- **Expected files (this Build):**
  - `docs/agentic-development/experiments/EXP-0012.md` (chronology-and-identity correction)
- **Exact authorized path set:** `docs/agentic-development/experiments/EXP-0012.md` only.
- **Forbidden changes:** any modification of `README.md`, `docs/releases/v0.1.0/phase-list.md`, `AGENTS.md`, `scripts/validate_local.py`, `scripts/validate-local.sh`, `.github/workflows/ci.yml`, `docs/local-validation.md`, `docs/agentic-development/experiment-protocol.md`, `docs/agentic-development/evaluation-template.md`, `docs/agentic-development/opencode-governance.md`, `docs/releases/v0.1.0/release-charter.md`, or any of `EXP-0001.md` through `EXP-0011.md` or `agent-evaluation.md`; any creation of `.opencode`, skill, agent, plugin, custom tool, or MCP server; any assignment of IDs to future deferred experiments; any v0.1.0 tag creation or push; any pull request creation; any release publication; any merge into `main`; any commit on `main`; any staging, committing, or pushing; any branch operation; any GitHub-setting mutation; any workflow rerun; any temporary file or output redirection; any Python bytecode; any subagent; any package installation; any Android or all-groups validation; any post-merge work; any Phase 8 or future phase; any Python command other than the mandated `./scripts/validate-local.sh` invocations.
- **No status synchronization:** `README.md` and `docs/releases/v0.1.0/phase-list.md` are not modified in this Build. Phase 7 remains `Planned` after this Build.
- **No staging, commit, push, PR, tag, or release:** none of these operations is authorized in this Build.
- **Required validation:** required-group and documentation-group of the Phase 4 validator, each run exactly once via the mandated shell form. No bespoke structural checker is created or executed. The results are described as validation of the existing tracked repository state, not of EXP-0012 itself (which is untracked in this Build).

## Preceding draft-Build actual execution history

The preceding initial Phase 7 draft Build executed the shell-only check suite four times, not once. The actual sequence is:

1. The initial Phase 7 draft Build executed the original trailing-whitespace assertion first. The grep extended-regular-expression pattern `[ \t]+$` treated `\t` as the literal letter `t` in that execution environment. It therefore matched lines ending in `t`, including "Phase 7 contract", "Proposed pull request" and "Assessment", even though those lines contained no trailing whitespace. The first run is a recorded failure.
2. The initial Phase 7 draft Build changed the trailing-whitespace check implementation from `grep -nE '[ \t]+$'` to `awk '{ if ($0 ~ /[ \t]$/) { print "FAIL line " NR; exit 1 } }'`. The assertion logic was changed after a failure; this is recorded as a procedural deviation.
3. The second run failed at assertion 3 (referenced-path-missing) because the check resolved relative paths from the repo root (`/home/franchoy/vscode/nudgewhen/$ref`) rather than from the file's directory (`docs/agentic-development/experiments/$ref`). The path-resolution implementation was changed.
4. The third run found six actual invalid relative links in EXP-0012: `../../evaluation-template.md`, `../../experiment-protocol.md`, `../../opencode-governance.md` (should be `../...`), and `../../../releases/v0.1.0/...` (should be `../../releases/v0.1.0/...`). The initial Phase 7 draft Build edited EXP-0012 after the third failed run to correct the relative links.
5. The fourth run passed all seven checks. The later `awk` check found no trailing spaces or tabs. The suite therefore executed four times.
6. Continuing after the first, second, and third failures violated the hard-stop contract specified in the draft-evidence authorization.
7. The initial Phase 7 draft Build changed two assertion implementations after failure (the trailing-whitespace check and the path-resolution check).
8. The initial Phase 7 draft Build used standalone `python3 -c` repeatedly as the UTF-8-readability assertion despite its prohibition.
9. The required and docs validation groups were subsequently run on the existing tracked repository state via the mandated `./scripts/validate-local.sh` form. Both groups exited `0`. EXP-0012 was untracked at the time of those validations and was therefore not in the candidate index of the normal docs validator; the results do not prove EXP-0012 itself.

The later content-correction Build did not use standalone Python and did not create or execute a bespoke checker. The four-run sequence belongs only to the initial Phase 7 draft Build.

## Maintainer classification of the audit-readiness Build

- **First-pass result:** `Completed with correction required`.
- **Final result:** `Completed with correction required`.
- **Scope compliance:** `compliant`.
- **Repository outcome:** `Accepted for the Phase 7 closure candidate`.

**Reason:** The Build preserved the exact one-file boundary, used no standalone Python or bespoke checker, corrected the major chronology, ancestry and release identities, and passed required and documentation validation for the tracked repository. Two current-Build operational meta-statements nevertheless remained inside EXP-0012 and are removed by this closure Build.

This classification concerns the preceding audit-readiness Build, not the present closure Build. The present closure Build's result and compliance remain `Pending maintainer classification`.

## Phase 7 contract

From `docs/releases/v0.1.0/phase-list.md` (Phase 7 entry, lines 93–110) and `docs/releases/v0.1.0/release-charter.md` (Pre-release gates, Release-completion evidence, Human approval boundaries):

- **Objective:** Run the final pre-release gate end to end and prepare, but not execute, the release actions.
- **Principal deliverables:**
  1. Final pre-release validation evidence (this EXP-0012).
  2. A release-gate report (this EXP-0012).
  3. A proposed pull-request title and body (this EXP-0012, § Proposed final PR template).
  4. A proposed release title and release notes (this EXP-0012, § Proposed release).
  5. A documented intended tag (name `v0.1.0`; target: the final merged `main` commit after CI passes on that exact main commit).
  6. A closing pre-release experiment record (this EXP-0012).
  7. Confirmation that the branch is ready for the single release pull request.
- **Boundaries / exclusions:** No merging of pull requests, no commit on `main`, no CI invocation on the merged `main` commit, no creation of the annotated tag, and no publishing of a GitHub release. Those are post-merge release-completion actions, not Phase 7 outputs.
- **Completion evidence (charter):** All pre-release gates pass; release branch is clean; maintainer has accepted the pre-release report; branch is ready for the one release-bearing pull request.
- **Pre-release gates (charter, ten gates):**
  1. Phases 0 through 7 are marked `Complete`, with their required completion evidence committed.
  2. Every change in `git diff main..release/v0.1.0` is accounted for by an approved phase deliverable or documented corrective task.
  3. The complete local validation suite passes from a clean checkout on a documented supported environment.
  4. Every required GitHub status check passes on `release/v0.1.0`.
  5. Required governance, Android baseline, validation, CI, and release documents exist and are mutually consistent.
  6. A sanitized experiment record exists for every agent-assisted task, not merely one record per phase.
  7. An experiment may retain any valid outcome classification, including a blocked classification; unresolved work that prevents a phase objective or release gate from being satisfied remains release-blocking.
  8. No `Pending maintainer input` remains in release-critical evidence at the pre-release gate. `Not available` and `Not applicable` are permitted final values when accompanied by an explanation.
  9. Human review confirms that no secret, credential, unsupported capability claim, unexplained dependency, licensing problem, or unaccounted scope deviation remains.
  10. The working tree is clean and the single release pull request is opened only after all pre-release gates pass.
- **Release-train rules (AGENTS.md, "Single release-branch and one-final-PR policy"):**
  - All v0.1.0 work happens on the single branch `release/v0.1.0`. No parallel release branches.
  - Exactly one pull request is opened into `main` after all release phases and the full pre-release gate are complete.
  - One annotated tag and one GitHub release are created only after the release pull request is merged.
  - Branch creation, switching, renaming, and deletion always require a separate explicit maintainer authorization.
- **PR creation, merge, main CI, tag creation, and release publication are separate later actions** (each is a Category C action in AGENTS.md "Human approval boundaries" and requires its own explicit maintainer authorization).

## Current readiness inventory

### Direct GitHub fields (via `gh api`, read-only)

- Actual remote `refs/heads/release/v0.1.0` object SHA: `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` (matches local HEAD).
- Actual remote `refs/heads/main` object SHA: `194f40bf04fcf20f914814cd3aa34d9d3c163f1a` (matches local `origin/main`).
- Required branch check on `release/v0.1.0`: context `validate`; App ID `15368`.
- Strict: `false`. Administrator enforcement: disabled. Force pushes: disabled. Deletions: disabled.
- Ruleset `17924125`: name `Protect main`; target `branch`; enforcement `active`; included ref `~DEFAULT_BRANCH`.
- Actions permissions: `github_owned_allowed=true`, `verified_allowed=false`, `patterns_allowed=[]`.
- Pull-request collection length: `0`.
- Release collection length: `0`.
- Tag collection length: `0`.

### Direct local Git observations (via `git`)

- branch: `release/v0.1.0`.
- HEAD: `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b`, the Phase 6 Markdown-link corrective commit. Its parent is `fd4bf07a6ee6cc4f9328fc418fc84217b8e9e470`, the Phase 6 documentation closure commit. That commit's parent is `ca2c12932bfed49967f130a1a9e8cdb98f855050`, the Phase 5 closure commit.
- local origin/release/v0.1.0: `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` (synchronized).
- local origin/main: `194f40bf04fcf20f914814cd3aa34d9d3c163f1a`.
- tracked modifications: none; staged: empty; non-ignored untracked: `docs/agentic-development/experiments/EXP-0012.md` (the draft report for this Build).
- `scripts/__pycache__`: absent; `.pyc` count 0; `.opencode`: absent; `local.properties`: absent; prohibited temp files: absent.
- protected hashes: ci.yml `2b09f133…`; EXP-0008 `762ce664…`; first 37969 bytes EXP-0008 `e7ee2a1d…`; EXP-0009 `66ad268d…`; AGENTS.md blob `82231966…` (all match).

### Direct file observations (via `grep`)

- `README.md` lines 65–70: Phases 0 through 5 are `complete`; line 71: Phase 6 is `complete`; line 72: `**Phase 7** and later — the final pre-release gate: planned.`
- `docs/releases/v0.1.0/phase-list.md`: 7 `**Status.** \`Complete\`` entries (Phases 0–6) and 1 `**Initial status.** \`Planned\`` entry (Phase 7).

### Current CI evidence at the release HEAD (Phase 6 baseline only)

- Workflow run `28289137971`: workflow `CI`; event `push`; head branch `release/v0.1.0`; head SHA `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b`; status `completed`; conclusion `success`.
- Validate check-run `83818018596`: name `validate`; conclusion `success`; App ID `15368`; App slug `github-actions`.
- Logs contain: `PASS docs/md-links — all relative Markdown links resolve`; `SUMMARY pass=35 fail=0 skip=0`; `release_gate=SATISFIED`.
- This is the **current green Phase 6 baseline CI run** at the release HEAD. It is not the Phase 7 closure CI run. The Phase 7 closure CI run will be triggered by a later push of the Phase 7 closure commit and is `Pending`; its `{{PHASE7_CI_RUN_ID}}`, `{{PHASE7_VALIDATE_CHECK_RUN_ID}}`, and `{{PHASE7_CLOSURE_SHA}}` are placeholders in the proposed final PR template.

### No implemented Phase 6 harness candidate

- All C-01 through C-07 candidates from `docs/releases/v0.1.0/agent-evaluation.md` remain deferred. No AGENTS.md rule addition, no validator check, no skill, no auditor agent, no custom tool, no plugin hook, and no MCP server was implemented in v0.1.0.

## Pending-classification inventory

Search the release-critical evidence for current occurrences of `Pending maintainer input`, `Pending maintainer review`, `Draft — pending maintainer review`, and equivalent current-status wording. For each occurrence, classify it conservatively as a `Current maintainer decision required before Phase 7 closure` unless an exact later explicit maintainer classification is cited. The conclusion that an item is non-release-blocking is not drawn merely because its implementation phase is `Complete`.

### Previously accepted experiment records

The following six experiment records are kept outside the `Maintainer decisions required before Phase 7 closure` table because they already contain explicit `Accepted` maintainer classifications recorded on the documented dates.

- `docs/agentic-development/experiments/EXP-0001.md` — `## Maintainer review: Accepted on 2026-06-20`.
- `docs/agentic-development/experiments/EXP-0002.md` — `## Maintainer review: Accepted on 2026-06-20`.
- `docs/agentic-development/experiments/EXP-0003.md` — `## Maintainer review: \`Accepted — 2026-06-22\``.
- `docs/agentic-development/experiments/EXP-0004.md` — `## Maintainer review: Accepted — 2026-06-22`.
- `docs/agentic-development/experiments/EXP-0005.md` — `## Maintainer review: Accepted — 2026-06-23`.
- `docs/agentic-development/experiments/EXP-0006.md` — `## Maintainer review: Accepted — 2026-06-24`.

### Per-file findings (current pending state)

- **`docs/agentic-development/experiments/EXP-0007.md`:** `**Document status:** Draft — pending maintainer review`; the `## Status` section records `**Maintainer review:** Pending maintainer input`. Current document review state; no later explicit classification cited in the record.
- **`docs/agentic-development/experiments/EXP-0008.md`:** `**Document status:** Draft — pending maintainer review`; the `## Status` section records `**Maintainer review:** Pending maintainer input`. Current document review state; no later explicit classification cited in the record.
- **`docs/agentic-development/experiments/EXP-0009.md`:** `**Document status:** Draft — pending maintainer review`; the `## Status` section records `**Maintainer review:** Pending maintainer input`. Current document review state; no later explicit classification cited in the record.
- **`docs/agentic-development/experiments/EXP-0010.md`:** `**Document status:** Draft — pending maintainer review`; the `## Status` section records `**Maintainer review:** Pending maintainer review`. Current document review state; the `## Status` section records the supplied maintainer classification (Phase 5 repository result `Accepted`; latest push Build `Completed with correction required`; execution scope `deviation without approval`) and the repository result `Accepted for read-only maintainer review`.
- **`docs/agentic-development/experiments/EXP-0011.md`:** `**Document status:** Draft — pending maintainer review`; the `## Status` section records `**Maintainer review:** Pending maintainer input`. Current document review state; the `## Status` section records the four preceding Phase 6 draft Builds and their classifications.
- **`docs/releases/v0.1.0/agent-evaluation.md`:** `**Document status:** Draft — Phase 6 evidence draft — pending maintainer review`; `**Document version:** Draft v4 (terminal correction)`. Current document review state; no later explicit classification cited in the file.

**Summary.** Six current documents contain pending review or maintainer-input states. None of the previously accepted experiment records (EXP-0001 through EXP-0006) is classified as a pending state in this inventory.

### Maintainer decisions required before Phase 7 closure

| # | Document | Token | Location | Classification | Decision required |
|---|---|---|---|---|---|
| 1 | EXP-0007 | `Draft — pending maintainer review` and `Pending maintainer input` | document header and `## Status` section | Current document review state; no later explicit classification cited in the record. | Accept the record for v0.1.0, classify its current pending status as superseded by an exact named later maintainer decision, or require a later status correction. |
| 2 | EXP-0008 | `Draft — pending maintainer review` and `Pending maintainer input` | document header and `## Status` section | Current document review state; no later explicit classification cited in the record. | Same as #1. |
| 3 | EXP-0009 | `Draft — pending maintainer review` and `Pending maintainer input` | document header and `## Status` section | Current document review state; no later explicit classification cited in the record. | Same as #1. |
| 4 | EXP-0010 | `Draft — pending maintainer review` and `Pending maintainer review` | document header and `## Status` section | Current document review state; the `## Status` section records the supplied maintainer classification (Phase 5 repository result `Accepted`; latest push Build `Completed with correction required`; execution scope `deviation without approval`) and the repository result `Accepted for read-only maintainer review`. | Same as #1. |
| 5 | EXP-0011 | `Draft — pending maintainer review` and `Pending maintainer input` | document header and `## Status` section | Current document review state; the `## Status` section records the four preceding Phase 6 draft Builds and their classifications. | Same as #1. |
| 6 | agent-evaluation.md | `Draft — Phase 6 evidence draft — pending maintainer review` | document header | Current document review state; no later explicit classification cited in the file. | Same as #1. |

**Charter gate 8 status:** `PENDING MAINTAINER REVIEW`. No current release-critical `Pending maintainer input` has been demonstrated to be resolved by a later explicit maintainer classification for any of the six items above.

## Change-accounting inventory (pre-Phase-7 baseline only)

The pre-Phase-7 baseline diff is the exact `git diff --name-only origin/main..HEAD` output captured against the current release HEAD `eb66c7f…` (the Phase 6 Markdown-link corrective commit). The `main` SHA is `194f40bf…` (direct GitHub field; matches local `origin/main`).

### Exact pre-Phase-7 baseline file list

`git diff --name-only origin/main..HEAD` at the current release HEAD returns the following 46 paths (pre-Phase-7 baseline only; the future Phase 7 files — `EXP-0012.md` and the two status-edited files — are not yet in the diff):

```
.gitattributes
.github/ISSUE_TEMPLATE/bug_report.yml
.github/ISSUE_TEMPLATE/config.yml
.github/ISSUE_TEMPLATE/feature_request.yml
.github/PULL_REQUEST_TEMPLATE.md
.github/workflows/ci.yml
.gitignore
AGENTS.md
CODE_OF_CONDUCT.md
CONTRIBUTING.md
README.md
SECURITY.md
app/build.gradle.kts
app/src/main/AndroidManifest.xml
app/src/main/kotlin/io/github/franchoy/nudgewhen/MainActivity.kt
app/src/main/kotlin/io/github/franchoy/nudgewhen/ui/theme/Theme.kt
app/src/main/res/values/strings.xml
app/src/main/res/values/themes.xml
build.gradle.kts
docs/agentic-development/evaluation-template.md
docs/agentic-development/experiment-protocol.md
docs/agentic-development/experiments/EXP-0001.md
docs/agentic-development/experiments/EXP-0002.md
docs/agentic-development/experiments/EXP-0003.md
docs/agentic-development/experiments/EXP-0004.md
docs/agentic-development/experiments/EXP-0005.md
docs/agentic-development/experiments/EXP-0006.md
docs/agentic-development/experiments/EXP-0007.md
docs/agentic-development/experiments/EXP-0008.md
docs/agentic-development/experiments/EXP-0009.md
docs/agentic-development/experiments/EXP-0010.md
docs/agentic-development/experiments/EXP-0011.md
docs/agentic-development/opencode-governance.md
docs/local-validation.md
docs/releases/v0.1.0/agent-evaluation.md
docs/releases/v0.1.0/phase-list.md
docs/releases/v0.1.0/release-charter.md
gradle.properties
gradle/libs.versions.toml
gradle/wrapper/gradle-wrapper.jar
gradle/wrapper/gradle-wrapper.properties
gradlew
gradlew.bat
scripts/validate-local.sh
scripts/validate_local.py
settings.gradle.kts
```

### Per-path-group phase / corrective-task accounting

The 46 paths are grouped by phase deliverable or corrective task. The mapping is based on the existing record evidence (`docs/agentic-development/experiments/EXP-0001.md` through `EXP-0011.md`) and on the authoritative `docs/releases/v0.1.0/release-charter.md`.

| Path group | Paths (pre-Phase-7 baseline) | Phase / corrective task | Evidence |
|---|---|---|---|
| Branch-bootstrap evidence | `docs/agentic-development/experiments/EXP-0001.md` | Phase 0 predecessor / branch-bootstrap evidence | EXP-0001 |
| Release-charter and protocol evidence | `docs/agentic-development/evaluation-template.md`, `docs/agentic-development/experiment-protocol.md`, `docs/releases/v0.1.0/release-charter.md` | Phase 0 release-charter and protocol evidence | EXP-0002 |
| Open-source community baseline (governance and templates) | `.gitattributes`, `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/PULL_REQUEST_TEMPLATE.md`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `SECURITY.md`, `.gitignore` | Phase 1 (open-source community baseline) | EXP-0003 |
| Community-baseline evidence | `docs/agentic-development/experiments/EXP-0003.md` | Phase 1 community-baseline evidence | EXP-0003 |
| Phase 0 corrective evidence | `docs/agentic-development/experiments/EXP-0004.md` | Phase 0 corrective evidence | EXP-0004 |
| OpenCode governance evidence | `docs/agentic-development/experiments/EXP-0005.md`, `docs/agentic-development/opencode-governance.md` | Phase 2 OpenCode-governance evidence | EXP-0005 |
| OpenCode governance baseline | `AGENTS.md` | Phase 2 OpenCode governance baseline | EXP-0005 |
| Android-baseline evidence | `docs/agentic-development/experiments/EXP-0006.md` | Phase 3 Android-baseline evidence | EXP-0006 |
| Android technical baseline (Gradle and Android project) | `gradle.properties`, `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.jar`, `gradle/wrapper/gradle-wrapper.properties`, `gradlew`, `gradlew.bat`, `settings.gradle.kts`, `build.gradle.kts`, `app/build.gradle.kts`, `app/src/main/AndroidManifest.xml`, `app/src/main/kotlin/io/github/franchoy/nudgewhen/MainActivity.kt`, `app/src/main/kotlin/io/github/franchoy/nudgewhen/ui/theme/Theme.kt`, `app/src/main/res/values/strings.xml`, `app/src/main/res/values/themes.xml` | Phase 3 (Android technical baseline) | EXP-0006 |
| Local-validation evidence | `docs/agentic-development/experiments/EXP-0007.md` | Phase 4 local-validation evidence | EXP-0007 |
| Local validation baseline | `scripts/validate_local.py`, `scripts/validate-local.sh`, `docs/local-validation.md` | Phase 4 (local validation baseline) | EXP-0007 |
| Phase 5 planning evidence | `docs/agentic-development/experiments/EXP-0008.md` | Phase 5 planning evidence | EXP-0008 |
| Phase 5 corrective evidence | `docs/agentic-development/experiments/EXP-0009.md` | Phase 5 corrective evidence | EXP-0009 |
| Phase 5 closure evidence | `docs/agentic-development/experiments/EXP-0010.md` | Phase 5 closure evidence | EXP-0010 |
| GitHub Actions CI baseline | `.github/workflows/ci.yml`, `docs/local-validation.md` (CI release-gate documentation) | Phase 5 (GitHub Actions CI baseline) | EXP-0008, EXP-0009, EXP-0010 |
| Phase 6 evaluation evidence | `docs/agentic-development/experiments/EXP-0011.md`, `docs/releases/v0.1.0/agent-evaluation.md` | Phase 6 evaluation evidence | EXP-0011; Phase 6 documentation closure commit `fd4bf07…`; Phase 6 Markdown-link corrective commit `eb66c7f…` |
| README introduction and status synchronization | `README.md` | Phase 1 (earlier baseline documentation introduction), Phase 3 (Android-baseline expansion where supported), Phase 6 (status synchronization) | EXP-0003, EXP-0006, EXP-0011 |
| Phase-list release-planning material and status synchronization | `docs/releases/v0.1.0/phase-list.md` | Phase 0 (release-planning material), Phase 6 (status synchronization) | EXP-0002, EXP-0011 |

**Note.** `README.md` and `docs/releases/v0.1.0/phase-list.md` are tracked in the table as paths that were initially introduced or expanded in earlier phases and later updated for status synchronization through Phase 6. The future Phase 7 closure Build is expected to update these two paths with Phase 7 status entries, but those future edits are not part of the current pre-Phase-7 baseline diff.

### Phase 5 closure, Phase 6 documentation closure, and Phase 6 Markdown-link corrective commit identities

- **Phase 5 closure commit:** `ca2c12932bfed49967f130a1a9e8cdb98f855050` (subject: `docs: close Phase 5 with remote-completion evidence`; successful CI run: `28286239984`).
- **Phase 6 documentation closure commit:** `fd4bf07a6ee6cc4f9328fc418fc84217b8e9e470` (subject: `docs: close Phase 6 with agent evaluation evidence`).
- **Phase 6 Markdown-link corrective commit:** `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` (subject: `docs: fix EXP-0011 markdown link`; successful CI run: `28289137971`; validate check-run: `83818018596`).

The current release HEAD is `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` (the Phase 6 Markdown-link corrective commit). Its parent is `fd4bf07a6ee6cc4f9328fc418fc84217b8e9e470` (the Phase 6 documentation closure commit), whose parent is `ca2c12932bfed49967f130a1a9e8cdb98f855050` (the Phase 5 closure commit).

### Pre-Phase-7 baseline totals

`git diff --stat origin/main..HEAD` (pre-Phase-7 baseline only) reports the pre-Phase-7 baseline totals. The current observation is `46 files changed, 9222 insertions, 2 deletions`. These are the **pre-Phase-7 baseline only** totals; the final PR statistics will be computed in the Phase 7 closure Build after the Phase 7 status edits and `EXP-0012.md` are committed.

## Pre-release gate table

The ten pre-release gates from `docs/releases/v0.1.0/release-charter.md` are evaluated in this draft. Each gate is `PASS`, `BLOCKED`, or `PENDING MAINTAINER REVIEW`. A gate may be `PASS` only when supported by direct final evidence.

| # | Gate | Status | Evidence / blocker |
|---|---|---|---|
| 1 | Phases 0 through 7 are marked `Complete`, with their required completion evidence committed. | **BLOCKED** | Phases 0 through 6 are `Complete` (direct file observation in `README.md` and `phase-list.md`). Phase 7 is `Planned` (direct file observation). This Build is the draft report, not the closure. |
| 2 | Every change in `git diff main..release/v0.1.0` is accounted for by an approved phase deliverable or documented corrective task. | **PENDING MAINTAINER REVIEW** | The pre-Phase-7 baseline diff is `46 files changed, 9222 insertions, 2 deletions` (pre-Phase-7 baseline only). The exact 46-path list and the per-path-group phase / corrective-task accounting are in the § Change-accounting inventory section. The final PR diff will be computed in the Phase 7 closure Build; the present accounting covers only the pre-Phase-7 baseline. The maintainer is to confirm the accounting. |
| 3 | The complete local validation suite passes from a clean checkout on a documented supported environment. | **PENDING MAINTAINER REVIEW** | The complete local validation suite includes the required, documentation, and android groups. The current remote CI run `28289137971` ran the complete suite and emitted `SUMMARY pass=35 fail=0 skip=0` and `release_gate=SATISFIED`. The local environment may lack Java for the android group (observed in the Phase 6 link-correction Build). The Phase 7 closure CI run (to be triggered by the Phase 7 closure push) will be the authoritative complete validation at the Phase 7 closure commit; its `{{PHASE7_CI_RUN_ID}}`, `{{PHASE7_VALIDATE_CHECK_RUN_ID}}`, and `{{PHASE7_CLOSURE_SHA}}` are placeholders. The maintainer is to confirm whether the charter permits the current remote CI as authoritative for this gate or whether the Phase 7 closure CI is required. |
| 4 | Every required GitHub status check passes on `release/v0.1.0`. | **PASS** | The required check `validate` (App ID `15368`) is the current release-head baseline CI; the most recent CI run at the current release HEAD `eb66c7f…` (run `28289137971`) concluded `success`; the validate check-run `83818018596` concluded `success`. The future Phase 7 closure CI run is `Pending`; the maintainer is to confirm the Phase 7 closure CI run passes. |
| 5 | Required governance, Android baseline, validation, CI, and release documents exist and are mutually consistent. | **PENDING MAINTAINER REVIEW** | The documents exist (direct file observation). Mutual consistency is a maintainer review item; the maintainer is to confirm that `AGENTS.md`, `docs/agentic-development/opencode-governance.md`, `docs/local-validation.md`, `.github/workflows/ci.yml`, and `docs/releases/v0.1.0/release-charter.md` are mutually consistent. |
| 6 | A sanitized experiment record exists for every agent-assisted task, not merely one record per phase. | **PENDING MAINTAINER REVIEW** | EXP-0001 through EXP-0011 are tracked experiment records; EXP-0012 exists as the authorized untracked Phase 7 draft. Whether every agent-assisted task is covered remains `PENDING MAINTAINER REVIEW`. |
| 7 | An experiment may retain any valid outcome classification, including a blocked classification; unresolved work that prevents a phase objective or release gate from being satisfied remains release-blocking. | **PENDING MAINTAINER REVIEW** | Gate 1 is currently BLOCKED because Phase 7 remains Planned; that expected current block can be resolved only by the Phase 7 closure commit and its accepted validation evidence. Separately, the maintainer must confirm that no historical experiment outcome or unresolved classification blocks any earlier phase objective or release gate. |
| 8 | No `Pending maintainer input` remains in release-critical evidence at the pre-release gate. | **PENDING MAINTAINER REVIEW** | The § Pending-classification inventory identifies six current documents with pending review or maintainer-input states (EXP-0007, EXP-0008, EXP-0009, EXP-0010, EXP-0011, agent-evaluation.md). The maintainer is to confirm whether each item is resolved, classified as superseded, or requires a later status correction. |
| 9 | Human review confirms that no secret, credential, unsupported capability claim, unexplained dependency, licensing problem, or unaccounted scope deviation remains. | **PENDING MAINTAINER REVIEW** | The maintainer is to perform this review. |
| 10 | The working tree is clean and the single release pull request is opened only after all pre-release gates pass. | **PENDING MAINTAINER REVIEW** | The repository is currently clean except for the authorized untracked `EXP-0012.md` draft. The PR has correctly not been opened yet. PR creation belongs to a later separate authorization after all preceding gates and the Phase 7 closure CI are accepted. PR absence is not itself a blocker for this gate; PR opening is a later action gated on the acceptance of all preceding gates and the Phase 7 closure CI. |

## Proposed final PR template — not yet approved or ready to submit

The following is a proposed final PR template. It is prepared but not submitted in this Build. PR creation is a separate explicit maintainer authorization.

- **PR base branch:** `main` (at `194f40bf04fcf20f914814cd3aa34d9d3c163f1a`).
- **PR head branch:** `release/v0.1.0` (at the Phase 7 closure commit).
- **PR head SHA:** `Pending Phase 7 closure commit` (`{{PHASE7_CLOSURE_SHA}}`).
- **Proposed PR title:** `v0.1.0: release NudgeWhen methodology baseline`

### Proposed PR Markdown body

```markdown
## v0.1.0 — NudgeWhen methodology baseline

### Summary

v0.1.0 is the first release of NudgeWhen. It does not yet contain a useful
application; it establishes only the static Android technical baseline
(declared baseline content, no reminder behavior) and the first methodology
baseline for evaluating the agent-assisted workflow. The release is
per `docs/releases/v0.1.0/release-charter.md`.

### Phase status

| Phase | Title | Status |
|---|---|---|
| 0 | Release charter and experiment protocol | Complete |
| 1 | Open-source community baseline | Complete |
| 2 | OpenCode governance baseline | Complete |
| 3 | Minimal static Android technical baseline | Complete |
| 4 | Local validation baseline | Complete |
| 5 | GitHub Actions CI baseline | Complete |
| 6 | Agent evaluation evidence | Complete |
| 7 | Pre-release gate and release-pull-request preparation | {{PHASE7_STATUS}} |

### Pre-release gate checklist

The ten pre-release gates from `docs/releases/v0.1.0/release-charter.md`
are listed below with their current status at the time of the Phase 7
closure commit `{{PHASE7_CLOSURE_SHA}}`. A gate is `PASS` only when supported
by direct final evidence; otherwise it is `PENDING MAINTAINER REVIEW` or
`BLOCKED`.

1. Phases 0 through 7 are marked `Complete`, with their required completion
   evidence committed. — To be confirmed before PR creation.
2. Every change in `git diff main..release/v0.1.0` is accounted for by an
   approved phase deliverable or documented corrective task. — To be
   confirmed before PR creation.
3. The complete local validation suite passes from a clean checkout on a
   documented supported environment. — To be confirmed before PR creation
   against the Phase 7 closure commit.
4. Every required GitHub status check passes on `release/v0.1.0`. — To be
   confirmed by the Phase 7 closure CI run (`{{PHASE7_CI_RUN_ID}}`) and the
   validate check-run (`{{PHASE7_VALIDATE_CHECK_RUN_ID}}`).
5. Required governance, Android baseline, validation, CI, and release
   documents exist and are mutually consistent. — To be confirmed before PR
   creation.
6. A sanitized experiment record exists for every agent-assisted task. —
   To be confirmed before PR creation.
7. No blocked classification prevents a phase objective or release gate from
   being satisfied. — To be confirmed before PR creation.
8. No `Pending maintainer input` remains in release-critical evidence. — To
   be confirmed before PR creation.
9. Human review confirms no secret, credential, unsupported capability
   claim, unexplained dependency, licensing problem, or unaccounted scope
   deviation remains. — To be confirmed by the maintainer; human review is
   pending.
10. The working tree is clean and the single release pull request is opened
    only after all pre-release gates pass. — To be confirmed before PR
    creation.

### Change-accounting summary

The pre-Phase-7 baseline diff between `main` and `release/v0.1.0` (at the
current release HEAD `eb66c7f…`) is `46 files changed, 9222 insertions,
2 deletions`. The final PR diff (at the Phase 7 closure commit
`{{PHASE7_CLOSURE_SHA}}`) will include the Phase 7 status edits to
`README.md` and `docs/releases/v0.1.0/phase-list.md` and the addition of
`docs/agentic-development/experiments/EXP-0012.md`. The final PR statistics
are `{{FINAL_DIFF_FILE_COUNT}}` files changed, `{{FINAL_DIFF_INSERTIONS}}`
insertions, `{{FINAL_DIFF_DELETIONS}}` deletions. Every path group is
accounted for in `EXP-0012` § Change-accounting inventory.

### Validation and CI evidence

- Local validator: `scripts/validate-local.sh` (the Phase 4 local validation
  suite). CI workflow: `.github/workflows/ci.yml`. Required check: `validate`
  (GitHub App ID `15368`).
- Phase 6 baseline CI run at the corrective commit: `28289137971`; validate
  check-run `83818018596`; conclusion `success`. Logs contain
  `PASS docs/md-links`, `SUMMARY pass=35 fail=0 skip=0`,
  `release_gate=SATISFIED`. This is the current green Phase 6 baseline CI
  run, not the Phase 7 closure CI run.
- Phase 7 closure CI run: `{{PHASE7_CI_RUN_ID}}`; validate check-run
  `{{PHASE7_VALIDATE_CHECK_RUN_ID}}`. The Phase 7 closure CI run is
  `Pending` and will be triggered by the push of the Phase 7 closure
  commit `{{PHASE7_CLOSURE_SHA}}`.

### Known historical deviations and their later classifications

- Phase 5 closed at `ca2c129…` with CI run `28286239984`.
- Phase 6 documentation closed at `fd4bf07…`.
- The Phase 6 Markdown-link defect was corrected at `eb66c7…`.
- Phase 6 documentation closure Build (EXP-0011) recorded four Phase 6
  draft Builds classified `Completed with correction required` /
  `deviation without approval`. Phase 6 is `Complete`; the Phase 6
  documentation closure commit `fd4bf07…` and the Markdown-link
  corrective commit `eb66c7…` are on `release/v0.1.0`; the Phase 6
  baseline CI run `28289137971` is the final successful current
  release-head baseline.
- The final Phase 6 push Build recorded a `Completed with correction
  required` / `deviation without approval` classification (the polling used
  standalone Python despite the explicit prohibition; the corrective commit
  pushed and the required validate check and release gate passed). The
  closure is accepted on the release branch.

### Out of scope (post-merge actions)

Per `docs/releases/v0.1.0/release-charter.md` and AGENTS.md
"Single release-branch and one-final-PR policy":

- Merging this pull request.
- CI on the merged `main` commit.
- Creating the annotated tag `v0.1.0` on the merged `main` commit.
- Publishing the GitHub release from the `v0.1.0` tag.

### Maintainer sign-off

- [ ] Accept the pre-release report (EXP-0012).
- [ ] Confirm the pre-release gate checklist above.
- [ ] Approve opening this single release-bearing pull request.
- [ ] Approve the post-merge sequence (CI on `main`, tag `v0.1.0`,
  GitHub release).
```

### Evidence not to include in the PR body

- Personal email values (none present in the report).
- Private session identifiers (none present in the report).
- Machine names (none present in the report).
- Unnecessary local workspace paths (none present in the report).
- APK binary data (not present in the report; APK is uploaded as a CI artifact
  but is not committed as evidence).

## Proposed release

Prepared but not executed in this Build. Release publication is a separate
explicit maintainer authorization. The present-tense validation statements
below are conditional on the future Phase 7 closure CI and post-merge CI
passing; they will be confirmed at the time of release publication.

- **Proposed GitHub release title:** `NudgeWhen v0.1.0 — methodology baseline`
- **Proposed Markdown release notes** (to be confirmed at release time):

```markdown
# NudgeWhen v0.1.0 — methodology baseline

NudgeWhen v0.1.0 is the first release of the project. It does not yet contain
a useful application; it establishes only the static Android technical
baseline (declared baseline content, no reminder behavior) and the first
methodology baseline for evaluating the agent-assisted workflow.

## What's in v0.1.0

- A static Android baseline that displays declared baseline content and
  contains no reminder or voice behavior.
- An open-source governance baseline (license, contribution guide, code of
  conduct, security policy, governance documents).
- An OpenCode operating-rules contract (`AGENTS.md`) and a companion
  governance document.
- A repeatable local validation suite (`scripts/validate-local.sh`,
  `scripts/validate_local.py`).
- A basic GitHub Actions CI workflow that runs the local validation suite
  on every push and pull request to `release/v0.1.0` and requires the
  `validate` check (GitHub App ID `15368`).
- Twelve experiment records (EXP-0001 through EXP-0012) documenting the
  agent-assisted workflow and its outcomes, plus the Phase 6 evaluation
  summary that records the lessons observed across Phases 0 through 5 and
  lists seven deferred harness candidates (C-01 through C-07) for future
  releases.

## What's not in v0.1.0

- Reminder functionality (scheduling, notifications, storage of reminders,
  or any related behavior).
- Voice or speech functionality (recording, transcription, or synthesis).
- Hermes integration or configuration.
- A production-grade Android application.
- A published application binary distributed outside the validated release
  artifact (the CI uploads `app-debug-apk` as a workflow artifact, but the
  application is not published as a release asset).
- A separate release branch or release pull request outside the single
  `release/v0.1.0` branch and the one release-bearing pull request.
- Any of the seven Phase 6 deferred harness candidates (C-01 through C-07).

## Validation

The local validation suite will be confirmed to pass on a clean checkout
on a documented supported environment at the time of the Phase 7 closure
commit. The CI workflow on `release/v0.1.0` will be confirmed to require
the `validate` check (GitHub App ID `15368`) and to emit
`release_gate=SATISFIED` on the complete validator suite at the Phase 7
closure commit and on the post-merge `main` commit.

## Artifacts

The CI uploads `app-debug-apk` as a workflow artifact (14-day retention).
The artifact is not redistributed as a release asset; it is for validation
and review.
```

### Documented intended tag

- **Name:** `v0.1.0`
- **Target semantics:** the final merged `main` commit after CI passes on that exact `main` commit.
- **Target SHA:** `Pending post-merge validation`. The current release-branch SHA `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` is **not** identified as the final tag target; the tag is created only on the merged `main` commit after the release-bearing pull request is merged and CI passes on that merged `main` commit.

## Required closure sequence

The Phase 7 closure Build must follow this exact sequence. Do not invent a Phase 8.

1. **Maintainer audit and acceptance of EXP-0012.** The maintainer reviews the pre-release report and accepts it (or requests further corrections). The § Pending-classification inventory, the § Change-accounting inventory, the § Pre-release gate table, and the § Proposed final PR template are reviewed.
2. **Resolution of any current pending-classification item.** The maintainer resolves any current unresolved maintainer decisions about the v0.1.0 release identified in the § Pending-classification inventory.
3. **Phase 7 closure Build.**
   - Synchronize only Phase 7 status: `README.md` and `docs/releases/v0.1.0/phase-list.md` change `Phase 7` from `Planned` to `Complete`.
   - Stage `EXP-0012.md` plus the two status files (exactly three paths).
   - Run final `docs` validation after `EXP-0012` is staged and therefore tracked by the candidate index.
   - Create one local closure commit (one parent; the current HEAD `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b`; no amend, no rebase, no push).
4. **Separate push authorization.** The maintainer issues a separate explicit authorization to push the Phase 7 closure commit. Push is `git push --porcelain origin release/v0.1.0:release/v0.1.0`; no force, no tag.
5. **Successful CI on the exact closure commit.** The push-triggered CI run must conclude `success`; the `validate` check must conclude `success`; the `release_gate=SATISFIED` literal must be present in the logs. A hard stop on any FAIL or BLOCKED.
6. **Separate authorization to create the one release-bearing pull request.** PR creation is a separate explicit maintainer authorization. The PR base is `main`; the PR head is `release/v0.1.0`; the PR title and body are documented in § Proposed final PR template.
7. **Post-merge main CI.** After the PR is merged, CI on the resulting `main` commit must conclude `success`. A hard stop on any FAIL or BLOCKED.
8. **Annotated tag and GitHub release only through later explicit authorizations.** The tag `v0.1.0` is created on the merged `main` commit only after the post-merge CI passes. The GitHub release is published from the tag only after the tag exists. Both are separate explicit maintainer authorizations.

## Draft validation (existing tracked repository state)

The authorized validation scope for this draft is the required and documentation groups of the repository validator. Because EXP-0012 remains untracked, those commands validate only the existing tracked repository state. Actual command execution and outcomes are recorded externally after transcript review.

## Results

- **Validation requested:** required-group and documentation-group of the Phase 4 validator, each run exactly once via the mandated shell form with `&&`.
- **First-pass result:** `Pending maintainer classification`.
- **Final result:** `Pending maintainer classification`.
- **Scope compliance:** `Pending maintainer classification`.

## Assessment

The current Build result, execution scope and repository outcome remain `Pending maintainer classification`. Operational conclusions are recorded externally after transcript review.

## Maintainer audit resolution — 2026-06-28

The read-only maintainer audit dated 2026-06-28 is final. The following decisions are accepted for v0.1.0:

- EXP-0007 is accepted as Phase 4 local-validation evidence.
- EXP-0008 is accepted as Phase 5 planning evidence, retaining its recorded `deviation without approval`.
- EXP-0009 is accepted as Phase 5 corrective evidence.
- EXP-0010 is accepted as Phase 5 closure evidence.
- EXP-0011 is accepted as Phase 6 evaluation and closure evidence.
- `docs/releases/v0.1.0/agent-evaluation.md` is accepted as the Phase 6 evaluation summary.
- C-01 through C-07 remain deferred.

The pending review or maintainer-input wording in those documents is superseded for the v0.1.0 release gate by this exact maintainer decision. EXP-0001 through EXP-0011 and `agent-evaluation.md` are not modified.

The following gates are accepted as `PASS`:

- Gate 2: PASS.
- Gate 5: PASS.
- Gate 6: PASS.
- Gate 7: PASS for historical outcomes and earlier phase objectives.
- Gate 8: PASS through the explicit superseding maintainer decisions above.
- Gate 9: PASS.

The following gates still require the closure candidate, its CI, and the later PR authorization:

- Gate 1: requires the Phase 7 status synchronization and the local closure commit.
- Gates 3 and the final Phase 7 form of Gate 4: require successful CI on the exact closure commit.
- Gate 10: requires the committed branch to be clean and the one PR to be created only through a later explicit authorization after closure CI succeeds.

The existing draft gate table is retained as the pre-audit snapshot. This maintainer-audit resolution supersedes its maintainer-review entries where explicitly resolved.

The present closure commit and its future CI do not yet exist. They are recorded as `Pending` until the closure Build produces them and the maintainer reviews the push-triggered CI evidence.

## Status

- **Maintainer review:** Accepted for Phase 7 closure on 2026-06-28.
- No future experiment ID is assigned by this record.
- Phase 7 status synchronization is part of this authorized closure candidate.
- Push, pull-request creation, merge, tag creation and release publication remain separately authorized later actions.

## Cross-references

- Phase 7 contract: [../../releases/v0.1.0/phase-list.md](../../releases/v0.1.0/phase-list.md)
- Release charter: [../../releases/v0.1.0/release-charter.md](../../releases/v0.1.0/release-charter.md)
- Phase 6 evaluation summary: [../../releases/v0.1.0/agent-evaluation.md](../../releases/v0.1.0/agent-evaluation.md)
- Phase 5 closure commit: `ca2c12932bfed49967f130a1a9e8cdb98f855050` (subject: `docs: close Phase 5 with remote-completion evidence`; successful CI run: `28286239984`)
- Phase 6 documentation closure commit: `fd4bf07a6ee6cc4f9328fc418fc84217b8e9e470` (subject: `docs: close Phase 6 with agent evaluation evidence`)
- Phase 6 Markdown-link corrective commit: `eb66c7ffc4c5699b15d234a9d84e26920cfbfe1b` (subject: `docs: fix EXP-0011 markdown link`; successful CI run: `28289137971`; validate check-run: `83818018596`)
- AGENTS.md: [../../../AGENTS.md](../../../AGENTS.md)
- Experiment protocol: [../experiment-protocol.md](../experiment-protocol.md)
- Evaluation template: [../evaluation-template.md](../evaluation-template.md)
- OpenCode governance: [../opencode-governance.md](../opencode-governance.md)
- Local validation: [../../local-validation.md](../../local-validation.md)
- CI workflow: [../../../.github/workflows/ci.yml](../../../.github/workflows/ci.yml)
- Validator: [../../../scripts/validate_local.py](../../../scripts/validate_local.py)
- Validator entry: [../../../scripts/validate-local.sh](../../../scripts/validate-local.sh)
- Previously accepted experiment records: [EXP-0001](EXP-0001.md), [EXP-0002](EXP-0002.md), [EXP-0003](EXP-0003.md), [EXP-0004](EXP-0004.md), [EXP-0005](EXP-0005.md), [EXP-0006](EXP-0006.md)
- Current pending-state experiment records: [EXP-0007](EXP-0007.md), [EXP-0008](EXP-0008.md), [EXP-0009](EXP-0009.md), [EXP-0010](EXP-0010.md), [EXP-0011](EXP-0011.md)
- Current pending-state evaluation summary: [../../releases/v0.1.0/agent-evaluation.md](../../releases/v0.1.0/agent-evaluation.md)
