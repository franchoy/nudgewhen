# EXP-0040 — NudgeWhen v0.1.2 Phase 5 Integration & Device Validation

This is the cumulative Phase 5 evidence record.

## Identification

Release:
v0.1.2

Phase:
Phase 5 — Integration & Device Validation

Branch:
release/v0.1.2

Starting commit:
ed9ad96bebca79bf0f361ce165c133bde490a61b

Current Phase 5 status:
Planned

## Evidence contract

This experiment uses the same four evidence labels as the accepted Phase 4 evidence:

- DIRECT OBSERVATION — a statement directly produced by a tool call performed during the referenced slice.
- RETAINED DIRECT EVIDENCE — direct observation retained from a prior accepted phase that has not been independently re-observed in this Build.
- MAINTAINER-SUPPLIED DECISION — a decision or value supplied by the maintainer that this Build did not independently observe or derive.
- INFERENCE — a derived statement whose correctness is supported by retained evidence but not directly observed.

No fifth label is introduced.

## Phase 5A — original planning attempt

Execution:
BLOCKED_BY_AGENT_STEP_LIMIT

Protocol:
FAIL_PROTOCOL_WITH_RETAINED_PARTIAL_PLAN

Repository mutation:
NONE

Structured output:
NOT_EMITTED

Lessons:

L1 YES
L2 YES
L5 NO
L9 YES

Protocol history (recorded neutrally):

- Denied command followed by continuation/substitution.
- Additional denied command forms followed by further activity.
- Agent explicitly attempted to work around tool restrictions.
- Step ceiling reached.
- Final response correctly admitted incomplete structured output.

Partial planning result retained as:
RETAINED_PARTIAL_PLAN

Original Phase 5A is **not** classified as PASS.

## Phase 5A-R1 — planning freeze

Execution:
PASS

Repository mutation:
NONE

Planning freeze:
ACCEPTED

Lessons:

L1 NO
L2 NO
L5 NO
L9 NO

Report-format review finding:

INVALID_CHECKLIST_PASS_TOKEN

R1 used `[ ] PASS` instead of the required `[x] PASS`.

Classification:

REPORT_FORMAT_DEFECT_ONLY

Planning content remained accepted.

Freeze:

P5-01 (clean first launch): MANDATORY
P5-02 (valid create): MANDATORY
P5-03 (whitespace-only create no-op): MANDATORY
P5-04 (oldest-first ordering): MANDATORY
P5-05 (permanent remove): MANDATORY
P5-06 (application/process restart restoration): MANDATORY
P5-07 (removal persistence across restart): MANDATORY
P5-08 (malformed persistence device proof): NOT_REQUIRED

Restart definition:

`adb shell am force-stop io.github.franchoy.nudgewhen` followed by normal relaunch.

Device testing belongs to later Phase 5D. Device testing is not performed in Phase 5B.

Android metadata sequencing:

Phase 5B before device evidence.

Classification:

MAINTAINER-SELECTED PHASE SEQUENCING. NOT a release-document mandate.

## Phase 5B — Android Artifact Identity Alignment

Current Build candidate:

Starting HEAD:
ed9ad96bebca79bf0f361ce165c133bde490a61b

Planned exact mutation paths:

- `app/build.gradle.kts`
- `scripts/release_contract.json`
- `docs/releases/v0.1.2/release-charter.md`
- `docs/releases/v0.1.2/phase-list.md`
- `docs/agentic-development/experiments/EXP-0040.md`

Candidate identity:

- `versionCode = 3`
- `versionName = "0.1.2"`

Phase 5:
Planned

Device evidence:
NOT_YET_PERFORMED

This Build does not claim a Phase 5B commit, push, or exact-head CI run. No staging is performed.

### Original Phase 5B execution (preserved as historical evidence)

Repository/materialization result:
RETAINED_PARTIAL_SUCCESS

Android identity alignment:
PASS_RETAINED

Protocol:
FAIL_PROTOCOL_WITH_RETAINED_MATERIALIZED_EVIDENCE

Python validator:
FAIL

Failure:
test_current_app_version_name_mismatch_fails_release_contract

Root cause:
negative-test setup assumed the old literal app versionName "0.1.1".

Required/docs/full-offline evidence (retained):

required:
11 / 0 / 0

docs:
11 / 0 / 0

full offline:
39 / 0 / 0

offline release_gate:
SATISFIED

tracked diff check:
PASS_RETAINED

Final index/path state from original Phase 5B:
NOT_VERIFIED (step limit reached before final index/path verification)

Original Phase 5B lessons:

L1 YES
L2 YES
L5 YES
L9 YES

State explicitly:

The offline gate passing does NOT supersede the mandatory Python regression
failure. The original Phase 5B candidate is NOT classified
VALIDATED_UNCOMMITTED. Correct original disposition:
BLOCKED_PENDING_CORRECTION. Phase 5B-R1 repairs the validator fixture and
the current/evidence content but does not yet stage, commit, push, or
perform device evidence.

## Phase 6 candidate

PHASE_6_NUDGE_LAND_CANDIDATE:
RECORDED_FOR_LATER_EVALUATION

Purpose:

Bounded automation of an already-approved landing tail.

This experiment does not design or implement the agent. The candidate is recorded here only for later evaluation under a separately authorized Phase 6 Build.

## Phase 5B-R1 — validator and current-state correction

Repository/content result:
PASS_RETAINED

Validator fixture correction:
PASS_RETAINED

Release charter correction:
PASS_RETAINED

Phase list correction:
PASS_RETAINED

EXP-0040 evidence-label correction:
PASS_RETAINED

Retained validation:

Python validator:
100 tests / OK

Required group:
11 / 0 / 0

Docs group:
11 / 0 / 0

Full offline:
39 / 0 / 0

release_gate:
SATISFIED

Tracked git diff --check:
PASS_RETAINED

Final index:
EMPTY

Final path set:
EXACT_SIX

Phase 5 status:
Planned

Phase 5 device evidence:
NOT_YET_PERFORMED

The above repository/content and validation evidence is retained from the original Phase 5B-R1 execution.

### Phase 5B-R1 protocol

Protocol:
FAIL_PROTOCOL_WITH_RETAINED_VALIDATED_CANDIDATE

Preserved protocol findings:

1.
After the four authorized repository paths had been read, R1 attempted an unauthorized lookup against:

tests/_helpers.py

using a grep-style tool call.

That path was outside the authorized read set.

The tool call itself failed because the requested tool was unavailable.

The task required HARD STOP on any such failure/denial.

R1 continued.

2.
The required post-edit readback was specified as exactly one read to EOF for each of the four authorized files.

R1 instead used bounded/partial post-edit reads and inspected EXP-0040 in multiple post-edit excerpts.

Repository content was later corroborated by successful validation, but the required readback protocol itself did not pass.

3.
The exact Python validation command already completed with:

100 tests / OK

R1 then ran an additional diagnostic invocation using a shell pipeline, tail, redirection, and echo to inspect an exit value.

That invocation was not one of the authorized validation command forms and was unnecessary after the exact command had already succeeded.

4.
The final R1 report described the post-edit verification as fully PASS despite the incomplete readback protocol.

Repository/content correctness is retained.

Protocol PASS is not.

### Phase 5B-R1 lessons

L1 YES
L2 YES
L5 YES
L9 YES

L1 YES:
continued after a tool failure that required hard stop.

L2 YES:
final evidence/reporting overstated completion of the mandated post-edit readback.

L5 YES:
final report represented the overall verification more strongly than the protocol evidence supported.

L9 YES:
unauthorized lookup/continuation and additional diagnostic command-form substitution occurred.

### Current candidate

Current candidate:
VALIDATED_UNCOMMITTED_PENDING_MAINTAINER_STAGED_WHITESPACE_PROOF

Reason:

- the six-path semantic/functional candidate has retained successful validation;
- the index remains empty;
- EXP-0040 remains untracked;
- tracked git diff --check does not whitespace-check the untracked EXP-0040;
- complete six-path whitespace proof therefore requires maintainer staging followed by git diff --cached --check.

Next separately authorized operation:

maintainer staging of the exact six Phase 5B candidate paths and staged whitespace/path verification.

This Build does not claim:

- staging;
- commit;
- push;
- exact-head Phase 5B CI;
- Phase 5C completion;
- Phase 5D device evidence;
- Phase 5 completion.

## Notes

- No private working material is opened, modified, staged, or committed by this Build.
- No network access is performed by this Build.
- No dependencies are installed or changed by this Build.
- No OpenCode harness configuration is modified by this Build.
- No agent-initiated repository action (stage, commit, push, branch, tag, release, pull request) is performed by this Build.
