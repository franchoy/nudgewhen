# EXP-0056 — Phase 3 Persistence Compatibility / Migration Proof — Build R1 + R1/R2 Evidence Corrections

## Identification

- Experiment ID: `EXP-0056`
- Date: `2026-09-14`
- Release: `v0.1.4`
- Phase: `Phase 3 — Persistence Compatibility / Migration Proof`
- Repository: `franchoy/nudgewhen`
- Branch: `release/v0.1.4`
- Starting commit: `fe876b86977f0f34bded3c3e774ffcaec016591f`
- Ending commit: `Not applicable — no commit was authorized or produced by EXP-0056.`
- Execution attempt or stage identifier: `Build R1 — Attempts 1, 2, 3A, and 3B plus R1 and R2 bounded evidence corrections`

## Tool and model

- Agent: `nudge-build`
- Mode: `Build`
- Access service / provider: Not available — the provider field was not directly displayed in the retained Build-session evidence for EXP-0056. AGENTS.md repository context records MiniMax M3 via the official MiniMax provider (`minimax/MiniMax-M3`), but that is carried-forward repository context and is not treated as direct Build-session evidence.
- Model (display string): `MiniMax-M3`
- Displayed model variant: `Not available — no distinct model variant was directly displayed in the retained EXP-0056 Build evidence.`
- Directly observed OpenCode mode: `Build`
- OpenCode version: Not available — `opencode --version` was not captured during this experiment.
- Relevant configuration: `Repository-carried context only — AGENTS.md governs repository-local operation; no OpenCode-harness or configuration modification was authorized by EXP-0056.`
- Session identifier: `Not available — the session identifier was not captured as repository-safe experiment evidence; raw/private session identifiers are not committed.`
- Mode-mismatch observation: `None — retained Build transcript surfaces nudge-build in Build mode, matching the authorization.`

### OpenCode harness subsection

EXP-0056 did NOT modify or validate the machine-readable OpenCode harness.

* **Custom agent display name:** `Not applicable — EXP-0056 did not modify or validate the machine-readable OpenCode harness; direct Build-agent evidence is recorded separately above as nudge-build.`

* **Project OpenCode configuration paths:** `Not applicable — EXP-0056 did not modify or validate any OpenCode harness configuration path.`

* **Configured default agent:** `Not applicable — EXP-0056 did not inspect or validate the configured default agent as part of this experiment.`

* **Fresh-process configuration-load result:** `Not applicable — EXP-0056 did not modify or validate the machine-readable OpenCode harness and performed no fresh-process harness-load validation.`

* **Selectable-agent inventory:** `Not applicable — EXP-0056 did not inspect or validate the selectable-agent inventory as part of this experiment.`

* **Permission-probe results:** `Not applicable — EXP-0056 did not perform OpenCode-harness permission probes as part of this experiment.`

* **Built-in-agent disablement result:** `Not applicable — EXP-0056 did not inspect or validate built-in-agent disablement as part of this experiment.`

* **Hidden-system-agent preservation result:** `Not applicable — EXP-0056 did not inspect or validate hidden-system-agent preservation as part of this experiment.`

This subsection is included for template completeness only. No fresh-process configuration-load evidence or permission probes are claimed for this experiment.

## Consumption

- Experiment-start usage snapshot: `Not available — no usage snapshot was captured at experiment start.`
- Pre-Build usage snapshot: `Not available — no directly comparable pre-Build usage snapshot was captured.`
- Post-Build usage snapshot: `Not available — no post-Build usage snapshot was captured.`
- Calculated Build-stage delta: `Not available — no comparable pre/post usage snapshots exist from which a reliable delta can be calculated.`
- Measurement source: `Not available — no usage measurement source was captured for EXP-0056.`
- Units displayed by provider: `Not available — no provider usage snapshot was captured.`
- Snapshot timestamps: `Not available — no usage snapshots were captured.`
- Missing-data explanations: `Usage evidence was not captured and is not reconstructed from request counts, elapsed time, or indirect signals.`

## Authorization and scope

The cumulative Build authorization for Phase 3 — Attempts 1, 2, 3A, 3B, the R1 bounded evidence correction, and the R2 final bounded evidence correction — permitted file modification only on the three exact authorized mutation paths: two tracked modifications and one new experiment-record path.

Authorized mutation path set:

1. `app/src/main/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStore.kt` — tracked modification
2. `app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt` — tracked modification
3. `docs/agentic-development/experiments/EXP-0056.md` — new / untracked experiment-record path

EXP-0056.md is recorded as a new / untracked candidate until it is staged/committed; it is NOT labelled as tracked in this record.

Path (1) and path (2) were already modified by Attempt 3A before Attempt 3B began. No further modification of path (1) or path (2) occurred in Attempt 3B, in the R1 bounded evidence correction, or in the R2 final bounded evidence correction.

Attempt 3B was bounded strictly to:

- C1 — current branch recovery
- C2 — current HEAD recovery (P5 recovery)
- C3 — current `git status --short --untracked-files=all` recovery (P4 recovery)
- C4 — current `git diff --cached --name-status` (corroboration of P3)
- C5 — current `git diff --name-status` (corroboration of P1)
- C6 — current `git diff --check` (corroboration of P2)
- C7 — final write of this experiment record

The R1 bounded evidence correction was bounded strictly to a single Write of `EXP-0056.md` after the four pre-write baseline checks (B1–B4). No additional file, command, or repository operation was authorized.

The R2 final bounded evidence correction authorized modification only of `docs/agentic-development/experiments/EXP-0056.md`; it authorized B1-B4 baseline reads, Read of EXP-0056 to explicit EOF, and one final Write of EXP-0056. It did not authorize production/test modification or validation reruns.

No staging, commit, push, network access, dependency or configuration change, Android identity change, Compose change, validator change, CI change, or release-document synchronization was authorized or performed.

Authorized read path set:

- The retained Attempt chronology references the directly observed authorized target/governance reads already described therein, such as the Attempt-3A EOF reads of `FileReminderStore.kt` and `FileReminderStoreTest.kt` and the Attempt-3B recovery reads of branch, HEAD, status, cached name-status, tracked diff name-status, and tracked diff check.

For earlier attempts whose exhaustive read-path list cannot be reconstructed safely:

- `Not available — the complete earlier-attempt read-path list was not retained as repository-safe evidence; known deviations are preserved explicitly in the attempt chronology.`

R2 authorized read path:

- `docs/agentic-development/experiments/EXP-0056.md`

R2 read-path purpose:

- Read current EXP-0056 to explicit EOF before the final R2 metadata/evidence Write.

No additional R2 repository-file read is claimed.

Authorized shell-command set:

- The retained Attempt chronology and validation evidence preserve the exact known B/V/P/C command families directly observed across Attempts 1, 2, 3A, and 3B (including the four pre-write baseline checks `git branch --show-current`, `git rev-parse HEAD`, `git status --short --untracked-files=all`, `git diff --cached --name-status`; the four validation groups V1–V7 gradle/python/validator invocations; the three P1–P3 static diff checks; and the six C1–C6 continuity calls).

For any incomplete earlier-attempt command order:

- `Not available — the full command-by-command order for that earlier attempt was not retained as repository-safe evidence; known executed deviations are recorded separately.`

Commands actually executed:

- The commands directly retained in the attempt chronology and validation evidence: the Attempt-3A V1–V7 invocations and the Attempt-3B C1–C6 invocations, plus the R1-correction B1–B4 baseline checks.

R2 shell baseline:

- `git branch --show-current`
- `git rev-parse HEAD`
- `git status --short --untracked-files=all`
- `git diff --cached --name-status`

R2 tool actions:

- Read `docs/agentic-development/experiments/EXP-0056.md` to explicit EOF.
- Write the complete corrected `docs/agentic-development/experiments/EXP-0056.md`.

The R2 Write was the final tool call of that correction session.

Command-form deviations:

- Attempt 1 — `git ls-files ... | grep ...` diagnostic attempt denied by harness; prolonged manual Base64 reasoning observed.
- Attempt 2 — V1 was retried after a first V1 invocation failure, contrary to the frozen no-retry boundary; XML and Git diagnostics continued after the second V1 hard stop.
- Attempt 3A — blocked before P4, P5, and the EXP Write by OpenCode maximum-step/tool-disable environment limit.

Hard-stop trigger and response:

- Attempt 2 — V1 first-invocation Kotlin unit-test compilation failures on the two newly written assertions treating Unit-returning `create()` / `remove()` as Boolean. Two test edits were then performed after V1 had begun — a discipline deviation. V1 was retried contrary to the frozen no-retry boundary.
- Attempt 3A — OpenCode maximum-step/tool-disable environment limit interrupted the run after V1–V7 and P1–P3.

Unexpected tool behavior:

- Attempt 3A — OpenCode maximum-step/tool-disable environment limit interrupted the run after V1–V7 and P1–P3.
- R1 recovery — OpenCode Read tool 50 KB / line-cap truncated the AGENTS.md read at line 323, requiring an additional authorized continuation read from offset 324 to reach explicit EOF at line 326.

Unexpected questions:

- `None — no agent question event is retained as having blocked this Build.`

## Task

- Objective: `Implement and deterministically prove NWR2 persistence with backward NWR1 loading and lazy same-file migration for v0.1.4 done state.`
- Original specification reference: `docs/releases/v0.1.4/done-state-architecture.md plus the maintainer-authorized Phase-3 Build/corrective prompts.`
- Expected files: `app/src/main/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStore.kt; app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt; docs/agentic-development/experiments/EXP-0056.md.`
- Forbidden changes: `No Reminder or ReminderController production modification; no Compose, Android identity, Gradle/dependency, validator architecture, or CI modification; no branch, commit, push, tag, release, or unrelated path modification; no staging, network access, dependency change, or configuration change.`
- Required validation: `V1 through V7 exact commands.`

## Execution

- Start timestamp: `Not available — a single experiment-wide repository-safe start timestamp was not captured across the multi-attempt EXP-0056 sequence.`
- End timestamp: `Not available — a single experiment-wide repository-safe end timestamp was not captured across the multi-attempt EXP-0056 sequence.`
- Agent mode: `Build`
- Agent question event count: `Not available — cumulative event counting was not captured during the multi-attempt Build and is not retroactively reconstructed.`
- Maintainer decision event count: `Not available — cumulative event counting was not captured during the multi-attempt Build and is not retroactively reconstructed.`
- Maintainer approval event count: `Not available — cumulative event counting was not captured during the multi-attempt Build and is not retroactively reconstructed.`
- Maintainer corrective-prompt event count: `Not available — cumulative event counting was not captured during the multi-attempt Build and is not retroactively reconstructed.`
- Counting method: `Not available — event counts were not captured contemporaneously; no retroactive count is fabricated.`
- Unexpected questions: `None — no retained blocking agent-question event.`
- Unexpected tool behavior: `OpenCode maximum-step/tool-disable limit interrupted Attempt 3A after V1-V7 and P1-P3; OpenCode Read tool size cap truncated AGENTS.md at 50 KB / line 323 during R1 recovery.`

### Detailed attempt chronology

#### Attempt 1

- Outcome: `Partially successful`
- Repository mutation: `None`
- Known deviations:
  - Denied unapproved `git ls-files ... | grep ...` diagnostic attempt.
  - Unnecessary extra domain reads.
  - Prohibited pre-validation test-count prediction.
  - Prolonged manual Base64 reasoning.
  - Implementation never started.
- Scope compliance: `deviation without approval`

#### Attempt 2

- Outcome: `Partially successful`
- Entry B1–B4: `PASS`
- Mutation result: exactly two authorized tracked paths modified (`FileReminderStore.kt` and `FileReminderStoreTest.kt`).
- First V1 invocation: Kotlin unit-test compilation failed because two newly written assertions treated Unit-returning `create()` / `remove()` as Boolean. Two test edits were then performed after V1 had begun — a discipline deviation. V1 was retried contrary to the frozen no-retry boundary.
- Second V1 reached test execution with direct XML: `tests=61 skipped=0 failures=5 errors=0`.
  - The five directly observed failures were:
    1. `P3_08` second-TAB positional assertion defect.
    2. `P3E_08` same positional assertion defect.
    3. `P3E_10` same positional assertion defect.
    4. `P3NWR2_09` incorrect expected line count.
    5. `P3NWR2_10` malformed manually authored Base64 fixture.
- After the second V1 hard stop, XML and Git diagnostics continued. That was another read-only discipline deviation.
- V2–V7: `NOT_EXECUTED`
- P1–P5: `NOT_EXECUTED`
- EXP-0056: `NOT_CREATED`
- Last directly observed Attempt-2 state: two modified tracked paths; HEAD `fe876b86977f0f34bded3c3e774ffcaec016591f`; index empty.
- Scope compliance: `deviation without approval`

#### Attempt 3A — corrective implementation and validation

Attempt 3A began from the authorized dirty two-path baseline.

Entry proofs directly observed:

- branch: `release/v0.1.4`
- HEAD: `fe876b86977f0f34bded3c3e774ffcaec016591f`
- working tree: exactly the two authorized modified tracked paths
- index: empty
- diff path set: exactly the two authorized modified tracked paths

The two exact target files were read to EOF.

Production correction directly performed:

- Removed Attempt-2 byte-equality write suppression.
- Restored minimal save flow: validate complete list → serialize complete NWR2 → `file.writeBytes(encoded)`.
- Retained dual NWR1/NWR2 loading.
- Retained NWR1 `done=false` semantics.
- Retained NWR2 `done` 0/1 semantics.
- Retained NWR2-only writes.
- Retained no load-time rewrite.
- Retained direct non-atomic same-file persistence.
- Corrected KDoc to attribute no-save NWR1 preservation to controller same-state / identical-edit paths.

Five exact test corrections directly performed:

1. `P3_08` second-TAB assertion corrected.
2. `P3E_08` second-TAB assertion corrected.
3. `P3E_10` second-TAB assertion corrected.
4. `P3NWR2_09` migrated file line count corrected from 2 to 3.
5. `P3NWR2_10` malformed fixture replaced using known-valid legacy encodings.

Pre-V1 static review directly observed:

- `git diff --check`: `NO OUTPUT`
- `git diff --name-status`: exactly the two authorized tracked paths

Attempt-3A pre-V1 self-corrections after that static review: `None`.

Attempt-3A validation (restated as historical direct evidence carried forward):

- V1 invoked exactly once:
  - `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.data.FileReminderStoreTest"`
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=61 skipped=0 failures=0 errors=0`
- V2:
  - `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.domain.ReminderTest"`
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=2 skipped=0 failures=0 errors=0`
- V3:
  - `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.domain.ReminderControllerTest"`
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=51 skipped=0 failures=0 errors=0`
  - The V3 XML directly included the landed no-save domain proofs: `D_03_setDone_same_state_returns_true_no_save`, `E_11_normalized_identical_edit_returns_true_no_save`.
- V4:
  - `python3 -B -m unittest tests.test_validator_core tests.test_validator_repository tests.test_nudge_land_cli tests.test_nudge_land_ledger tests.test_nudge_land_runtime`
  - Direct result: `Ran 244 tests in 2.021s` / `OK`.
  - Additional PASS/FAIL text emitted by validator-unit-test fixtures is test fixture output and does not change the unittest result.
- V5:
  - `./scripts/validate-local.sh --group required`
  - Direct result: `SUMMARY pass=11 fail=0 skip=0`, `release_gate=NOT_SATISFIED`.
- V6:
  - `./scripts/validate-local.sh --group docs`
  - Direct result: `SUMMARY pass=11 fail=0 skip=0`, `release_gate=NOT_SATISFIED`.
- V7:
  - `./scripts/validate-local.sh --skip-android`
  - Direct result: `SUMMARY pass=22 fail=0 skip=0`, `release_gate=NOT_SATISFIED`.

Attempt-3A pre-EXP proof completed before environment limit:

- P1 `git diff --name-status`: exactly the two authorized tracked paths — PASS.
- P2 `git diff --check`: NO OUTPUT — PASS.
- P3 `git diff --cached --name-status`: NO OUTPUT — PASS.

Attempt-3A environment block:

Before P4, P5, and this EXP-0056 Write, OpenCode reached its maximum step limit and disabled further tools. Attempt 3A therefore did NOT directly execute:

- P4 `git status --short --untracked-files=all`
- P5 `git rev-parse HEAD`
- the final Write of EXP-0056

Recorded attempt outcomes:

- `ATTEMPT_3A_OUTCOME`: `Blocked by environment`
- `ATTEMPT_3A_BLOCK_REASON`: `OPENCODE_MAXIMUM_STEP_LIMIT_BEFORE_P4_P5_AND_EXP_WRITE`

This environment block does NOT invalidate V1–V7 or P1–P3.

The Attempt-3A generated final prose contained report-only overstatements. Recorded review findings:

1. It said all P1–P5 had passed even though only P1–P3 executed.
2. It claimed the post-validation exact working-tree/untracked state even though P4 did not execute.
3. It claimed post-validation HEAD unchanged even though P5 did not execute.
4. It labelled the Build `LOCAL_PASS_WRITE_COMPLETE...` even though EXP-0056 was not written.

- `ATTEMPT_3A_REPORT_FINDING`: `FINALIZATION_STATE_OVERSTATED_NON_MUTATING`

#### Attempt 3B — finalization recovery

Attempt 3B performs ONLY current continuity proof, recovery of missing P4/P5 semantics, and final EXP-0056 serialization. Attempt 3B does not rerun V1–V7 and does not modify `FileReminderStore.kt` or `FileReminderStoreTest.kt`.

Attempt-3B baseline continuity calls (direct evidence only through C6):

- C1 `git branch --show-current`: `release/v0.1.4` — PASS.
- C2 `git rev-parse HEAD`: `fe876b86977f0f34bded3c3e774ffcaec016591f` — PASS. This is the current-session recovery of P5.
- C3 `git status --short --untracked-files=all`:
  ```
   M app/src/main/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStore.kt
   M app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt
  ```
  PASS — exactly the expected two tracked modifications, no additional non-ignored untracked path. This directly proves EXP-0056 does not yet exist. This is the current-session recovery of P4.
- C4 `git diff --cached --name-status`: NO OUTPUT — PASS. Corroboration / refresh of P3.
- C5 `git diff --name-status`:
  ```
  M	app/src/main/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStore.kt
  M	app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt
  ```
  PASS — corroboration / refresh of P1.
- C6 `git diff --check`: NO OUTPUT — PASS. Corroboration / refresh of P2.

Pre-write state after C1–C6:

- `PRE_EXP_WRITE_BRANCH`: `release/v0.1.4`
- `PRE_EXP_WRITE_HEAD`: `fe876b86977f0f34bded3c3e774ffcaec016591f`
- `PRE_EXP_WRITE_STATUS`: `EXACT_TWO_TRACKED_MODIFIED_PATHS`
- `PRE_EXP_WRITE_UNTRACKED`: `NONE`
- `PRE_EXP_WRITE_INDEX`: `EMPTY`
- `PRE_EXP_WRITE_TRACKED_DIFF`: `EXACT_TWO_PATHS`
- `PRE_EXP_WRITE_DIFF_CHECK`: `PASS_NO_OUTPUT`

This is sufficient continuity proof for this record. No validation was rerun.

#### R1 bounded evidence correction

Administrative correction bounded strictly to a single Write of `EXP-0056.md` after the four pre-write baseline checks (B1–B4).

Recorded concisely:

- correction scope: `EXP-0056 only`;
- B1-B4 baseline passed (branch `release/v0.1.4`; HEAD `fe876b86977f0f34bded3c3e774ffcaec016591f`; status exactly the expected three-path dirty candidate; index empty);
- `EXP-0056` read to EOF at the then-current 350-line serialization;
- E1-E4 correction Write succeeded as the final tool call of the R1 session;
- no production/test mutation;
- no validation rerun;
- external post-write audit required.

R1 did not modify `FileReminderStore.kt` or `FileReminderStoreTest.kt`; it did not rerun any validation command; it did not introduce a new implementation attempt. R1 was an administrative EXP-only correction.

#### R2 final bounded evidence correction

Administrative correction bounded strictly to a single Write of `EXP-0056.md` after the four pre-write baseline checks (B1–B4) and a Read of `EXP-0056.md` to explicit EOF.

Recorded concisely:

- correction scope: `EXP-0056 only`;
- B1-B4 baseline passed (branch `release/v0.1.4`; HEAD `fe876b86977f0f34bded3c3e774ffcaec016591f`; status exactly the expected three-path dirty candidate; index empty);
- `EXP-0056` read to EOF at the then-current 522-line serialization;
- eight OpenCode-harness fields added;
- nested-backtick formatting corrected;
- historical repository-result provenance clarified;
- post-correction-audit R1 history added;
- R2 provenance markers added;
- final Write succeeded as the final tool call of the R2 session;
- no production/test mutation;
- no validation rerun;
- independent R2 audit required.

R2 did not modify `FileReminderStore.kt` or `FileReminderStoreTest.kt`; it did not rerun any validation command; it did not introduce a new implementation attempt. R2 was an administrative EXP-only correction.

## Results

### Files actually changed before this record's final write

Technical tracked files:

- `app/src/main/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStore.kt`
- `app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt`

Both files were last modified during the technical Build/correction work before V1 in Attempt 3A. Both files were read to EOF after Attempt-3A corrections, and the Attempt-3A `git diff --check` returned NO OUTPUT. Neither file was modified by Attempt 3B, by the R1 bounded evidence correction, by the R2 final bounded evidence correction, or by this R3 cumulative-metadata correction.

Experiment record:

- `docs/agentic-development/experiments/EXP-0056.md`

This experiment record was:

- created/written in Attempt 3B;
- rewritten by the R1 bounded evidence correction;
- rewritten by the R2 final bounded evidence correction;
- is being rewritten by this R3 cumulative-metadata correction.

At the externally audited candidate point, the first two are tracked modifications and `EXP-0056.md` is untracked.

- Unexpected files changed: `None`
- Validation requested: `V1 through V7 exact commands`
- Validation actually executed: `V1 through V7 exact commands`

Validation results (carried forward from Attempt-3A direct evidence):

- V1 — `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.data.FileReminderStoreTest"`:
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=61 skipped=0 failures=0 errors=0`
- V2 — `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.domain.ReminderTest"`:
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=2 skipped=0 failures=0 errors=0`
- V3 — `./gradlew --offline :app:testDebugUnitTest --rerun-tasks --tests "io.github.franchoy.nudgewhen.domain.ReminderControllerTest"`:
  - Direct command result: `BUILD SUCCESSFUL`
  - Direct XML: `tests=51 skipped=0 failures=0 errors=0`; included landed no-save domain proofs `D_03_setDone_same_state_returns_true_no_save` and `E_11_normalized_identical_edit_returns_true_no_save`.
- V4 — `python3 -B -m unittest tests.test_validator_core tests.test_validator_repository tests.test_nudge_land_cli tests.test_nudge_land_ledger tests.test_nudge_land_runtime`:
  - Direct result: `Ran 244 tests in 2.021s` / `OK`.
- V5 — `./scripts/validate-local.sh --group required`:
  - Direct result: `SUMMARY pass=11 fail=0 skip=0`; `release_gate=NOT_SATISFIED`.
- V6 — `./scripts/validate-local.sh --group docs`:
  - Direct result: `SUMMARY pass=11 fail=0 skip=0`; `release_gate=NOT_SATISFIED`.
- V7 — `./scripts/validate-local.sh --skip-android`:
  - Direct result: `SUMMARY pass=22 fail=0 skip=0`; `release_gate=NOT_SATISFIED`.

For numeric command exit status where the surface did not return a numeric code: `Not available — the tool result did not surface a numeric command exit status; success/failure is recorded only from the directly surfaced command result text.`

- First-pass result: `Partially successful — Attempt 1 did not reach implementation.`
- Final result: `Successful with correction — Attempt 3A achieved the technical result and Attempt 3B completed initial evidence serialization; the R1 bounded evidence correction, the R2 final bounded evidence correction, and the R3 cumulative-metadata correction corrected EXP-0056 only; the independent R2 audit accepted the technical candidate and required the R2 metadata correction, after which this R3 cumulative-metadata correction addresses the final remaining R2 audit findings.`

### Repository result

Immediately before this EXP write (C1–C6 direct evidence):

- Exactly two modified tracked paths (`FileReminderStore.kt`, `FileReminderStoreTest.kt`).
- No non-ignored untracked path.
- Index empty (`git diff --cached --name-status` produced NO OUTPUT).
- HEAD unchanged at `fe876b86977f0f34bded3c3e774ffcaec016591f`.
- `git diff --check` clean (NO OUTPUT).

After this EXP write (C7):

- `EXPECTED_THREE_PATH_DIRTY_CANDIDATE_PENDING_EXTERNAL_POST_WRITE_PROOF`

- **Historical pre-correction repository result (independent R1 audit):** `EXACT_THREE_PATH_DIRTY_CANDIDATE_AUDITED`

For the current R3 correction boundary (the future independent post-R3 audit owns the current post-R3 repository result):

- `POST_R3_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_R3_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

For the superseded R2 correction boundary (retained as historical evidence; the independent post-R2 audit already owns that historical post-R2 repository result):

- `POST_R2_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_R2_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

For the superseded R1 correction boundary (retained as historical evidence):

- `POST_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

- Repository result: `NOT_DIRECTLY_OBSERVED_BY_DESIGN_PENDING_R3_POST_WRITE_AUDIT`
- Outcome classification: `Successful with correction`
- Scope compliance: `deviation without approval`

### Self-corrections

- Attempt-2 post-V1 test edits (treating Unit-returning `create()` / `remove()` as Boolean) were an unauthorized discipline deviation. The corrected pre-V1 Attempt-3 test edits (`P3_08`, `P3E_08`, `P3E_10`, `P3NWR2_09`, `P3NWR2_10`) are the valid pre-V1 corrections that produced the green V1 result.
- Attempt-2 XML/Git diagnostics continuing after the second V1 hard stop were an unauthorized read-only discipline deviation.

### Maintainer-audit corrections (R1 review findings)

R1 audit-process findings:

1. required AGENTS.md read omitted;
2. ordinary line-level `git diff -- <path>` unavailable in nudge-audit harness;
3. read-only denied/unavailable diagnostic attempts occurred;
4. repository was not modified.

Maintainer later accepted the R1 technical-inspection substitution.

R1 recovery specification finding:

- The recovery prompt required exactly four calls while also requiring AGENTS.md to explicit EOF.
- The first AGENTS read was capped at 50 KB / line 323.
- A fifth read of the same exact authorized AGENTS.md file, offset 324, was used to reach explicit EOF at line 326.

Classification:

- `R1_RECOVERY_SPECIFICATION`: `FOUR_CALL_BUDGET_INCOMPATIBLE_WITH_AGENTS_EXPLICIT_EOF`
- `R1_RECOVERY_EXECUTION_SCOPE`: `READ_ONLY_DEVIATION_WITHOUT_APPROVAL`
- `R1_RECOVERY_REPOSITORY_EFFECT`: `NONE`
- `R1_RECOVERY_AGENTS_EOF`: `RECOVERED_326_LINES`
- `MAINTAINER_EVIDENCE_ACCEPTANCE`: `YES`

Report-only corrections:

- The recovery did NOT complete with exactly four tool calls.
- Post-recovery HEAD/index/status were not independently reobserved after the AGENTS continuation read.
- No repository-mutating operation was executed by the recovery.

### Post-correction audit R1 findings

- `POST_CORRECTION_AUDIT_R1_RESULT`: `PASS_TECHNICAL_CANDIDATE_WITH_EXP0056_CORRECTION_STILL_REQUIRED`
- `POST_CORRECTION_AUDIT_R1_EOF`: `522_LINES`
- `POST_CORRECTION_AUDIT_R1_E1`: `PASS`
- `POST_CORRECTION_AUDIT_R1_E2`: `PASS`
- `POST_CORRECTION_AUDIT_R1_E3`: `PASS`
- `POST_CORRECTION_AUDIT_R1_E4`: `FAIL_SUBSTANTIVE_STRUCTURE_CORRECTION_STILL_REQUIRED`
- `POST_CORRECTION_AUDIT_R1_TOOL_DISCIPLINE`: `DEVIATION_WITHOUT_APPROVAL`

Recorded neutrally:

- The post-correction audit attempted an unlisted `wc -l <EXP-0056-path>` command.
- The nudge-audit harness denied that command.
- The command did not execute.
- The audit then continued with the explicitly authorized Read of EXP-0056 rather than stopping after the denied command.

Classification:

- `POST_CORRECTION_AUDIT_R1_DENIED_WC`: `NO_EXECUTION_NO_REPOSITORY_EFFECT`
- `POST_CORRECTION_AUDIT_R1_CONTINUATION_AFTER_HARD_STOP`: `READ_ONLY_DEVIATION_WITHOUT_APPROVAL`
- `MAINTAINER_EVIDENCE_ACCEPTANCE`: `YES`

### Post-correction audit R2 findings (final independent R2 audit)

Final independent R2-audit result:

- `R2_FINAL_AUDIT_RESULT`: `PASS_TECHNICAL_CANDIDATE_WITH_FINAL_EXP0056_METADATA_CORRECTION_REQUIRED`
- `R2_FINAL_AUDIT_EOF`: `574_LINES`
- `R2_FINAL_AUDIT_HARNESS_STRUCTURE`: `PASS`
- `R2_FINAL_AUDIT_E1`: `PASS`
- `R2_FINAL_AUDIT_E2`: `PASS`
- `R2_FINAL_AUDIT_E3`: `PASS`
- `R2_FINAL_AUDIT_MARKDOWN_SERIALIZATION`: `PASS`
- `R2_FINAL_AUDIT_REPOSITORY_RESULT_PROVENANCE`: `PASS`
- `R2_FINAL_AUDIT_CUMULATIVE_METADATA`: `FAIL_FINAL_BOUNDED_METADATA_CORRECTION_REQUIRED`
- `R2_FINAL_AUDIT_PRIVACY`: `PASS`

Audit-process deviation recorded neutrally:

- The final R2 audit was explicitly instructed not to run grep or diagnostic supplements.
- After the complete 574-line EXP read, it attempted three Bash grep commands.
- The harness denied all three.
- It then attempted three calls to an unavailable grep tool.
- Those calls did not execute repository commands and caused no repository mutation.
- The audit continued instead of hard-stopping after the first denied command.

Classification:

- `R2_FINAL_AUDIT_TOOL_DISCIPLINE`: `READ_ONLY_DEVIATION_WITHOUT_APPROVAL`
- `R2_FINAL_AUDIT_FORBIDDEN_GREP_ATTEMPTS`: `3_BASH_DENIED_PLUS_3_UNAVAILABLE_TOOL_ATTEMPTS`
- `R2_FINAL_AUDIT_REPOSITORY_EFFECT`: `NONE`
- `R2_FINAL_AUDIT_CONTINUATION_AFTER_HARD_STOP`: `YES`
- `MAINTAINER_EVIDENCE_ACCEPTANCE`: `YES`

Report-only finding:

- `R2_FINAL_AUDIT_REPORT_FINDING`: `CLAIMED_NO_DIAGNOSTIC_SUPPLEMENT_DESPITE_GREP_ATTEMPTS`

### Review findings (EXP-internal frozen findings)

- E1: `CONFIRMED_TRACKED_CONTENT_WORDING_DEFECT`
- E2: `DIRECT_MODEL_DISPLAY_MINIMAX_M3_PROVIDER_NOT_DIRECTLY_DISPLAYED`
- E3: `CONFIRMED_PROTOCOL_PLACEHOLDER_EXPLANATION_DEFECT`
- E4: `CONFIRMED_SUBSTANTIVE_STRUCTURE_DEFECT`
- Privacy: `PASS`

### Defects found later

- E1 through E4 evidence/record defects; no production or test defect found by the independent technical audit.

## Assessment

### OpenCode observations

- OpenCode maximum-step/tool-disable limit interrupted Attempt 3A after V1–V7 and P1–P3; this is recorded as an environment/tool observation, not as model behavior.
- OpenCode Read tool 50 KB / line-cap truncated the AGENTS.md read at line 323 during R1 recovery; the continuation read from offset 324 reached EOF at line 326.

### Model observations

- Attempt-1 reasoning drift and prolonged manual Base64 reasoning prevented implementation.
- Attempt-2 test-authoring defects (Unit-returning `create()` / `remove()` asserted as Boolean; five positional/line-count/fixture defects) caused the second V1 to fail with five failures.
- Successful bounded correction in Attempt 3 (production simplification plus five test corrections) produced green V1–V7 and was independently audited.

### Task-specification observations

- The later R1 governance-recovery prompt incorrectly assumed AGENTS.md could reach EOF in one read under a four-call budget.

### Environment observations

- OpenCode maximum-step limit in Attempt 3A.
- 50 KB AGENTS.md read cap in R1 recovery.

### Direct observations

- B1 branch: `release/v0.1.4`
- B2 HEAD: `fe876b86977f0f34bded3c3e774ffcaec016591f`
- B3 status: exactly two tracked modifications plus one `?? docs/agentic-development/experiments/EXP-0056.md`
- B4 cached name-status: NO OUTPUT

### Inferences

- The semantic description of Attempt-3A as "corrective implementation and validation" and Attempt-3B as "finalization recovery" is an inference based on chronology and command sequence, not a directly observed label.
- The semantic description of the R1 correction as "bounded evidence/structure correction" is an inference based on the prompt's bounded scope, not a directly observed label.
- The semantic description of the R2 correction as "final bounded evidence correction" is an inference based on the prompt's bounded scope, not a directly observed label.
- The semantic description of this R3 correction as "cumulative-metadata correction" is an inference based on the prompt's bounded scope, not a directly observed label.

### Corroborating evidence

The later independent candidate audit directly confirmed:

- branch: `release/v0.1.4`
- HEAD: `fe876b86977f0f34bded3c3e774ffcaec016591f`
- exact three-path state (two tracked modifications plus the untracked EXP-0056.md)
- empty index
- clean tracked diff check (`NO OUTPUT`)
- technical production contract (NWR2 writes, backward NWR1 loading, lazy same-file migration, no load-time rewrite)
- test coverage (V1 `FileReminderStoreTest` 61/0/0/0)

### Lessons supported

- Final-write self-proof must remain external.
- Large governance reads require sufficient continuation-call budget.

### Candidate rule changes

- `None — a single experiment does not by itself promote a governance rule.`

### Candidate validation changes

- `None — no validator change is authorized by this evidence correction.`

### Candidate reusable skills or commands

- `None — no reusable skill/command is authorized by this correction.`

## Status

- Maintainer review: `Pending maintainer input — the technical candidate is independently accepted, but final Phase-3 progression requires an audit rerun after this corrected EXP-0056 serialization.`
- Follow-up experiments required: `None — the evidence correction remains within EXP-0056; an independent audit rerun is still required before Phase-3 closure synchronization.`

## Final-write provenance boundary

This correction must not create a new self-proof loop. Before writing the corrected EXP, direct baseline evidence (B1–B4) was directly observed and is included above. The corrected EXP itself cannot directly prove:

- its own final serialized content
- its own EOF after correction
- post-correction status
- post-correction whitespace
- post-correction index
- post-correction HEAD

Current R3 correction provenance markers (unambiguous current self-proof boundary):

- `POST_R3_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R3_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_R3_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

Historical R2 correction provenance markers (retained as historical evidence; superseded by R3 markers above):

- `POST_R2_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_R2_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_R2_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

Historical R1 correction provenance markers (retained as historical evidence):

- `POST_CORRECTION_EXP_STATUS`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_DIFF_CHECK`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_INDEX`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `POST_CORRECTION_EXP_HEAD`: `NOT_DIRECTLY_OBSERVED_BY_DESIGN`
- `EXPECTED_POST_CORRECTION_AUDIT_BASELINE`: `EXACT_THREE_PATH_DIRTY_CANDIDATE`

The independent post-R3 audit owns proof of the corrected EXP content/EOF and post-R3-correction repository state.

## Validation checklist

- [x] PASS — V1 — `FileReminderStoreTest` — `61 / 0 / 0 / 0`
- [x] PASS — V2 — `ReminderTest` — `2 / 0 / 0 / 0`
- [x] PASS — V3 — `ReminderControllerTest` — `51 / 0 / 0 / 0`
- [x] PASS — V4 — validator regression — `Ran 244 tests / OK`
- [x] PASS — V5 — `validate-local.sh --group required` — `11 / 0 / 0`, `release_gate=NOT_SATISFIED`
- [x] PASS — V6 — `validate-local.sh --group docs` — `11 / 0 / 0`, `release_gate=NOT_SATISFIED`
- [x] PASS — V7 — `validate-local.sh --skip-android` — `22 / 0 / 0`, `release_gate=NOT_SATISFIED`
- [x] PASS — C1 — branch — `release/v0.1.4`
- [x] PASS — C2 — HEAD — `fe876b86977f0f34bded3c3e774ffcaec016591f` (P5 recovery)
- [x] PASS — C3 — status — `EXACT_TWO_TRACKED_MODIFIED_PATHS` (P4 recovery)
- [x] PASS — C4 — cached name-status — NO OUTPUT (P3 corroboration)
- [x] PASS — C5 — tracked diff name-status — `EXACT_TWO_PATHS` (P1 corroboration)
- [x] PASS — C6 — diff check — NO OUTPUT (P2 corroboration)
- [x] PASS — B1 — branch — `release/v0.1.4`
- [x] PASS — B2 — HEAD — `fe876b86977f0f34bded3c3e774ffcaec016591f`
- [x] PASS — B3 — status — `EXACT_TWO_TRACKED_MODIFIED_PLUS_ONE_UNTRACKED_EXP`
- [x] PASS — B4 — cached name-status — NO OUTPUT
- [ ] BLOCKED — Post-EXP-write (R3 boundary) repository proof — deferred to independent post-R3 external audit; not directly observed by design