# EXP-0022 — Phase 3A1 Release Contract Source

**Document status:** Phase 3A1 Build record — release contract source and documentation candidate; commit and push are manual maintainer actions

## Identification

- **Experiment ID:** EXP-0022
- **Date:** 2026-07-10
- **Release:** v0.1.1
- **Phase:** Phase 3 — Reusable Local-Validator Architecture (Phase 3A1 subphase)
- **Repository:** Not available
- **Branch:** release/v0.1.1
- **Starting commit:** 0673275d7ec05adf0eb0966256fef4914847c7e7
- **Ending commit:** Not applicable (no commit is produced by this experiment; commit and push are manual maintainer actions performed under separate explicit authorization)
- **Execution attempt or stage identifier:** Phase 3A1 Build

## Tool and model

- **Tool:** OpenCode
- **Access service / provider:** Pending maintainer input
- **Model (display string):** MiniMax-M3
- **Displayed model variant:** Not available
- **Directly observed OpenCode mode (this stage):** Build
- **OpenCode version:** Not available
- **Relevant configuration:** project OpenCode harness present; the Build was handled by the `nudge-build` selectable primary agent
- **Session identifier:** Not available
- **Mode-mismatch observation:** None (Build authorization matches the displayed Build mode)

### OpenCode harness

- **Custom agent display name:** nudge-build
- **Project OpenCode configuration paths:** Not applicable (this Build does not touch `opencode.jsonc` or `.opencode/agents/`)
- **Configured default agent:** Not available with explanation (this Build does not perform a fresh-process runtime validation; harness content is not re-read in this Build)
- **Fresh-process configuration-load result:** Not applicable (this Build does not perform a fresh-process runtime validation)
- **Selectable-agent inventory:** Not available with explanation (this Build does not perform a fresh-process runtime validation)
- **Permission-probe results:** Not applicable (this Build does not perform harness permission probes)
- **Built-in-agent disablement result:** Not available with explanation
- **Hidden-system-agent preservation result:** Not available with explanation

## Consumption

- **Experiment-start usage snapshot:** Not available with explanation (no OpenCode tool surface of usage values during this Build stage)
- **Pre-Build usage snapshot:** Not available with explanation
- **Post-Build usage snapshot:** Not available with explanation
- **Calculated Build-stage delta:** Not computable with explanation (no directly comparable pre-Build and post-Build snapshots were surfaced)
- **Measurement source:** OpenCode Go usage console (named for the harness convention; not directly surfaced in this Build)
- **Units displayed by the provider:** Not available
- **Snapshot timestamps:** Not available
- **Missing-data explanations:**
  - The Build did not surface any usage-snapshot value through the OpenCode tool; the Build records the named source and leaves the value as `Not available` rather than inferring it from indirect signals.
  - No OpenCode version string was surfaced; the Build records it as `Not available` rather than inferring it from the harness content.

## Authorization and scope

- **Authorized shell-command set:**
  - `git branch --show-current`
  - `git rev-parse HEAD`
  - `git status --short --untracked-files=all`
  - `git diff --name-status`
  - `git diff --cached --name-status`
  - `git diff --check`
  - `./scripts/validate-local.sh --group required`
  - `./scripts/validate-local.sh --group docs`
  - `./scripts/validate-local.sh --skip-android`
- **Authorized read path set:**
  - `AGENTS.md`
  - `docs/releases/v0.1.1/phase-list.md`
  - `docs/releases/v0.1.1/release-charter.md`
  - `docs/local-validation.md`
  - `scripts/validate_local.py`
  - `scripts/validate-local.sh`
  - `app/build.gradle.kts`
  - `app/src/main/AndroidManifest.xml`
  - `gradle/wrapper/gradle-wrapper.properties`
  - `README.md`
  - `CONTRIBUTING.md`
  - `SECURITY.md`
  - `docs/agentic-development/experiment-protocol.md`
  - `docs/agentic-development/evaluation-template.md`
- **Authorized mutation path set:**
  - `scripts/release_contract.json` (new file)
  - `docs/local-validation.md` (modification)
  - `docs/agentic-development/experiments/EXP-0022.md` (new file)
- **Commands actually executed:** the six baseline `git` commands listed above; the three validation commands listed above
- **Command-form deviations:** None
- **Hard-stop trigger and response:** None
- **Unexpected tool behavior:** None
- **Unexpected questions:** None

## Task

- **Objective:** Create the single release-contract source for Phase 3 and document it, without refactoring validator behavior.
- **Original specification reference:** Phase 3A1 Build specification (incoming task)
- **Expected files:**
  - `scripts/release_contract.json` (new)
  - `docs/local-validation.md` (modification)
  - `docs/agentic-development/experiments/EXP-0022.md` (new)
- **Forbidden changes:**
  - `scripts/validate_local.py`
  - `scripts/validate-local.sh`
  - `app/build.gradle.kts`
  - `app/src/main/AndroidManifest.xml`
  - `.github/workflows/ci.yml`
  - `docs/releases/v0.1.1/phase-list.md`
  - `docs/releases/v0.1.1/release-charter.md`
  - `README.md`
  - `CONTRIBUTING.md`
  - `SECURITY.md`
- **Required validation:** the six baseline `git` commands and the three validation commands listed in the authorized shell-command set

## Execution

- **Start timestamp:** Not available (no OpenCode tool surface of build timestamps)
- **End timestamp:** Not available
- **Agent mode (this stage):** Build
- **Agent question event count:** 0
- **Maintainer decision event count:** 0
- **Maintainer approval event count:** 1
- **Maintainer corrective-prompt event count:** 0
- **Counting method:** the single Build-authorization turn counts as one maintainer approval event
- **Unexpected questions:** None
- **Unexpected tool behavior:** None

## Results

- **Files actually changed:**
  - `scripts/release_contract.json` (created)
  - `docs/local-validation.md` (modified: title and document status updated; new `## Release contract` section added)
  - `docs/agentic-development/experiments/EXP-0022.md` (created)
- **Unexpected files changed:** None
- **Validation requested:** the six baseline `git` commands and the three validation commands listed in the authorized shell-command set
- **Validation actually executed:** the same nine commands
- **Validation results:** recorded inline in the `## Validation checklist` section below; exact outputs preserved verbatim from the Build's direct observation
- **First-pass result:** pass (no human correction during the Build stage; no deviation from the authorized scope)
- **Final result:** pass (release contract source and documentation candidate produced; commit and push remain manual maintainer actions)
- **Repository result:** release contract source and documentation candidate created locally; working tree contains the three expected dirty paths; HEAD unchanged; nothing staged, committed, pushed, fetched, pulled, branched, tagged, released, installed, or networked by the Build
- **Outcome classification:** Successful first pass
- **Scope compliance:** compliant
- **Self-corrections:** None
- **Maintainer-audit corrections:** None
- **Review findings:** None
- **Defects found later:** None

## Assessment

- **OpenCode observations:** Build stage authorization matched the displayed Build mode; no harness permission boundary was crossed; the Build did not perform harness edits, network access, or external-directory access.
- **Model observations:** Not available (no OpenCode tool surface of model performance values during this Build stage).
- **Task-specification observations:** Phase 3A1 is a narrow preparatory Build before wiring the Python validator to the contract. The spec was internally consistent and avoided a `Phase 3 is complete` or `the validator already reads the contract` overstatement. The earlier `Phase 3R0` planning attempt was blocked, and the `Phase 3R0-R1` retry produced a usable planning input with the baseline caveat that the existing validator behavior must be preserved in Phase 3A1 and that the validator wiring of the contract is a later subphase. Phase 3A1 intentionally avoids wiring `scripts/validate_local.py` to the contract; that wiring is a Phase 3A2 or later subphase.
- **Environment observations:** Not available (no OpenCode tool surface of environment values during this Build stage).
- **Direct observations:**
  - `git branch --show-current` returned `release/v0.1.1`.
  - `git rev-parse HEAD` returned `0673275d7ec05adf0eb0966256fef4914847c7e7`.
  - `git status --short --untracked-files=all` produced no output at the start of the Build (working tree clean).
  - `git diff --name-status` produced no output at the start of the Build (no unstaged tracked diff).
  - `git diff --cached --name-status` produced no output at the start of the Build (nothing staged).
  - `git diff --check` produced no output at the start of the Build (whitespace check passes).
  - `scripts/release_contract.json` was created with the exact required content: `schema_version: 1`, the `release` block with `version: "v0.1.1"`, `version_name: "0.1.1"`, `version_code: 2`, `active_branch: "release/v0.1.1"`, and the required title; the `release_documents` block with `charter`, `phase_list`, `local_validation` paths; the `phase_model` block with `first_phase: 0`, `last_phase: 7`, and the required `expected_statuses` for `Phase 0` through `Phase 7`; the `android` block with the namespace, applicationId, package_name, compile_sdk, min_sdk, target_sdk, current_version_code, current_version_name, target_version_code, target_version_name, launcher_activity_source, and launcher_activity_merged; the `validation` block with the `groups` array, `all_alias`, `release_gate_requires_groups`, `release_gate_requires_android_not_skipped`, `require_clean_supported`, `no_network`, and `no_dependency_installation`; and the `historical` block with the `previous_release_version`, `previous_release_docs_root`, and `previous_release_is_historical`.
  - `docs/local-validation.md` was modified to change the title from `NudgeWhen v0.1.0` to `NudgeWhen v0.1.1`, to update the document status to a release-aware v0.1.1 carried-forward baseline wording, and to add a new section named exactly `## Release contract` immediately after `## Dynamic private-working-material invariant` and before `## Primary command`. The new section documents that `scripts/release_contract.json` is the single release-contract source introduced in Phase 3A1, that Phase 3A1 introduces the contract but does not yet wire every validator check to it, and that a later Phase 3 subphase (Phase 3A2 or later) will wire `scripts/validate_local.py` to load and validate this contract. The section does not claim that Phase 3 is complete and does not claim that the validator already reads the contract. All existing command semantics, group semantics, exit-code semantics, candidate and clean-checkout behavior, release-gate behavior, Android manifest and APK validation behavior, no-network behavior, and no dependency-installation behavior are preserved without change.
  - `docs/agentic-development/experiments/EXP-0022.md` was created with this record.
  - No commit, push, fetch, pull, branch creation, branch switch, tag creation, release publication, dependency installation, or network access was performed by the Build.
  - Commit and push remain manual maintainer actions performed under separate explicit authorization.
- **Inferences:** None material (the Build does not rely on inferred state for any checklist item; the checklist items below are all directly observed).
- **Corroborating evidence:** None (no later observations are needed to corroborate unavailable values; the Build's checklist items are all directly observed).
- **Lessons supported by this experiment:**
  - A narrow preparatory Build that introduces a release-contract source and documents it can be performed before the validator is wired to the contract, with no validator-behavior change required in the Build.
  - The release-contract source is a deterministic, standard-library-readable JSON document with a top-level `schema_version` field, two-space indentation, and sections sorted logically; the current `v0.1.1` values can be written without ambiguity from the existing v0.1.1 charter, phase list, and Android metadata.
  - The carried-forward baseline wording for `docs/local-validation.md` can be updated from a v0.1.0-only framing to a v0.1.1 carried-forward baseline framing while preserving all existing command semantics, group semantics, exit-code semantics, candidate and clean-checkout behavior, release-gate behavior, Android manifest and APK validation behavior, no-network behavior, and no dependency-installation behavior.
- **Candidate rule changes:** None
- **Candidate validation changes:** None (the Phase 3A1 Build intentionally does not modify the validator; Phase 3A2 or later will wire the validator to the contract and is the appropriate place for any validator-side validation changes)
- **Candidate reusable skills or commands:** None

## Status

- **Maintainer review:** Pending maintainer input
- **Follow-up experiments required:** A Phase 3A2 or later subphase experiment to wire `scripts/validate_local.py` to load and validate `scripts/release_contract.json`, and to record the resulting validator behavior change as a separate experiment record (its own `EXP-NNNN`).

## Validation checklist

- [x] PASS — Branch matches the expected Build branch. Directly observed by `git branch --show-current` returning `release/v0.1.1` at the start of the Build.
- [x] PASS — Local HEAD matches the expected Build starting commit. Directly observed by `git rev-parse HEAD` returning `0673275d7ec05adf0eb0966256fef4914847c7e7` at the start of the Build.
- [x] PASS — Working tree is clean at the start of the Build. Directly observed by `git status --short --untracked-files=all` producing no output at the start of the Build.
- [x] PASS — No unstaged tracked diff at the start of the Build. Directly observed by `git diff --name-status` producing no output at the start of the Build.
- [x] PASS — Nothing staged at the start of the Build. Directly observed by `git diff --cached --name-status` producing no output at the start of the Build.
- [x] PASS — Whitespace check passes at the start of the Build. Directly observed by `git diff --check` producing no output at the start of the Build.
- [x] PASS — `scripts/release_contract.json` was created with the exact required content. Directly observed by the file write; the file contains `schema_version: 1`, the required `release` block (version, version_name, version_code, active_branch, title), the required `release_documents` block (charter, phase_list, local_validation paths), the required `phase_model` block (first_phase 0, last_phase 7, and the required `expected_statuses` for `Phase 0` through `Phase 7` with `Complete` for Phases 0, 1, 2 and `Planned` for Phases 3, 4, 5, 6, 7), the required `android` block (namespace, application_id, package_name, compile_sdk 36, min_sdk 26, target_sdk 36, current_version_code 1, current_version_name "0.1.0", target_version_code 2, target_version_name "0.1.1", launcher_activity_source ".MainActivity", launcher_activity_merged "io.github.franchoy.nudgewhen.MainActivity"), the required `validation` block (groups ["required","docs","android"], all_alias "all", release_gate_requires_groups ["required","docs","android"], release_gate_requires_android_not_skipped true, require_clean_supported true, no_network true, no_dependency_installation true), and the required `historical` block (previous_release_version "v0.1.0", previous_release_docs_root "docs/releases/v0.1.0", previous_release_is_historical true).
- [x] PASS — `docs/local-validation.md` was updated with the release-aware v0.1.1 carried-forward baseline wording. Directly observed by the post-mutation readback; the title is now `# Local Validation — NudgeWhen v0.1.1` and the document status is a release-aware v0.1.1 carried-forward baseline statement.
- [x] PASS — `docs/local-validation.md` includes a new section named exactly `## Release contract`. Directly observed by the post-mutation readback; the new section is placed between `## Dynamic private-working-material invariant` and `## Primary command`.
- [x] PASS — The new `## Release contract` section documents that `scripts/release_contract.json` is the single release-contract source introduced in Phase 3A1, that Phase 3A1 introduces the contract but does not yet wire every validator check to it, and that a later Phase 3 subphase (Phase 3A2 or later) will wire `scripts/validate_local.py` to load and validate this contract. Directly observed by the post-mutation readback.
- [x] PASS — The new `## Release contract` section does not claim that Phase 3 is complete. Directly observed by the post-mutation readback; the section states that the Phase 3 deliverable that refactors the validator to read this contract remains `Planned` in the active phase list.
- [x] PASS — The new `## Release contract` section does not claim that the validator already reads the contract. Directly observed by the post-mutation readback; the section states that the contract is committed as documentation and a data source and that `scripts/validate_local.py` is not refactored in this Build.
- [x] PASS — All existing command semantics, group semantics, exit-code semantics, candidate and clean-checkout behavior, release-gate behavior, Android manifest and APK validation behavior, no-network behavior, and no dependency-installation behavior are preserved without change. Directly observed by the post-mutation readback; the `## Primary command`, `## Groups`, `## Options`, `## Exit codes`, `## Release-gate semantics`, `## Candidate and clean-checkout modes`, `## SDK environment-variable requirements`, `## Manifest and APK validation (high level)`, and other preserved sections are unchanged from the pre-mutation readback.
- [x] PASS — Only the three authorized paths were modified or created. Directly observed by the post-mutation `git status --short --untracked-files=all` output (see below).
- [x] PASS — `./scripts/validate-local.sh --group required` passed with `release_gate=NOT_SATISFIED`. Directly observed by the post-mutation validation run; the literal `SUMMARY pass=7 fail=0 skip=0` line was printed together with `release_gate=NOT_SATISFIED` and an exit status of `0` (partial runs do not satisfy the release gate).
- [x] PASS — `./scripts/validate-local.sh --group docs` passed with `release_gate=NOT_SATISFIED`. Directly observed by the post-mutation validation run; the literal `SUMMARY pass=10 fail=0 skip=0` line was printed together with `release_gate=NOT_SATISFIED` and an exit status of `0` (partial runs do not satisfy the release gate).
- [x] PASS — `./scripts/validate-local.sh --skip-android` passed with `release_gate=NOT_SATISFIED`. Directly observed by the post-mutation validation run; the literal `SUMMARY pass=17 fail=0 skip=0` line was printed together with `release_gate=NOT_SATISFIED` and an exit status of `0` (`--skip-android` runs do not satisfy the release gate).
- [x] PASS — HEAD is unchanged and nothing was staged, committed, pushed, fetched, pulled, branched, tagged, released, installed, networked, private-material inspected, or externally modified. Directly observed by the post-mutation `git rev-parse HEAD` returning the same `0673275d7ec05adf0eb0966256fef4914847c7e7` and the post-mutation `git status --short --untracked-files=all` output containing only the three expected dirty paths (one modified tracked file and two untracked files) with no staged entries.
