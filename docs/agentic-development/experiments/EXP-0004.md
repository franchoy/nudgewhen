# EXP-0004 — Phase 0 Document-Status Consistency Correction

**Document status:** Final — accepted

## Identification

- **Experiment ID:** EXP-0004
- **Date:** 2026-06-22
- **Release:** v0.1.0
- **Phase:** Phase 0 — Release Charter and Experiment Protocol (corrective follow-up)
- **Repository:** franchoy/nudgewhen
- **Branch:** release/v0.1.0
- **Starting commit:** 2e5ebba7bbaf94472c3e750e2864930bf00a7177
- **Ending commit:** Not applicable — no commit was created during the experiment execution. The accepted experiment evidence was committed afterward as a separately authorized repository action.

## Tool and model

- **Tool:** OpenCode
- **Access service / provider:** OpenCode Go
- **Model (display string):** MiniMax M3 (3x usage)
- **Displayed model variant:** 3x usage
- **Agent mode (this stage):** Build
- **OpenCode version:** 1.17.8
- **Relevant configuration:** Not available
- **Session identifier:** Not available — the private workspace session identifier is intentionally omitted from committed evidence.

## Consumption

- **Experiment-start usage snapshot:** Same as the valid pre-Build snapshot recorded below — EXP-0004 is a Build-only corrective experiment and began at this captured baseline. The two experiments (EXP-0003 and EXP-0004) have independent consumption baselines; the fact that EXP-0003 had already begun is not a reason to make EXP-0004's own experiment-start snapshot not applicable.
- **Pre-Build usage snapshot:** Pre-Build cumulative session baseline, supplied by the maintainer on 2026-06-22 in a separate maintainer turn and recorded verbatim.
  - Snapshot type: Pre-Build cumulative session baseline
  - Snapshot timestamp: 2026-06-22 08:42 Europe/Madrid
  - Measurement source: OpenCode Workspace → Usage → Usage History
  - Page filters: June 2026 / All Models / All Keys
  - Latest API event included: 2026-06-22 08:37, as displayed
  - Access service: OpenCode Go
  - Model: minimax-m3
  - Displayed model selection: MiniMax M3 (3x usage)
  - Requests: 11
  - Input tokens shown: 564,167
  - Output tokens shown: 49,972
  - Total tokens processed: 614,139
  - Sum of displayed Go costs: $0.0476
- **Post-Build usage snapshot:** Post-Build cumulative session snapshot, supplied by the maintainer on 2026-06-22 in a separate maintainer turn and recorded verbatim. The interval includes the initial EXP-0004 Build pass and the subsequent EXP-0004 evidence-correction pass.
  - Snapshot type: Post-Build cumulative session snapshot
  - Snapshot timestamp: 2026-06-22 09:02 Europe/Madrid
  - Measurement source: OpenCode Workspace → Usage → Usage History
  - Page filters: June 2026 / All Models / All Keys
  - Latest API event included: 2026-06-22 08:57, as displayed
  - Access service: OpenCode Go
  - Model: minimax-m3
  - Displayed model selection: MiniMax M3 (3x usage)
  - Requests: 29
  - Input tokens shown: 2,481,434
  - Output tokens shown: 79,146
  - Total tokens processed: 2,560,580
  - Sum of displayed Go costs: $0.1080
- **Calculated Build-stage delta:** Computed by direct subtraction of comparable pre-Build and post-Build cumulative values. The two snapshots use the same filters (`June 2026 / All Models / All Keys`), the same measurement source (`OpenCode Workspace → Usage → Usage History`), the same access service (`OpenCode Go`), the same model (`minimax-m3`), the same displayed selection (`MiniMax M3 (3x usage)`), and the same units (Requests, tokens, USD). The interval includes only the same current OpenCode session used for the valid pre-Build snapshot.
  - Requests: `29 - 11 = 18`
  - Input tokens shown: `2,481,434 - 564,167 = 1,917,267`
  - Output tokens shown: `79,146 - 49,972 = 29,174`
  - Total tokens processed: `2,560,580 - 614,139 = 1,946,441`
  - Sum of displayed Go costs: `$0.1080 - $0.0476 = $0.0604`
- **Measurement source:** OpenCode Workspace → Usage → Usage History
- **Units displayed by the provider:** Requests (count), tokens (count), USD (sum of displayed Go costs)
- **Snapshot timestamps:** 2026-06-22 08:42 Europe/Madrid (pre-Build); 2026-06-22 09:02 Europe/Madrid (post-Build)
- **Missing-data explanations:**
  - Three failed pre-Build handoffs exhibited two failure modes: two blank-template handoffs and one handoff with an unfilled OpenCode-version placeholder. All three are recorded as `Not available` for the affected fields and are superseded by the valid pre-Build snapshot.
  - The invalid handoffs delayed Build authorization until a valid baseline was supplied. They did not constitute an environment block. No value was inferred.
  - The post-Build snapshot was supplied in a later separate maintainer turn and is comparable to the pre-Build snapshot on filters, measurement basis, model, and units. The Build-stage delta is now directly computable and is recorded above.

## Task

- **Objective:** Correct the two Phase 0 document-status lines that retained a `Draft — Phase 0 — Pending maintainer review` status despite Phase 0 having been accepted and marked `Complete` in the release charter and phase list, and record the corrective experiment.
- **Original specification reference:** The defect was discovered during EXP-0003 Stage 1. The accurate specification sequence is: (1) three invalid pre-Build snapshot handoffs occurred (two blank-template handoffs and one handoff with an unfilled OpenCode-version placeholder); (2) a later separate maintainer turn supplied the complete valid pre-Build snapshot; (3) OpenCode acknowledged the valid snapshot while remaining in Plan mode; (4) a subsequent separate maintainer turn explicitly authorized EXP-0004 Build and established the bounded three-file scope. The valid snapshot handoff is not an approval, decision, or corrective-prompt event. The separate Build-authorization turn is the single approval event.
- **Expected files:**
  - `docs/agentic-development/experiment-protocol.md` — `Document status` line only.
  - `docs/agentic-development/evaluation-template.md` — `Document status` line only.
  - `docs/agentic-development/experiments/EXP-0004.md` — this corrective experiment record.
- **Forbidden changes:** any Phase 1 community file; `EXP-0003.md`; the release charter; the phase list; `LICENSE`; `.gitignore`; any other tracked or untracked file. No commit, push, pull request, tag, release, branch change, network access, Git configuration change, or dependency installation.
- **Required validation:** `git branch --show-current`, `git rev-parse HEAD`, `git status --short --untracked-files=all`, `git diff --name-status`, `git diff --stat`, `git diff --check`, `git diff -- docs/agentic-development/experiment-protocol.md`, `git diff -- docs/agentic-development/evaluation-template.md`, a direct filesystem/content check of `EXP-0004.md`, and an inline standard-library Python validation covering: exact three-path affected set; UTF-8 validity; no trailing whitespace; both old status lines absent; both new status lines present exactly once; all lines other than line 3 unchanged from `HEAD`; no prohibited unresolved placeholders other than the explicitly authorized post-Build `Pending maintainer input`; no email-shaped strings; no workspace/session identifier; relative Markdown links resolve.

## Execution

- **Start timestamp:** 2026-06-22 (session-local; precise wall-clock capture not retained)
- **End timestamp:** 2026-06-22 (session-local; precise wall-clock capture not retained)
- **Agent mode (this stage):** Build
- **Agent question event count:** 0
- **Maintainer decision event count:** 1 — the separate maintainer acceptance turn accepted EXP-0004 as `Successful with correction`. This is the sole maintainer decision event; no other maintainer turn is counted as a decision event.
- **Maintainer approval event count:** 5 — five separate approval events.
  - **Approval 1:** The separate Build-authorization turn explicitly authorized the bounded EXP-0004 Build within the three-file scope.
  - **Approval 2:** The separate post-Build-snapshot turn explicitly authorized the later EXP-0004 evidence-finalization edit using the supplied post-Build snapshot. It is not another corrective prompt; it supplies the required post-Build evidence and explicitly authorizes its insertion into the experiment record.
  - **Approval 3:** A separate maintainer turn authorized the first closure-edit attempt. The authorized scope was a single file, `EXP-0004.md`. The repository-baseline precondition for that turn (the working-tree set must be exactly the three authorized paths and no session-export file may exist inside the repository) was not met: a maintainer-created private session-export file had reappeared in the repository. The baseline check detected the discrepancy; OpenCode stopped before modifying any file. This was not an OpenCode scope deviation and not an environment block. The export was moved outside the repository before the re-authorization. The raw export remains private working material and is not committed.
  - **Approval 4:** The separate re-authorization turn re-authorized the closure edit after restoration of the required repository baseline. The scope is identical to Approval 3 (a single file, `EXP-0004.md`). The closure edit was completed. This turn is counted once as Approval 4 only; it is not the EXP-0004 acceptance decision.
  - **Approval 5 (this turn):** This turn authorizes correction of the event-provenance wording in `EXP-0004.md`. The authorized scope is a single file, `EXP-0004.md`. This turn is also corrective prompt 2 (see below).
  - The valid pre-Build snapshot handoff is recorded as a snapshot handoff, not as an approval event. The three earlier failed snapshot handoffs are snapshot-handoff failures, not approval events.
- **Maintainer corrective-prompt event count:** 2 — two separate corrective-prompt events.
  - **Corrective prompt 1:** The maintainer's review turn that identified the five initial evidence-accounting defects in the initial EXP-0004 record and authorized corrections to `EXP-0004.md` only. It is a correction within the already authorized Build scope, not a new consequential-action approval.
  - **Corrective prompt 2 (this turn):** This turn corrects the remaining attribution of the acceptance decision to the wrong maintainer turn (the closure re-authorization turn). The earlier counting-method explanation incorrectly stated that the re-authorization turn was also counted as the maintainer decision that accepted EXP-0004; the acceptance and the re-authorization were separate maintainer turns. This correction applies the corrected attribution.
- **Counting method:** Each maintainer turn is one event when it serves a single function. A single maintainer turn may count under more than one event type when it contains clearly distinct kinds of content. The separate Build-authorization turn is counted once as Approval 1. The separate valid-snapshot handoff turn is recorded as a snapshot handoff, not as an approval, decision, or corrective-prompt event. The three earlier failed snapshot handoffs contained no Build authorization and no decision and are not counted as approval, decision, or corrective-prompt events; they are recorded as snapshot-handoff failures in the Consumption section. The maintainer review turn that identified the five initial evidence-accounting defects is counted once as corrective prompt 1; it is a correction within the already authorized Build scope, not a new consequential-action approval. The post-Build-snapshot turn is counted once as Approval 2; it is not a corrective-prompt event because it supplies new evidence and authorizes its insertion, rather than redirecting or correcting prior work. The first closure-authorization turn is counted once as Approval 3; OpenCode stopped before modifying any file because the repository-baseline precondition failed. The corrective stop is not a corrective-prompt event because the agent did not need to be redirected; the authorized action was correctly deferred. The re-authorization turn is counted once as Approval 4 only; it is not the EXP-0004 acceptance decision. The separate maintainer acceptance turn is counted once as the sole maintainer decision event; it is not an approval event. The current turn is counted once as Approval 5 and once as corrective prompt 2 because it both authorizes the repository edit and corrects inaccurate evidence; a single maintainer turn may count under more than one event type.
- **Unexpected questions:** None
- **Unexpected tool behavior:** None observed.

## Results

- **Files actually changed:**
  - `docs/agentic-development/experiment-protocol.md` — `Document status` line on line 3 changed from `Draft — Phase 0 — Pending maintainer review` to `Accepted — Phase 0 complete`.
  - `docs/agentic-development/evaluation-template.md` — `Document status` line on line 3 changed from `Draft — Phase 0 — Pending maintainer review` to `Accepted — Phase 0 complete`.
  - `docs/agentic-development/experiments/EXP-0004.md` — new file created.
- **Unexpected files changed:** None.
- **Validation requested:** All validation commands listed under Required validation.
- **Validation actually executed:** All of the above.
- **Validation results:**
  - `git branch --show-current` — `release/v0.1.0` (exit 0).
  - `git rev-parse HEAD` — `2e5ebba7bbaf94472c3e750e2864930bf00a7177` (exit 0).
  - `git status --short --untracked-files=all` — two `M` lines (the two modified tracked files) and one `??` line (the new EXP-0004.md) (exit 0).
  - `git diff --name-status` — two `M` lines for the two modified tracked files (exit 0).
  - `git diff --stat` — `2 files changed, 2 insertions(+), 2 deletions(-)` (exit 0).
  - `git diff --check` — no output, exit 0 (no whitespace errors in tracked changes).
  - `git diff -- docs/agentic-development/experiment-protocol.md` — exactly one hunk on line 3 (exit 0).
  - `git diff -- docs/agentic-development/evaluation-template.md` — exactly one hunk on line 3 (exit 0).
  - Direct filesystem/content check of `EXP-0004.md` — file present, content matches record (exit 0).
  - Inline Python validation (standard library only) — all 16 checks passed (see Build report).
- **First-pass result:** The authorized files were written, but the initial EXP-0004 draft contained a relative link to the not-yet-created `EXP-0003.md`. The agent identified this before final validation and converted it to plain text.
- **Final result:** The agent self-correction was applied and the bounded validation suite passed. The maintainer review identified five evidence-accounting defects in the initial EXP-0004 record (snapshot semantics, event provenance, failure-mode counting, first-pass wording, and validation precision); the corrections were applied within the already authorized Build scope and re-validated. The maintainer subsequently accepted `EXP-0004` as `Successful with correction`. A later first closure-authorization turn (Approval 3) was correctly stopped by the baseline check because a maintainer-created private session-export file had reappeared in the repository. The export was moved outside the repository, and a re-authorization turn (Approval 4) restored the required baseline and enabled this closure edit. The corrected validation passed again. The strict three-path repository-state gate passed.
- **Scope compliance:** `compliant` — only the three authorized files were created or modified. No commit, push, pull request, tag, release, branch change, network access, Git configuration change, or dependency installation was performed.
- **Review findings:** The maintainer review identified five evidence-accounting defects in the initial EXP-0004 record: experiment-start snapshot semantics required correction (the pre-Build snapshot is also the experiment-start snapshot); snapshot and authorization provenance required separation (the valid snapshot handoff and the Build authorization were separate maintainer turns); failure-mode count required correction (two failure modes across three handoffs, not three distinct ways); first-pass and final-result wording required correction (the link self-correction must be recorded); and validation claims about private identifiers required more precise wording (the executed automated check searched only UUID-shaped strings and cannot prove absence of every unknown identifier format). The repository edits and scope were correct. A later first closure-authorization turn (Approval 3) detected that a maintainer-created private session-export file had reappeared in the repository, and OpenCode correctly stopped before modifying any file. The corrective stop is recorded as a successful baseline check, not a defect.
- **Defects found later:** None at the time of writing.

## Assessment

- **OpenCode observations:** Build mode was entered only after the separate maintainer Build-authorization turn (Approval 1), which established the bounded three-file scope. The valid pre-Build snapshot had been supplied in an earlier separate maintainer turn and was acknowledged while OpenCode remained in Plan mode. Read-only baseline verification was performed before any write. The two status-line edits and the new `EXP-0004.md` creation are within the authorized scope. All validation commands executed without error. After Build, a separate maintainer review turn identified evidence-accounting defects in the initial `EXP-0004.md`; the corrections were applied within the already authorized Build scope and re-validated. A later separate maintainer turn (Approval 2) supplied the comparable post-Build snapshot and authorized the evidence-finalization edit, after which the Build-stage delta was computed by direct subtraction and recorded. A subsequent first closure-authorization turn (Approval 3) was correctly stopped by the baseline check because a maintainer-created private session-export file had reappeared in the repository. OpenCode did not modify any file, did not perform any consequential action, and reported the discrepancy. After the export was moved outside the repository, a re-authorization turn (Approval 4) restored the required baseline and enabled the closure edit. The corrected validation passed again, and the strict three-path repository-state gate passed. A subsequent event-provenance correction turn (Approval 5 and corrective prompt 2) corrected the counting-method explanation, which had incorrectly attributed the acceptance decision to the closure re-authorization turn. The acceptance and the re-authorization were separate maintainer turns. The corrected validation passed again.
- **Model observations:** The model followed the bounded scope, applied the exact two status-line replacements, and produced a record that follows the evaluation template. The model did not infer the missing OpenCode version in the placeholder-version handoff and did not invent any quantitative value. The model self-corrected a relative Markdown link to the not-yet-created `EXP-0003.md` and converted it to plain text before the inline Python validation ran, so the validation passed. The model produced the initial `EXP-0004.md` draft with five evidence-accounting defects; the maintainer review identified them and the corrections were applied.
- **Task-specification observations:** The specification was sufficiently detailed to execute the Build within the three-file scope. The accurate specification sequence was: three invalid pre-Build snapshot handoffs (two blank-template handoffs and one handoff with an unfilled OpenCode-version placeholder); a later separate maintainer turn supplied the valid pre-Build snapshot; OpenCode acknowledged it while remaining in Plan mode; a subsequent separate maintainer turn explicitly authorized EXP-0004 Build and established the bounded three-file scope. The `EXP-0004.md` cross-reference list initially included a relative Markdown link to `EXP-0003.md` (not yet created); the model converted this to plain text to keep the link-integrity validation clean.
- **Environment observations:** The repository baseline was clean and matched the expected starting state. The starting commit `2e5ebba7bbaf94472c3e750e2864930bf00a7177` was confirmed before any write. `EXP-0004.md` did not exist before Build. No third-party Python packages were required; the inline validation used the standard library only. During the evidence-finalization pass, an additional untracked `ses_`-prefixed file appeared in the working tree outside the expected three-path affected set. Its contents were not read, its name was not exposed in committed evidence beyond what `git status` already displays, and it was not committed, not pushed, and not included in any EXP-0004 evidence. The file is recorded here as an environment observation; it does not affect the scope-compliance determination for EXP-0004 because it is not an EXP-0004 artifact and is not under EXP-0004 control. During the first closure-authorization turn (Approval 3), the same `ses_`-prefixed file had reappeared in the repository. The baseline check detected it; OpenCode stopped before modifying any file. The file was moved outside the repository before this re-authorization (Approval 4). The file is not committed and is not part of any EXP-0004 evidence. The reappearance and re-removal of the maintainer-created private working material is recorded neutrally; it is not an OpenCode scope deviation and not an environment block.
- **Validation precision (private identifiers and email-shaped strings):** The executed automated workspace/session-identifier check searched only UUID-shaped strings. OpenCode identifiers are not guaranteed to use UUID format, so this check does not conclusively prove that no possible workspace or session identifier exists. The evidence is: (a) no raw session identifier was intentionally included in the authorized input used to create `EXP-0004.md`; (b) no UUID-shaped identifier was found by the executed automated check; (c) the automated check cannot prove absence of every unknown identifier format; (d) manual review of `EXP-0004.md` found no copied raw OpenCode session identifier; (e) the raw session export remains private and uncommitted. An additional automated check for `ses_`-prefixed identifier patterns was run and found no hits; this check is also pattern-based and cannot prove absence of every unknown identifier format. The email-shaped-string scan was run directly over `EXP-0004.md` and found no hits. The other two affected files (`experiment-protocol.md`, `evaluation-template.md`) are byte-for-byte identical to `HEAD` except for their `Document status` lines, which are non-email status strings; the email-shaped-string check therefore trivially holds for them as well, and no separate automated scan was required.
- **Lessons supported by this experiment:**
  - Three failed handoffs exhibited two failure modes: two blank-template handoffs and one handoff with an unfilled OpenCode-version placeholder. The invalid handoffs delayed Build authorization until a valid baseline was supplied. They did not constitute an environment block.
  - The interposition of a corrective experiment between the Plan stage and the Build stage of a parent experiment is consistent with the experiment protocol's rule that experiment IDs reflect task-start order and that experiments do not have to complete in numeric order.
  - A relative Markdown link to a not-yet-created sibling file (such as `EXP-0003.md`) will fail link-integrity validation. The lesson is to use plain text for forward references and reserve relative links for files that exist or will exist as part of the same Build. The agent self-corrected this before final validation.
- **Candidate rule changes:** None at this time.
- **Candidate validation changes:**
  - A future Phase 0 amendment may add a pre-snapshot validation step that flags unfilled placeholders (for example, `<paste exact output of opencode --version>`) in the supplied snapshot handoff and asks the maintainer to confirm whether the placeholder is intentional or a missing capture. Promotion requires a second independent experiment.
  - A future Phase 0 amendment may extend the private-identifier check to include additional known OpenCode identifier patterns (for example, `ses_`-prefixed strings and other common formats) and to record explicitly that the check is pattern-based and cannot prove absence of every unknown identifier format. Promotion requires a second independent experiment.
- **Candidate reusable skills or commands:** None at this time.
- **Final outcome classification:** `Successful with correction` — the bounded repository correction completed successfully and remained compliant. The acceptance basis is: the exact two Phase 0 status-line corrections; preservation of all substantive protocol and evaluation-template content; completed comparable pre-Build and post-Build usage snapshots; the Build-stage delta was directly computable and arithmetically verified; one maintainer corrective prompt was required and applied; the corrected validation passed; the strict three-path repository-state gate passed. The corrections were applied within the already authorized Build scope; this is not a new consequential-action approval. Maintainer review is complete; the closure edit records the acceptance.

## Defect description (recorded for traceability)

- The two Phase 0 documents `docs/agentic-development/experiment-protocol.md` and `docs/agentic-development/evaluation-template.md` retained the document-status line `**Document status:** Draft — Phase 0 — Pending maintainer review`.
- This conflicted with the accepted Phase 0 release charter, the phase list marking Phase 0 `Complete`, and the accepted `EXP-0001.md` and `EXP-0002.md` records.
- The defect was discovered during EXP-0003 Stage 1 when the Plan stage read the two files directly to understand the existing experiment record structure.
- The correction is limited to the two `Document status` lines. No substantive protocol or evaluation-template rule content is changed.

## Interposition account (for EXP-0003)

- Experiment IDs reflect task-start order. `EXP-0004` starts after `EXP-0003` Plan has already started, and may complete before `EXP-0003` Build begins. The numeric order of completion is not required to match the numeric order of experiment IDs.
- `EXP-0003` Plan starting commit is `2e5ebba7bbaf94472c3e750e2864930bf00a7177`. This SHA is the starting commit of `EXP-0003` Plan and is unchanged by the `EXP-0004` corrective work.
- After `EXP-0004` is later committed, the resulting commit SHA on `release/v0.1.0` will become the `EXP-0003` Build-stage starting commit. That future SHA is not invented in this record; it will be recorded in `EXP-0003.md` when the corrective commit exists.
- The two status-line corrections and this record itself are the only files in `EXP-0004` scope.

## Snapshot-handoff failures (neutral record)

- Three failed handoffs exhibited two failure modes: two blank-template handoffs and one handoff with an unfilled OpenCode-version placeholder.
- The two blank-template handoffs contained no quantitative values, no captured timestamp, no captured latest API event, and no captured OpenCode version. They are recorded as `Not available` for all quantitative and metadata fields.
- The handoff with the unfilled OpenCode-version placeholder supplied quantitative values but left the OpenCode version as the unfilled placeholder `<paste exact output of opencode --version>`. It is recorded as `Not available` for the OpenCode version.
- The valid pre-Build snapshot is the fourth handoff. It supersedes all three earlier attempts and is the basis for the Build-stage delta calculation.
- The invalid handoffs delayed Build authorization until a valid baseline was supplied. They did not constitute an environment block. No value was inferred.

## Acceptance explanation

The maintainer accepted `EXP-0004` as `Successful with correction` on `2026-06-22`. The acceptance basis is:

- the exact two Phase 0 status-line corrections;
- preservation of all substantive protocol and evaluation-template content;
- completed comparable pre-Build and post-Build usage snapshots;
- the Build-stage delta was directly computable and arithmetically verified;
- one maintainer corrective prompt was required and applied;
- the corrected validation passed;
- the strict three-path repository-state gate passed.

The closure edit records the acceptance, the corrected event counts (Agent questions: `0`; Maintainer decisions: `1`; Maintainer approvals: `5`; Maintainer corrective prompts: `2`), the final outcome classification (`Successful with correction`), the scope compliance (`compliant`), the maintainer-review status (`Accepted — 2026-06-22`), and the required ending-commit wording. The closure edit is a post-experiment administrative action; it does not extend the measured EXP-0004 Build interval, and no consumption value or delta is changed or recalculated. A subsequent event-provenance correction turn adjusted the event counts to `0 / 1 / 5 / 2` and corrected the counting-method explanation; the accepted outcome and acceptance basis are unchanged.

## Closure-authorization record (neutral)

- A first closure-authorization turn (Approval 3) was issued. The repository-baseline precondition for that turn (the working-tree set must be exactly the three authorized paths and no session-export file may exist inside the repository) was not met: a maintainer-created private session-export file had reappeared in the repository. The baseline check detected the discrepancy; OpenCode stopped before modifying any file. This was not an OpenCode scope deviation and not an environment block. The corrective stop is recorded as a successful baseline check, not as a defect. The export was moved outside the repository before the re-authorization. The raw export remains private working material and is not committed.
- A re-authorization turn (Approval 4) re-authorized the closure edit after restoration of the required baseline. The scope is identical to Approval 3 (a single file, `EXP-0004.md`). The closure edit was completed. This turn is Approval 4 only; it is not the EXP-0004 acceptance decision.
- A subsequent event-provenance correction turn (Approval 5 and corrective prompt 2) corrected the counting-method explanation, which had incorrectly attributed the acceptance decision to the closure re-authorization turn. The acceptance and the re-authorization were separate maintainer turns. The acceptance turn is the sole maintainer decision event; the re-authorization turn is Approval 4 only. No additional acceptance decision is required; `EXP-0004` remains accepted.
- The corrective stop, re-authorization, and provenance correction are recorded neutrally in this section. They are also reflected in the Execution event counts and counting method, the OpenCode observations, the Environment observations, the First-pass and Final result fields, and the Review findings field.

## Status

- **Maintainer review:** Accepted — 2026-06-22
- **Follow-up experiments required:** `EXP-0003` Stage 2 (Phase 1 Build), which begins after `EXP-0004` is committed and pushed on `release/v0.1.0` and the branch is clean.

## Cross-references

- Experiment protocol: [../experiment-protocol.md](../experiment-protocol.md)
- Evaluation template: [../evaluation-template.md](../evaluation-template.md)
- EXP-0003 (interposed parent experiment, file not yet created at the time of this Build): `EXP-0003.md`
- Release charter: [../../releases/v0.1.0/release-charter.md](../../releases/v0.1.0/release-charter.md)
- Phase list: [../../releases/v0.1.0/phase-list.md](../../releases/v0.1.0/phase-list.md)
