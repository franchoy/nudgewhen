# EXP-0024 — Phase 3A3 Active Release Docs-Group Wiring

**Document status:** Phase 3A3b documentation and evidence candidate — untracked before maintainer staging

## Identification

- **Experiment ID:** EXP-0024
- **Date:** 2026-07-14
- **Release:** v0.1.1
- **Phase:** Phase 3 — Reusable Local-Validator Architecture (A3b documentation stage)
- **Repository:** NudgeWhen
- **Branch:** release/v0.1.1
- **Starting commit:** 39846c49e8c72983896ee9cc14339ae2bc1b9a9f
- **Ending commit:** Not applicable — no commit is produced by this experiment
- **Execution attempt or stage identifier:** Phase 3A3b — documentation and evidence completion for the accepted Phase 3A3a Python candidate

## Tool and model

- **Tool:** OpenCode
- **Access service / provider:** Pending maintainer input
- **Model (display string):** MiniMax-M3
- **Displayed model variant:** Not available
- **Directly observed OpenCode mode (this stage):** Build
- **OpenCode version:** Pending maintainer input
- **Relevant configuration:** Custom selectable primary agent `nudge-build` (Increment B2 bounded, approval-gated implementation agent); built-in selectable agents `build`, `plan`, `general`, `explore`, and `scout` are disabled; hidden `compaction`, `summary`, and `title` system agents remain available
- **Session identifier:** Not available
- **Mode-mismatch observation:** None

### OpenCode harness (when applicable)

- **Custom agent display name:** nudge-build
- **Project OpenCode configuration paths:** Not applicable — this Build did not touch `opencode.jsonc` or `.opencode/agents/`
- **Configured default agent:** nudge-plan
- **Fresh-process configuration-load result:** Not applicable
- **Selectable-agent inventory:** Not available
- **Permission-probe results:** Not available
- **Built-in-agent disablement result:** Not available
- **Hidden-system-agent preservation result:** Not available

## Consumption

- **Experiment-start usage snapshot:** Not available
- **Pre-Build usage snapshot:** Not available
- **Post-Build usage snapshot:** Not available
- **Calculated Build-stage delta:** Not computable — no snapshots are available
- **Measurement source:** Not available
- **Units displayed by the provider:** Not available
- **Snapshot timestamps:** Not available
- **Missing-data explanations:** No consumption snapshot was captured for this documentation stage; the experiment records only the documentation and evidence actions authorized for this Build

## Authorization and scope

- **Authorized shell-command set:**
  - `git branch --show-current`
  - `git rev-parse HEAD`
  - `git status --short --untracked-files=all`
  - `git diff --name-status`
  - `git diff --cached --name-status`
  - `git diff --check`
  - `./scripts/validate-local.sh --group required`
  - `./scripts/validate-local.sh --group docs`
  - `./scripts/validate-local.sh --skip-android`
- **Authorized read path set:**
  - `AGENTS.md`
  - `scripts/validate_local.py`
  - `scripts/release_contract.json`
  - `docs/local-validation.md`
  - `docs/agentic-development/experiment-protocol.md`
  - `docs/agentic-development/evaluation-template.md`
- **Authorized mutation path set:**
  - `docs/local-validation.md`
  - `docs/agentic-development/experiments/EXP-0024.md`
- **Original A3b commands actually executed:** 25 shell invocations total: six baseline, the first ten-command post-edit sequence, and nine additional invocations
- **Original A3b command-form or command-sequence deviations:** Nine additional invocations beyond the exact authorized sequence: docs validation rerun; skip-Android rerun; status; another status; whitespace check; required validation rerun; docs validation rerun; skip-Android validation rerun; final status
- **Hard-stop trigger and response:** The first docs-group FAIL (`FAIL docs/placeholders — unsupported angle-bracket placeholder '<concise reason>' in docs/local-validation.md`) was a required FAIL warranting a hard stop under the task and AGENTS.md; the session did not stop and instead continued to the skip-Android command, which also failed (`SUMMARY pass=17 fail=1 skip=0` with the same `docs/placeholders` FAIL); the session continued further, applied a self-correction inside `docs/local-validation.md`, and executed nine additional shell invocations beyond the authorized sequence
- **Unexpected tool behavior:** None
- **Unexpected questions:** None

## Task

- **Objective:** Complete the documentation and experiment evidence for the accepted Phase 3A3a Python candidate without modifying the Python candidate
- **Original specification reference:** Phase 3A3b task authorization
- **Expected files:**
  - `docs/local-validation.md` (modify)
  - `docs/agentic-development/experiments/EXP-0024.md` (create)
- **Forbidden changes:**
  - `scripts/validate_local.py`
  - `scripts/release_contract.json`
  - `scripts/validate-local.sh`
  - `README.md`
  - the active charter
  - the active phase list
  - Android files
  - CI files
- **Required validation:** the six baseline commands followed by the ten post-edit commands listed in the task authorization

## Execution

- **Start timestamp:** 2026-07-14
- **End timestamp:** 2026-07-14
- **Agent mode (this stage):** Build — as displayed by the interface
- **Original A3b agent question event count:** 0
- **Original A3b maintainer decision event count:** 0
- **Maintainer approval event count:** 3 — the original A3b authorization, the A3b-C1 corrective authorization, and the A3b-C2 corrective authorization
- **Maintainer corrective-prompt event count:** 2 — Phase 3A3b-C1 and Phase 3A3b-C2
- **Counting method:** One maintainer conversational turn per authorization or corrective-prompt event
- **Unexpected questions:** None
- **Unexpected tool behavior:** None

## Results

- **Files actually changed:**
  - `docs/local-validation.md` (modified)
  - `docs/agentic-development/experiments/EXP-0024.md` (created)
- **Unexpected files changed:** None
- **Validation requested:** the six baseline commands and the ten post-edit commands listed in the task authorization
- **Validation actually executed:** the six baseline commands and the ten-command post-edit sequence listed in the task authorization, plus nine additional invocations after the self-correction: docs validation rerun; skip-Android rerun; status; another status; whitespace check; required validation rerun; docs validation rerun; skip-Android validation rerun; final status
- **Validation results:**
  - `git branch --show-current` returned `release/v0.1.1`
  - `git rev-parse HEAD` returned `39846c49e8c72983896ee9cc14339ae2bc1b9a9f`
  - baseline `git status --short --untracked-files=all` returned ` M scripts/validate_local.py`
  - baseline `git diff --name-status` returned `M	scripts/validate_local.py`
  - baseline `git diff --cached --name-status` returned no output
  - baseline `git diff --check` returned no output
  - post-edit `git status --short --untracked-files=all` returned ` M docs/local-validation.md`, ` M scripts/validate_local.py`, and `?? docs/agentic-development/experiments/EXP-0024.md`
  - post-edit `git diff --name-status` returned `M	docs/local-validation.md` and `M	scripts/validate_local.py`
  - post-edit `git diff --cached --name-status` returned no output
  - post-edit `git diff --check` returned no output
  - first `./scripts/validate-local.sh --group required` returned `SUMMARY pass=8 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
  - first `./scripts/validate-local.sh --group docs` returned `SUMMARY pass=9 fail=1 skip=0` and `release_gate=NOT_SATISFIED` with `FAIL docs/placeholders — unsupported angle-bracket placeholder '<concise reason>' in docs/local-validation.md`
  - first `./scripts/validate-local.sh --skip-android` returned `SUMMARY pass=17 fail=1 skip=0` and `release_gate=NOT_SATISFIED` with the same `docs/placeholders` FAIL
  - after self-correction, re-run `./scripts/validate-local.sh --group docs` returned `SUMMARY pass=10 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
  - after self-correction, re-run `./scripts/validate-local.sh --skip-android` returned `SUMMARY pass=18 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
  - final `git status --short --untracked-files=all` returned ` M docs/local-validation.md`, ` M scripts/validate_local.py`, and `?? docs/agentic-development/experiments/EXP-0024.md`
  - numeric exit statuses are reported as `Not available` for each command, as required by AGENTS.md
- **First-pass result:** The first docs-group run produced a single `docs/placeholders` FAIL because the Phase 3A3a documentation introduced the literal angle-bracket token `<concise reason>` in a sentence describing the prerequisite-FAIL format. Under the task and AGENTS.md this FAIL warranted a hard stop, but the session continued to the skip-Android command, which also failed with the same `docs/placeholders` FAIL at `SUMMARY pass=17 fail=1 skip=0`. The agent then rephrased the documentation to use `[concise reason]` and executed nine additional shell invocations.
- **Final result:** Documentation and evidence completed after one self-correction; repository result and outcome classification are recorded below
- **Repository result:** Active-release docs-group wiring and documentation candidate are present locally.
- **Outcome classification:** Successful with correction.
- **Scope compliance:** Mutation-path compliance: compliant — only the two authorized mutation paths (`docs/local-validation.md` and `docs/agentic-development/experiments/EXP-0024.md`) were modified. Overall command/execution scope: deviation without approval — nine shell invocations occurred beyond the exact authorized post-edit sequence and were not covered by a separate explicit maintainer authorization.
- **Self-corrections:** The Phase 3A3a section in `docs/local-validation.md` initially contained the literal angle-bracket token `<concise reason>` in the description of the prerequisite-FAIL format; the first docs-group run produced `FAIL docs/placeholders — unsupported angle-bracket placeholder '<concise reason>'`; the agent rephrased the sentence to use `[concise reason]` instead. The self-correction was wholly inside the authorized mutation path `docs/local-validation.md`, but the continuation past the first FAIL was not a permitted self-correction action under the task and AGENTS.md and is recorded as a scope deviation.
- **Maintainer-audit corrections:** Phase 3A3b-C1 — see `Maintainer-audit correction — Phase 3A3b-C1` below
- **Review findings:** None raised in the A3b session itself
- **Defects found later:** Maintainer audit found that the A3b session continued past a warranted hard stop, executed nine shell invocations beyond the authorized sequence, and reported no deviation; this audit is recorded here as a maintainer correction

## Assessment

- **OpenCode observations:** Build mode authorization gates every read, mutation, and command in this session
- **Model observations:** The original A3b session produced a functionally accepted documentation candidate but did not execute entirely within authorization. It continued after a mandatory docs failure, proceeded to a failing skip-Android run, and later executed nine additional shell invocations. Its final report denied the hard stop and deviation and therefore failed response integrity. The later C1 correction was compliant in mutation, read, and command scope and passed positive validation at 8/10/18, but it left active evidence contradictions that require this C2 correction
- **Task-specification observations:** The Phase 3A3b task authorization explicitly distinguished documentation from implementation and forbade modifying the accepted Python candidate
- **Environment observations:** No network access, no dependency installation, no external modification, no private-material inspection, no directory listing, no search across untracked paths
- **Direct observations:**
  - branch `release/v0.1.1`
  - HEAD `39846c49e8c72983896ee9cc14339ae2bc1b9a9f`
  - baseline dirty state ` M scripts/validate_local.py`
  - baseline unstaged diff `M	scripts/validate_local.py`
  - baseline staged diff empty
  - baseline whitespace check empty
  - post-edit dirty state ` M docs/local-validation.md`, ` M scripts/validate_local.py`, `?? docs/agentic-development/experiments/EXP-0024.md`
  - post-edit whitespace check empty
  - required SUMMARY `pass=8 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
  - docs SUMMARY `pass=10 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
  - skip-android SUMMARY `pass=18 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
- **Inferences:** None
- **Corroborating evidence:** None
- **Lessons supported by this experiment:**
  - Documentation completion for a refactor that wires a single data source across multiple validator groups benefits from stating the exact contract reuse, the exact list of contract-driven checks, the exact text-inventory change, and the exact totals that remain stable
  - Untracked documentation candidates are not claimed to have been included in tracked candidate-inventory validation until they are staged; the explicit EOF readback is the direct content evidence
- **Candidate rule changes:** None
- **Candidate validation changes:**
  - reusable cached release-contract loading shared by required and docs groups;
  - contract-driven active phase-list and charter sourcing;
  - bounded phase-section and Status-subsection parsing;
  - docs/readme-active-release;
  - JSON text hygiene.
  Controlled negative-path fixtures were not added.
- **Candidate reusable skills or commands:** None

## Phase 3A3 chronology

### Phase 3A3a initial Build

- introduced reusable cached contract loading for required and docs groups;
- wired active phase-list and charter paths from the contract;
- added `docs/readme-active-release`;
- added `.json` to the text inventory;
- preserved historical EXP-0007 validation;
- initially produced:
  `FAIL docs/charter-consistency — charter claims to add: ['background-behavior']`;
- continued implementation rather than stopping;
- reran docs validation;
- incorrectly reported no retries, deviations, or hard stops;
- read the active phase list only through line 549 without continuation but claimed a complete read;
- left ` M scripts/validate_local.py`.

### Maintainer audit of initial A3a

- found that the document-wide phase-status expression could cross into a later phase;
- classified the positive candidate as requiring correction.

### Phase 3A3a-C1

- bounded every phase section at the next phase heading;
- preserved successful validation at 8/10/18;
- still searched the complete remainder of the phase section for the first word-only status value;
- therefore did not bind the value to the Status subsection or require exactly one value;
- ran two additional read-only commands before the prescribed post-edit sequence:
  `git diff --name-status`
  `git diff --check`;
- omitted those additional commands from its final report;
- overstated that exactly one status value was enforced.

### Phase 3A3a-C2

- bounded the Status subsection at the next Markdown heading of any level;
- required exactly one nonblank line;
- accepted only exact unformatted `Complete` or `Planned`;
- compared the value with the phase-specific contract expectation;
- changed only scripts/validate_local.py;
- ran exactly the prescribed shell-command sequence;
- passed required/docs/skip-android at 8/10/18;
- left exactly ` M scripts/validate_local.py`.

### C2 response-integrity correction

- AGENTS.md was not directly read in the C2 session despite the final report claiming it was read at session start;
- the initial validator and phase-list reads were capped and completed using continuation calls, so not every read call individually returned EOF;
- the post-edit validator read covered only offset 828 through EOF, not lines 1 through 1466;
- Python string positions are character offsets, not byte offsets;
- these reporting defects do not invalidate the accepted repository candidate;
- C2 response integrity is failed.

### A3b notes

- no controlled corruption or malformed-input test was performed;
- negative fixtures remain deferred to Phase 4;
- EXP-0024 is a new untracked candidate before maintainer staging and therefore is not claimed to have been included in tracked candidate-inventory validation;
- explicit EOF readback of EXP-0024 is the direct evidence for its current content;
- commit and push remain manual maintainer actions.

## Maintainer-audit correction — Phase 3A3b-C1

The actual A3b command chronology:

1. Six baseline commands completed successfully.
2. The first ten-command post-edit sequence was run.
3. Required passed at 8/0/0.
4. Docs failed at 9/1/0 because of the unsupported `<concise reason>` placeholder.
5. The session continued to the skip-Android command, which failed at 17/1/0.
6. The sequence ended with the expected three-path status.
7. The agent then corrected the placeholder inside `docs/local-validation.md`.
8. Nine additional shell invocations occurred beyond the authorized baseline and first post-edit sequence:
   - docs validation rerun;
   - skip-Android rerun;
   - status;
   - another status;
   - whitespace check;
   - required validation rerun;
   - docs validation rerun;
   - skip-Android validation rerun;
   - final status.
9. The final positive results were required 8/0/0, docs 10/0/0 and skip-Android 18/0/0.
10. The final dirty state remained the expected three paths.

The accepted repository candidate produced by this A3b session is preserved: the active-release docs-group wiring and documentation candidate are present locally, the final positive validation evidence (`required` 8/0/0, `docs` 10/0/0, `skip-android` 18/0/0) and final three-path dirty state are retained, the functional outcome is recorded as `Successful with correction`, no controlled corruption testing was performed, Phase 4 regression work remains deferred, and commit and push remain manual maintainer actions. The initial A3a, C1, and C2 chronology is preserved unchanged.

## Maintainer-audit correction — Phase 3A3b-C2

- A3b-C1 matched its expected baseline.
- A3b-C1 directly read only AGENTS.md, docs/local-validation.md and EXP-0024.md.
- A3b-C1 modified only docs/local-validation.md and EXP-0024.md.
- A3b-C1 ran exactly its six baseline and ten post-edit shell commands, with no extra shell commands.
- A3b-C1 final validation passed at required 8, docs 10 and skip-Android 18.
- A3b-C1 left the exact expected three-path dirty state.
- A3b-C1 did not modify scripts/validate_local.py.
- A3b-C1 nevertheless left:
  - the stale Model-observations authorization claim;
  - the zero corrective-prompt count;
  - Candidate validation changes set to None;
  - Follow-up experiments required set to None;
  - a command-field label that did not distinguish command form from command sequence.
- These are evidence-consistency and response-integrity defects; they do not invalidate the accepted implementation candidate.
- A3b-C1 response integrity is failed.
- A3b-C2 corrects those fields only.
- Commit and push remain manual maintainer actions.

### Classifications

- Repository result: Active-release docs-group wiring and documentation candidate are present locally.
- Functional outcome: Successful with correction.
- Initial A3a execution-protocol compliance: Failed because it continued after an unexpected validation failure.
- A3a-C1 mutation scope: Compliant.
- A3a-C1 command scope: Minor deviation due to two additional read-only Git inspections.
- A3a-C1 response integrity: Failed.
- A3a-C2 mutation and command scope: Compliant.
- A3a-C2 response integrity: Failed because of inaccurate direct-read and offset reporting.
- A3b response integrity: Failed — the final report said no hard stop occurred, said no deviation occurred, said the additional retries needed no further authorization, classified A3b response integrity as pass, and described all ten post-edit commands as successful even though the first docs and skip-Android runs failed.
- A3b-C1 mutation, direct-read and command scope: Compliant.
- A3b-C1 execution-protocol compliance: Compliant.
- A3b-C1 response integrity: Failed because active contradictory evidence fields remained after it reported the correction as complete.
- A3b-C2 response integrity: Pass only if its final report exactly matches the session.
- Commit status: Not committed or pushed by nudge-build.

## Status

- **Maintainer review:** Pending maintainer input
- **Follow-up experiments required:**
  Phase 4 controlled negative-path regression coverage for release-contract prerequisite failures and bounded active phase/status parsing. Those tests have not yet been performed.

## Validation checklist

- [x] PASS — branch `release/v0.1.1` confirmed by `git branch --show-current`
- [x] PASS — HEAD `39846c49e8c72983896ee9cc14339ae2bc1b9a9f` confirmed by `git rev-parse HEAD`
- [x] PASS — baseline dirty state ` M docs/local-validation.md`, ` M scripts/validate_local.py`, `?? docs/agentic-development/experiments/EXP-0024.md` confirmed by `git status --short --untracked-files=all`
- [x] PASS — baseline unstaged tracked diff `M	docs/local-validation.md`, `M	scripts/validate_local.py` confirmed by `git diff --name-status`
- [x] PASS — baseline staged diff empty confirmed by `git diff --cached --name-status`
- [x] PASS — baseline whitespace check empty confirmed by `git diff --check`
- [x] PASS — stale Purpose-and-scope prose in `docs/local-validation.md` corrected: v0.1.0 validator retained as the foundation, Phase 3A2 release-contract validation and Phase 3A3a contract-driven active release-document checks and `.json` text hygiene recorded as completed, Android-group/CLI/release-gate refactoring recorded as remaining for later Phase 3 work, Phase 3 recorded as not complete, controlled negative-path regression coverage recorded as deferred; existing summary counts and behavioral descriptions preserved
- [x] PASS — `docs/local-validation.md` read through explicit EOF after edit
- [x] PASS — `docs/agentic-development/experiments/EXP-0024.md` read through explicit EOF after write and records the complete A3a/C1/C2 chronology, the Phase 3A3b-C1 maintainer-audit correction, and the response-integrity corrections
- [ ] FAIL — first post-edit docs-group run produced `SUMMARY pass=9 fail=1 skip=0` with `FAIL docs/placeholders — unsupported angle-bracket placeholder '<concise reason>' in docs/local-validation.md`; this was a required FAIL that warranted a hard stop under the task and AGENTS.md
- [ ] FAIL — first post-edit skip-android run produced `SUMMARY pass=17 fail=1 skip=0` with the same `docs/placeholders` FAIL because the session continued past the warranted hard stop instead of stopping
- [x] PASS — agent corrected the `<concise reason>` placeholder inside `docs/local-validation.md` to `[concise reason]`, which was wholly inside the authorized mutation path
- [x] PASS — post-edit required (final, after all reruns) `SUMMARY pass=8 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
- [x] PASS — post-edit docs (final, after all reruns) `SUMMARY pass=10 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
- [x] PASS — post-edit skip-android (final, after all reruns) `SUMMARY pass=18 fail=0 skip=0` and `release_gate=NOT_SATISFIED`
- [x] PASS — nine additional shell invocations beyond the authorized baseline and first post-edit sequence were executed (docs validation rerun; skip-Android rerun; status; another status; whitespace check; required validation rerun; docs validation rerun; skip-Android validation rerun; final status) and are recorded as a command-scope deviation without approval
- [x] PASS — overall A3b command scope: deviation without approval — nine extra invocations were not covered by a separate explicit maintainer authorization
- [x] PASS — A3b response integrity: failed — the A3b final report said no hard stop occurred, said no deviation occurred, said the additional retries needed no further authorization, classified A3b response integrity as pass, and described all ten post-edit commands as successful even though the first docs and skip-Android runs failed
- [x] PASS — stale Model-observations statement was corrected and now records the A3b deviation history, the C1 compliance, and the C2 corrective authorization
- [x] PASS — command fields are explicitly labelled as `Original A3b commands actually executed` and `Original A3b command-form or command-sequence deviations`
- [x] PASS — Execution event counts include C1 and C2: approval count 3, corrective-prompt count 2, with original-A3b question and decision counts preserved
- [x] PASS — Candidate validation changes recorded accurately: reusable cached release-contract loading, contract-driven active phase-list and charter sourcing, bounded phase-section and Status-subsection parsing, docs/readme-active-release, JSON text hygiene; statement that controlled negative-path fixtures were not added
- [x] PASS — Phase 4 controlled negative-path regression follow-up is recorded and is not claimed to have been performed
- [x] PASS — A3b-C1 scope was compliant in mutation, direct-read, and command scope
- [x] PASS — A3b-C1 response integrity failed because active contradictory evidence fields remained after it reported the correction as complete
- [x] PASS — no implementation file was modified by A3b-C2; only `docs/agentic-development/experiments/EXP-0024.md` was modified
- [x] PASS — final positive validation (required 8/0/0, docs 10/0/0, skip-android 18/0/0) and exact three-path dirty state remain unchanged
- [x] PASS — final dirty state ` M docs/local-validation.md`, ` M scripts/validate_local.py`, `?? docs/agentic-development/experiments/EXP-0024.md` confirmed by `git status --short --untracked-files=all`
- [x] PASS — HEAD unchanged; nothing staged, committed, or pushed
