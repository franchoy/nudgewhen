# EXP-0003 — Phase 1 Open-Source Community Baseline

**Document status:** Final — accepted

## Identification

- **Experiment ID:** EXP-0003
- **Date:** 2026-06-22
- **Release:** v0.1.0
- **Phase:** Phase 1 — Open-Source Community Baseline
- **Repository:** franchoy/nudgewhen
- **Branch:** release/v0.1.0

### Commits serving four recorded roles

Three distinct commits serve four distinct roles in the EXP-0003 evidence:

| Role | SHA | Description |
| --- | --- | --- |
| EXP-0003 Plan starting commit | `2e5ebba7bbaf94472c3e750e2864930bf00a7177` | The pre-Phase 1 baseline. The EXP-0003 Plan stage began from this commit. |
| EXP-0004 corrective commit | `5ed214c4f07b60c1c6ea5850d63cabbbcef64900` | The corrective commit that fixed the Phase 0 document-status inconsistency. |
| Maintainer-created `.gitignore` commit (interposed) | `bb5655c8bc3cfe2c20fd6750690ca72c49c455a8` | A maintainer repository action that added the `session-ses_*.md` pattern to `.gitignore`. Outside the EXP-0003 Phase 1 agent Build scope. |
| EXP-0003 Stage 2 Build starting commit | `bb5655c8bc3cfe2c20fd6750690ca72c49c455a8` | The current clean `release/v0.1.0` HEAD at the time of EXP-0003 Build authorization. Same SHA as the interposed `.gitignore` commit; the agent Build begins from this commit. |

The `.gitignore` commit is the Build starting commit because the agent Build begins from the current clean HEAD. The two roles are recorded as distinct because they originate from different actions and different maintainer turns.

- **Plan starting commit:** `2e5ebba7bbaf94472c3e750e2864930bf00a7177`
- **Build starting commit:** `bb5655c8bc3cfe2c20fd6750690ca72c49c455a8`
- **Ending commit:** Not applicable — no commit was created during this Build. Any later commit requires separate maintainer authorization.

## Tool and model

- **Tool:** OpenCode
- **Access service / provider:** OpenCode Go
- **Model (display string):** MiniMax M3 (3x usage)
- **Displayed model variant:** 3x usage
- **Plan mode version:** `1.17.9` — measured during the EXP-0003 Plan stage
- **Build mode version:** `1.17.9` — measured during the EXP-0003 Build stage
- **OpenCode version associated with the historical experiment-start snapshot:** `1.17.8`
- **Relevant configuration:** Not available
- **Session identifier:** Not available — the private workspace session identifier is intentionally omitted from committed evidence. No raw session identifier or personal email is included.

## Consumption

### Historical experiment-start / pre-Plan snapshot (preserved verbatim)

The historical cumulative baseline dated `2026-06-20 15:16 Europe/Madrid` is preserved as the EXP-0003 experiment-start evidence. It is associated with OpenCode version `1.17.8` and was captured before the EXP-0003 Plan stage began.

- Snapshot type: `Experiment-start / Pre-Plan cumulative session baseline`
- Snapshot timestamp: `2026-06-20 15:16 Europe/Madrid`
- Latest API event included: `2026-06-20 15:11, as displayed`
- Measurement source: `OpenCode Workspace → Usage → Usage History`
- Page filters: `June 2026 / All Models / All Keys`
- Access service: `OpenCode Go`
- Model: `minimax-m3`
- Displayed selection: `MiniMax M3 (3x usage)`
- Requests: `53`
- Input tokens shown: `5,690,131`
- Output tokens shown: `145,746`
- Total tokens processed: `5,835,877`
- Sum of displayed Go costs: `$0.2084`
- OpenCode version associated with this snapshot: `1.17.8`

This historical snapshot does not bracket the complete June 22 Plan and Build lifecycle. It was captured two days before the EXP-0003 Plan run. It cannot support a complete EXP-0003 Plan-plus-Build delta. It is preserved as the experiment-start evidence for traceability and for the cumulative-snapshot follow-up when a later supplied snapshot explicitly brackets the Plan and Build lifecycle.

### Stage 2 pre-Build snapshot (preserved verbatim)

The Stage 2 pre-Build cumulative session baseline dated `2026-06-22 10:18 Europe/Madrid` is preserved as the EXP-0003 Stage 2 pre-Build evidence. It is intended for comparison with a later comparable post-Build snapshot to calculate the Phase 1 Build-stage delta.

- Snapshot type: `Stage 2 pre-Build cumulative session baseline`
- Snapshot timestamp: `2026-06-22 10:18 Europe/Madrid`
- Latest API event included: `2026-06-22 10:16, as displayed`
- Measurement source: `OpenCode Workspace → Usage → Usage History`
- Page filters: `June 2026 / All Models / All Keys`
- Access service: `OpenCode Go`
- Model: `minimax-m3`
- Displayed selection: `MiniMax M3 (3x usage)`
- Requests: `70`
- Input tokens shown: `10,215,443`
- Output tokens shown: `139,206`
- Total tokens processed: `10,354,649`
- Sum of displayed Go costs: `$0.3689`
- OpenCode version: `1.17.9`

Arithmetic: `10,215,443 + 139,206 = 10,354,649`.

These totals were calculated from exactly the 70 displayed rows belonging to the current OpenCode session. Rows belonging to the three earlier displayed sessions were excluded. The private session identifier is not reproduced in committed evidence. No value was inferred from pricing, elapsed time, or any other session.

### Post-Build snapshot (supplied)

The post-Build cumulative session snapshot was supplied by the maintainer on 2026-06-22 and is recorded verbatim.

- **Snapshot type:** `Stage 2 post-Build cumulative session snapshot`
- **Snapshot timestamp:** `2026-06-22 10:47 Europe/Madrid`
- **Latest API event included:** `2026-06-22 10:43, as displayed`
- **Measurement source:** `OpenCode Workspace → Usage → Usage History`
- **Page filters:** `June 2026 / All Models / All Keys`
- **Access service:** `OpenCode Go`
- **Model:** `minimax-m3`
- **Displayed selection:** `MiniMax M3 (3x usage)`
- **Requests:** `103`
- **Input tokens shown:** `18,807,392`
- **Output tokens shown:** `179,280`
- **Total tokens processed:** `18,986,672`
- **Sum of displayed Go costs:** `$0.5985`
- **OpenCode version:** `1.17.9`

Arithmetic: `18,807,392 + 179,280 = 18,986,672`.

These totals were calculated from exactly the 103 displayed rows belonging to the same current OpenCode session. Rows belonging to the three earlier displayed sessions were excluded. The private session identifier is not reproduced in committed evidence. No additional OpenCode request occurred between the final corrective-pass report and the capture of this snapshot. No value was inferred from pricing, elapsed time, or any other session.

### Build-stage delta (computed)

The Build-stage delta is computed by direct subtraction of comparable pre-Build and post-Build cumulative values. The two snapshots use the same filters (`June 2026 / All Models / All Keys`), the same measurement source (`OpenCode Workspace → Usage → Usage History`), the same access service (`OpenCode Go`), the same model (`minimax-m3`), the same displayed selection (`MiniMax M3 (3x usage)`), and the same units. The interval includes only the same current OpenCode session used for the valid pre-Build snapshot.

- **Requests:** `103 - 70 = 33`
- **Input tokens:** `18,807,392 - 10,215,443 = 8,591,949`
- **Output tokens:** `179,280 - 139,206 = 40,074`
- **Total tokens processed:** `18,986,672 - 10,354,649 = 8,632,023`
- **Displayed Go costs:** `$0.5985 - $0.3689 = $0.2296`

Cross-check: `8,591,949 + 40,074 = 8,632,023`.

This is the measured **EXP-0003 Stage 2 Build consumption**. It covers the initial Build, the ignored-file baseline interruption and continuation, validation, self-correction, the corrective pass, and the final reporting through the 2026-06-22 10:43 event. It is not described as a clean one-pass implementation cost.

### Complete experiment delta (preserved)

- **Complete EXP-0003 Plan-plus-Build cumulative delta:** `Not computable from the available non-bracketing historical evidence. The historical experiment-start snapshot dated 2026-06-20 15:16 Europe/Madrid does not bracket the June 22 Plan and Build lifecycle.`

The historical experiment-start snapshot does not bracket the June 22 Plan and Build lifecycle. A complete Plan-plus-Build delta is not inferred.

## Task

- **Objective:** Create the Phase 1 open-source community baseline (nine affected paths, of which one is a tracked modification and eight are untracked creations) and record the EXP-0003 Plan and Build stages.
- **Original specification reference:** Maintainer instructions in this conversation; accepted corrected Phase 1 Plan; accepted Stage 2 snapshot handoff and Phase 1 Build authorization.
- **Expected files (Build scope):**
  - `README.md` — modify, status line and one-paragraph concept only.
  - `CONTRIBUTING.md` — create.
  - `CODE_OF_CONDUCT.md` — create.
  - `SECURITY.md` — create.
  - `.github/ISSUE_TEMPLATE/bug_report.yml` — create.
  - `.github/ISSUE_TEMPLATE/feature_request.yml` — create.
  - `.github/ISSUE_TEMPLATE/config.yml` — create.
  - `.github/PULL_REQUEST_TEMPLATE.md` — create.
  - `docs/agentic-development/experiments/EXP-0003.md` — create (this file).
- **Forbidden changes:** any other tracked or untracked file. `LICENSE`, `.gitignore`, the Phase 0 documents, `release-charter.md`, `phase-list.md`, `EXP-0001.md`, `EXP-0002.md`, `EXP-0004.md`, Android source, Gradle, manifests, resources, tests, `AGENTS.md`, `opencode.json`, OpenCode skills and commands, Hermes configuration, GitHub Actions, Dependabot, CodeQL, Codacy, release automation, funding and sponsorship, discussions, project boards. No commit, push, pull request, tag, release, branch change, network access, Git configuration change, Git identity inspection, or dependency installation.
- **Required validation:** bounded local validation as defined in the Stage 2 Build authorization.

## Stage 1 — Plan (accepted)

The accepted Plan stage outcome is `Successful with correction`. The Plan was accepted by the maintainer.

### Stage 1 event counts

- **Agent question event count:** 0
- **Maintainer decision event count:** 2
- **Maintainer approval event count:** 0
- **Maintainer corrective-prompt event count:** 3
- **Counting method:** A single maintainer turn may count under more than one event type when it contains clearly distinct kinds of content. The two decision events correspond to the maintainer's plan-acceptance decisions across the multiple correction cycles. The three corrective-prompt events correspond to three separate maintainer turns that directed corrections to the Plan. The Plan stage included no consequential-action approvals; consequential actions required separate, later, explicit maintainer turns.

### Stage 1 event list (neutral)

- The Plan stage began with a read-only verification of the repository baseline.
- The Plan stage read `experiment-protocol.md` and `evaluation-template.md` directly and discovered the Phase 0 document-status inconsistency. Both files still contained the document-status line `Draft — Phase 0 — Pending maintainer review` despite Phase 0 being marked `Complete` in the release charter and phase list.
- The Plan stage inspected Git configuration in the earlier turn. That inspection exposed a private email value in raw session output. The value is intentionally omitted from all committed evidence. The raw session export is classified as private working material and was not committed.
- During an earlier Plan stage, an unauthorized `git ls-remote` command was executed despite the Plan-mode network-access prohibition. The command result is not used as necessary evidence because equivalent local remote-tracking information was already available. This was a scope deviation without prior approval. No tracked file was modified.
- The agent produced an initial Plan, then a corrected Plan after the maintainer supplied a list of corrections, then a final corrected Plan after the maintainer supplied a second list of corrections.
- The maintainer accepted the final corrected Plan. The acceptance is a maintainer decision, not a consequential-action approval.
- No file was modified during the Plan stage.

### Stage 1 assessment (neutral)

- **OpenCode observations:** Plan mode was nominally respected: no file edits, no commits, no destructive commands, no branch operations. Plan mode was not fully respected: an unauthorized `git ls-remote` was executed, and a private email value was exposed in raw session output. The corrected Plan addresses the maintainer's corrections and removes the previous Plan's unsupported claims.
- **Model observations:** The model produced a first-pass Plan that required twelve maintainer corrections, indicating material gaps in the first-pass output. The corrected Plan reduced unsupported claims and aligned the file count, security route, and Code of Conduct options with the maintainer's corrections. A second independent experiment is required before any lesson is promoted to a project rule.
- **Task-specification observations:** The corrected Plan covered the three residual corrections and aligned the lifecycle, ending-commit semantics, and Code of Conduct deferral wording.
- **Environment observations:** The repository state was clean and matched the expected baseline at the start of the Plan stage. The Plan stage spanned multiple maintainer turns, including the interposition of the EXP-0004 corrective experiment and the maintainer-created `.gitignore` commit.

### Interposition account

- The `EXP-0004` corrective experiment was interposed between the EXP-0003 Plan stage and the EXP-0003 Build stage. `EXP-0004` modified only the two Phase 0 document-status lines and created `EXP-0004.md`. The corrective commit is `5ed214c4f07b60c1c6ea5850d63cabbbcef64900`. `EXP-0004` was committed and pushed on `release/v0.1.0`.
- The maintainer later added and accepted the `.gitignore` commit `bb5655c8bc3cfe2c20fd6750690ca72c49c455a8`. This commit is a maintainer repository action outside the EXP-0003 Phase 1 agent Build scope.
- Reporting corrections to the EXP-0004 repository-action report were reporting-only and did not require amendment or force-push of the accepted commit. The corrections are preserved in this record.

### Stage 1 scope compliance

`deviation without approval` — the Plan stage executed an unauthorized `git ls-remote` despite the Plan-mode network-access prohibition, and a Git configuration inspection exposed a private email value in raw session output. Neither was authorized in advance. Neither modified a tracked file. The second correction turn is recorded as a corrective-prompt event but is not a new scope deviation; it is a refinement of the corrected Plan.

### Stage 1 outcome classification

`Successful with correction` — accepted by the maintainer after three corrective-prompt events across the Plan stage.

## Stage 2 — Build

### Stage 2 authorization

The Stage 2 Build is authorized across three separate, explicit maintainer turns. The authorized scope is the nine paths and local validation defined in the Stage 2 Build authorizations. The authorizations are not new Phase 1 policy decisions.

### Stage 2 final event counts

- **Agent question event count:** 0
- **Maintainer decision event count:** 2
- **Maintainer approval event count:** 5
- **Maintainer corrective-prompt event count:** 2

### Stage 2 events and roles

- **Maintainer decision 1:** The maintainer decided that a local `session-ses_*.md` export is acceptable when it is demonstrably matched by the committed `.gitignore` rule, untracked, and unstaged. The export is private working material and is not part of the nine-path repository change set. This decision authorized the Build to proceed under the amended ignored-file baseline.
- **Approval 1:** The initial Phase 1 Build authorization under the strict (pre-amendment) baseline. The Build stopped before modifying any file because the strict baseline check found the local `session-ses_*.md` export.
- **Approval 2:** The continuation authorization under the amended ignored-file baseline. The Build proceeded and created the nine affected paths.
- **Corrective prompt 1:** The maintainer's amendment of the too-strict session-export baseline after the Build stopped. The amendment added the explicit ignored-file tolerance and caused the Build to resume.
- **Approval 3:** The corrective-pass authorization for the previous turn, which modified the five files listed in the corrective authorization and updated `EXP-0003.md` accordingly.
- **Corrective prompt 2:** The previous corrective pass, correcting the community files (`README.md`, `SECURITY.md`, the two issue forms) and the `EXP-0003.md` evidence to match the expanded initial Build, the ignored-file acceptance, and the initial-validation link defect.
- **Approval 4:** Post-Build evidence insertion authorization: the maintainer supplied the comparable post-Build usage history and authorized insertion of the snapshot and calculated Build-stage delta.
- **Maintainer decision 2:** Final EXP-0003 maintainer acceptance as `Successful with correction`.
- **Approval 5:** Final maintainer-review closure edit authorization. This turn modifies `EXP-0003.md` to record the acceptance, update the event counts, and add the acceptance basis. No commit, push, pull request, tag, or release is performed in this turn.

### Stage 2 counting method

Each maintainer turn is one event when it serves a single function. A single maintainer turn may count under more than one event type when it contains clearly distinct kinds of content. The three approvals are each counted once. The two corrective prompts are each counted once. The one decision is counted once. The Stage 2 Build did not contain any agent question events.

### Stage 2 factual evidence (corrected)

- The local `session-ses_*.md` export is present in the working tree, is matched by `.gitignore` line 37 (`session-ses_*.md`), is untracked, and is unstaged. The export is private working material and is not part of the nine-path repository change set. The export is acceptable in the working tree only because it is ignored, untracked, and unstaged.
- The acceptance of the ignored export is a maintainer-authorized baseline amendment (Approval 2), not an independent "verified acceptable" determination by the Build. The Build verified the match against `.gitignore` and then proceeded under the amended authorization.
- `README.md` was expanded from its original two-line stub into the complete Phase 1 landing page. The expansion is not limited to a status line and one paragraph; it covers concept, status, what exists and does not exist, long-term design intentions, agentic-development experiment purpose, release-train status, contribution, conduct, security, support, and license pointers.
- The initial Stage 2 Build (under the strict baseline) detected a baseline discrepancy and stopped before modifying any file. The Build did not modify any file until the maintainer authorized the amended baseline.
- The initial full validation (run after the eight files were created and `README.md` was modified) detected one broken-relative-link defect: the pull-request template used `../../` relative paths from `.github/`, which resolve above the repository root. The Build (OpenCode) corrected the four links from `../../` to `../` (one level up to the repository root). The focused link validation was rerun and passed. The subsequent final validation passed.
- The original full validation was not entirely green. The first full validation run reported one FAIL on the pull-request-template link-integrity check. The defect was corrected, the focused link validation passed, and the final validation passed. The original validation run is not claimed to be entirely green.
- A precise total number of validation checks is not claimed. The validation suite covered the categories required by the Stage 2 Build authorization: exact affected set, UTF-8, trailing whitespace, prohibited placeholders, no email-shaped strings, no UUID-shaped identifiers, no `ses_`-prefixed identifiers, no local paths, relative Markdown links resolve, README-specific checks, YAML schema and form structure, `config.yml` checks, EXP-0003 content checks, and cross-document consistency. The exact count of individual checks is not reproduced here because it is not reproducibly calculated.
- The final affected state is exactly one modified path (`README.md`) and eight created paths (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/PULL_REQUEST_TEMPLATE.md`, `docs/agentic-development/experiments/EXP-0003.md`).
- Maintainer redirection occurred twice during Stage 2: (a) the ignored-file baseline correction after the initial Build stop, and (b) this corrective evidence and content pass.
- The two issue forms were updated in the corrective pass: the top-level `labels: []` lines were removed, and the acknowledgement checkbox elements received field-level `validations: required: true`.
- `SECURITY.md` was updated in the corrective pass: the primary reporter workflow is now unambiguously the **Report a vulnerability** button in the repository's **Security** area, not the maintainer's draft-advisory workflow. The unsupported claim that private reporting necessarily keeps details out of the commit history until publication was removed; the corrected wording states only that it submits the vulnerability privately to the repository maintainers.
- `README.md` support guidance was updated in the corrective pass: the generic instruction to open a GitHub Issue was replaced with specific routing through the configured GitHub issue forms and the security and conduct routes.

### Stage 2 files actually changed (corrected)

- `README.md` — modified. Expanded from the original two-line stub to the complete Phase 1 landing page. The corrective pass replaced the generic support instruction with specific form routing.
- `CONTRIBUTING.md` — created. Unchanged in the corrective pass.
- `CODE_OF_CONDUCT.md` — created. Unchanged in the corrective pass.
- `SECURITY.md` — created. The corrective pass corrected the primary reporter workflow wording and removed the unsupported commit-history claim.
- `.github/ISSUE_TEMPLATE/bug_report.yml` — created. The corrective pass removed the top-level `labels: []` line and added field-level `validations: required: true` to the acknowledgement checkbox element.
- `.github/ISSUE_TEMPLATE/feature_request.yml` — created. The corrective pass removed the top-level `labels: []` line and added field-level `validations: required: true` to the acknowledgement checkbox element.
- `.github/ISSUE_TEMPLATE/config.yml` — created. Unchanged in the corrective pass. `blank_issues_enabled: false` and the single security contact link remain.
- `.github/PULL_REQUEST_TEMPLATE.md` — created. The Build corrected the four relative paths in the "Related documents" section from `../../` to `../`. The corrective pass did not change the template further.
- `docs/agentic-development/experiments/EXP-0003.md` — created. The corrective pass updated the Stage 2 event counts, added the two corrective prompts and the extra approval, corrected the factual evidence, and removed inaccurate claims.

### Stage 2 validation results

- `git branch --show-current` — `release/v0.1.0` (exit 0).
- `git rev-parse HEAD` — `bb5655c8bc3cfe2c20fd6750690ca72c49c455a8` (exit 0). Unchanged.
- `git status --short --untracked-files=all` — one `M` line for `README.md` plus eight `??` lines for the eight created files. No other path appears. The `session-ses_1120.md` file is excluded by `.gitignore` and is not shown.
- `git diff --name-status` — only `M README.md` (exit 0).
- `git diff --stat` — `1 file changed, 79 insertions(+), 2 deletions(-)` (exit 0).
- `git diff --check` — no output, exit 0.
- `opencode --version` — `1.17.9` (exit 0).
- Inline Python validation (standard library plus PyYAML) — the categories covered include exact affected set, UTF-8, trailing whitespace, prohibited placeholders, no email-shaped strings, no UUID-shaped identifiers, no `ses_`-prefixed identifiers, no local paths, relative Markdown links resolve, README-specific checks, YAML schema and form structure, `config.yml` checks, EXP-0003 content checks, and cross-document consistency. The initial full validation reported one FAIL on the pull-request-template link-integrity check; after the Build's self-correction (four paths from `../../` to `../`), the focused link validation passed and the final validation passed.
- PyYAML was available for full parse validation of all three issue-form YAML files. No dependency was installed.

### Stage 2 scope compliance

`compliant` — only the nine authorized files were created or modified. No commit, push, pull request, tag, release, branch change, network access, Git configuration change, Git identity inspection, or dependency installation was performed. `LICENSE`, `.gitignore`, Phase 0 documents, the release charter, the phase list, prior experiment records, Android code, Gradle, manifests, resources, tests, `AGENTS.md`, `opencode.json`, OpenCode skills and commands, Hermes configuration, GitHub Actions, Dependabot, CodeQL, Codacy, release automation, funding and sponsorship, discussions, and project boards were not touched. Compliance holds under the amended authorization (Approval 2) and the corrective pass (Approval 3).

### Stage 2 preliminary Build result

`Completed with correction — reviewed and accepted.`

The Stage 2 Build completed within the authorized scope. The initial Build stopped once for the ignored-file baseline discrepancy and resumed under the amended authorization. The Build detected one self-correction (pull-request template paths). The corrective pass corrected the issue forms, `SECURITY.md`, `README.md` support guidance, and this evidence. The post-Build evidence-insertion authorization supplied the comparable post-Build snapshot and the calculated Build-stage delta. The final affected state is exactly one modified path and eight created paths. Maintainer review was completed and the experiment was accepted as `Successful with correction`.

### Stage 2 self-correction

During the initial full validation, the link-integrity check failed for `.github/PULL_REQUEST_TEMPLATE.md` because the "Related documents" section used `../../` relative paths, which resolve above the repository root from `.github/`. The Build (OpenCode) corrected the four paths to `../` (one level up to the repository root). The focused link validation was rerun and passed. The subsequent final validation passed. This self-correction is recorded neutrally; it was not a maintainer redirect and is distinct from the two maintainer corrective-prompt events (ignored-file baseline and this corrective pass).

## Final outcome and review

- **Final EXP-0003 outcome classification:** `Successful with correction`
- **Maintainer review:** `Accepted — 2026-06-22`
- **Follow-up experiments required:** `None`

### Acceptance basis

EXP-0003 is accepted because the Phase 1 community baseline was completed within the amended authorized scope: one modified tracked file and eight created files; post-Build usage evidence was recorded; the Build-stage delta was directly computed; the ignored local session export remained private, ignored, untracked and unstaged; validation passed after correction; and no prohibited action was performed.

The experiment is `Successful with correction` because:

- the Plan stage required correction and had a scope deviation without approval;
- the Build stage stopped once on the original ignored-file baseline;
- the baseline was amended by the maintainer;
- the pull-request template link defect was self-corrected;
- a corrective content and evidence pass was required;
- a post-Build evidence insertion pass was required.

### Ending-commit wording

`Not applicable — no commit was created during this Build. Any later commit requires separate maintainer authorization.`

No commit SHA is invented or inserted. A later corrective commit, when separately authorized, will be recorded in the repository-action report and in the release-gate evidence where required.

## Status

- **Maintainer review:** Accepted — 2026-06-22
- **Follow-up experiments required:** None

## Cross-references

- Experiment protocol: [../experiment-protocol.md](../experiment-protocol.md)
- Evaluation template: [../evaluation-template.md](../evaluation-template.md)
- EXP-0001: [./EXP-0001.md](./EXP-0001.md)
- EXP-0002: [./EXP-0002.md](./EXP-0002.md)
- EXP-0004 (corrective interposition): [./EXP-0004.md](./EXP-0004.md)
- Release charter: [../../releases/v0.1.0/release-charter.md](../../releases/v0.1.0/release-charter.md)
- Phase list: [../../releases/v0.1.0/phase-list.md](../../releases/v0.1.0/phase-list.md)
