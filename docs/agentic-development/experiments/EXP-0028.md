# EXP-0028 — Phase 4 Validator Regression and Repository-Consistency Evidence Synchronization

**Document status:** Evidence synchronization draft — accepted v0.1.1 Phase 4 implementation result recorded; Phase 4 remains Planned; maintainer review, implementation/evidence repository action, clean committed-state proof, exact-head CI, and closure synchronization remain outstanding

## Identification

```text
Experiment ID:      EXP-0028
Date:               2026-08-08
Release:            v0.1.1
Phase:              Phase 4 — Validator Regression Suite and Repository-Consistency Enforcement
Repository:         NudgeWhen
Branch:             release/v0.1.1
Starting commit:    4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4
Ending commit:      Not applicable — this evidence Build creates no commit
Execution stage:    Phase 4E1 — implementation evidence synchronization with E1-R1, E1-R2, E1-R3 and E1-R4 evidence-integrity corrections
```

## Tool and model

```text
Tool:                                  OpenCode
Directly displayed agent and model:    Nudge-Build · MiniMax-M3
Directly observed mode:                Build
```

## Consumption

```text
OpenCode version:           Not available — E1-R1 does not run opencode --version
Access service/provider:    Not available unless directly displayed independently
Displayed model variant:    Not available unless independently displayed
Session identifier:        Not available — private/raw session identifiers are not published
Pre-task usage:            Not available
Post-task usage:           Not available
Token consumption:         Not available
Monetary consumption:      Not available
```

E1-R1 does not infer consumption from request count, elapsed time, model multiplier, or any OpenCode subscription signal.

## Task

EXP-0028 records Phase 4E1 implementation evidence synchronization and the bounded E1-R1, E1-R2, E1-R3 and E1-R4 corrections required to preserve historically accurate Phase 4 implementation evidence. The corrected record must:

- record that EXP-0028 was legitimately assigned when the Phase 4E1 evidence task started; E1-R1, E1-R2, E1-R3 and E1-R4 are corrective stages within EXP-0028 and assign no new experiment identifier; the earlier Phase 4R0-R2 draft mention of EXP-0028 was discarded because the evidence-record task had not yet started at that point;
- preserve the blocked Phase 4R0 and Phase 4R0-R1 planning history;
- preserve the R2 source-completion result and the fact that its architecture required maintainer correction;
- preserve the accepted Phase 4R0-R3 corrected architecture result;
- preserve implementation attempts, corrections, blocked attempts, technical successes, protocol deviations, and final-response defects;
- record the accepted 64-test regression result;
- record the accepted repository-consistency enforcement result;
- record CI/documented canonical-gate integration;
- record candidate no-residue evidence;
- record the Phase 4 / Phase 5 boundary;
- record known pre-closure limitations;
- correct the E1 evidence-integrity defects described in section "Phase 4E1 — evidence synchronization original attempt" below;
- distinguish maintainer-audited prior-stage evidence, direct E1 observations supplied through maintainer audit, and direct E1-R1 observations;
- leave the evidence artifact untracked and unstaged for maintainer review.

The evidence record is not Phase 4 closure. It must not classify Phase 4 Complete.

## Authorization and scope

Files authorized to read in E1 (pre-write, bounded):

- `docs/agentic-development/experiment-protocol.md` — line 1 through explicit EOF.
- `docs/releases/v0.1.1/phase-list.md` — lines 360 through 470 inclusive.

Files authorized to read in E1-R1 (pre-correction, bounded):

- `docs/releases/v0.1.1/phase-list.md` — lines 324 through 387 inclusive (the complete active Phase 4 section).
- `docs/agentic-development/experiments/EXP-0028.md` — line 1 through explicit EOF (pre-correction full readback of the file being corrected).

Files authorized to modify in E1-R1:

- `docs/agentic-development/experiments/EXP-0028.md` (this record) — exactly once.

Files explicitly not authorized to read or modify in E1 or E1-R1:

- `tests/*` (including `tests/__init__.py`, `tests/_helpers.py`, `tests/test_validator_core.py`, `tests/test_validator_repository.py`).
- `scripts/validate_local.py`.
- `docs/local-validation.md`.
- `.github/workflows/ci.yml`.
- `AGENTS.md`.
- `docs/releases/v0.1.1/release-charter.md`.
- `scripts/release_contract.json`.
- `EXP-0027.md` and any other prior experiment record.
- Any path matching the generic `session-ses_*.md` private working-material pattern.

Actions explicitly not authorized in E1-R1:

- staging, committing, pushing, fetching, pulling, branch operations, tag operations, release operations, pull-request operations, network access, dependency installation, Gradle execution, Android SDK execution, OpenCode harness changes, Hermes integration, Phase 5 work, closure synchronization, creation of any further experiment record.

Scope-compliance result: `compliant` (see section "Scope and execution classification").

## Evidence-source classification

EXP-0028 distinguishes four evidence categories:

1. **Maintainer-audited prior-stage evidence supplied to EXP-0028.**

   This includes R0, R0-R1, R0-R2, R0-R3, B1 through Phase 4I1, and the E1 maintainer-audit correction supplied to R1.

   R2 rejection and R3 acceptance are in this category. They are NOT inference.

2. **Direct E1 observations preserved through maintainer audit.**

   This includes:
   - E1 baseline;
   - EXP-0028 creation;
   - E1 post-write readback;
   - E1 64-test result;
   - E1 8/11/19 focused validator results;
   - E1 final eight-path state;
   - E1 unchanged HEAD.

3. **Direct E1-R1 pre-artifact observations.**

   This includes:
   - the complete Phase 4 section read in R1;
   - the current EXP-0028 pre-correction read;
   - R1 64-test result;
   - R1 8/11/19 focused validator results;
   - R1 pre-correction eight-path candidate state.

4. **Maintainer-audited completed E1-R1 post-artifact evidence.**

   This includes:
   - the corrected EXP-0028 complete readback;
   - R1 final eight-path state;
   - R1 final tracked diff;
   - R1 final empty index;
   - R1 final clean tracked `git diff --check`;
   - R1 final HEAD;
   - R1 final-tool boundary.

```text
Inference:
None required for the classifications above.
```

E1-R1 does not replay or reopen private session transcripts. No `session-ses_*.md` file is read, searched, listed, or mentioned by concrete identifier in this record.

## Phase 4 objective and starting state

The active Phase 4 section in `docs/releases/v0.1.1/phase-list.md` is the current authoritative statement. E1-R1 directly read the complete Phase 4 section from the corrected authorized range (lines 324–387) and confirmed the objective, deliverables, path scope, required behavior, boundaries, required validation, completion evidence and `Planned` status.

### Direct E1-R1 Phase 4 objective (observed at line 331)

```text
Add tests for the validator itself and ensure the repository gate
detects the stale-state contradictions that escaped v0.1.0 validation.
```

### Direct E1-R1 Phase 4 principal deliverables (observed at lines 335–340)

```text
- standard-library Python test suite under an agreed tests/ structure
- minimal deterministic fixtures
- additional repository-consistency checks in the validator
- a single documented command or gate sequence combining tests and
  repository validation
- CI integration for the validator test suite
- phase experiment record
```

### Direct E1-R1 Phase 4 boundaries (observed at line 354)

```text
- no Android functionality
- no production dependencies
- no network-backed tests
- no emulator tests
- no attempt to test conversational agent behavior that is not
  repository-observable
```

### Direct E1-R1 Phase 4 status (observed at line 376)

```text
Planned
```

### E1 source defect (authoritative record)

```text
E1 source defect:
the authorized E1 phase-list window began at line 360 and exposed only
the tail of Phase 4; it did not contain the heading, objective,
principal deliverables, path scope, required behavior or boundaries.

E1 should therefore have hard-stopped before mutation.

E1-R1 directly read the complete Phase 4 section from the corrected
authorized range and confirmed the objective, deliverables, boundaries,
validation requirements, completion evidence and Planned status.
```

E1 starting working-tree state (maintainer-audited prior-stage evidence supplied to R1; the seven-path state at E1 start was the accepted pre-E1 candidate):

```text
git branch --show-current
release/v0.1.1

git rev-parse HEAD
4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4

git status --short --untracked-files=all
 M .github/workflows/ci.yml
 M docs/local-validation.md
 M scripts/validate_local.py
?? tests/__init__.py
?? tests/_helpers.py
?? tests/test_validator_core.py
?? tests/test_validator_repository.py

git diff --name-status
M	.github/workflows/ci.yml
M	docs/local-validation.md
M	scripts/validate_local.py

git diff --cached --name-status
(no output)

git diff --check
(no output)
```

E1 only added `docs/agentic-development/experiments/EXP-0028.md` to the untracked set, producing the expected eight-path cumulative candidate.

E1-R1 starting working-tree state (direct observation, six commands):

```text
git branch --show-current
release/v0.1.1

git rev-parse HEAD
4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4

git status --short --untracked-files=all
 M .github/workflows/ci.yml
 M docs/local-validation.md
 M scripts/validate_local.py
?? docs/agentic-development/experiments/EXP-0028.md
?? tests/__init__.py
?? tests/_helpers.py
?? tests/test_validator_core.py
?? tests/test_validator_repository.py

git diff --name-status
M	.github/workflows/ci.yml
M	docs/local-validation.md
M	scripts/validate_local.py

git diff --cached --name-status
(no output)

git diff --check
(no output)
```

E1-R1 does not modify any of those paths. E1-R1 only edits the untracked `docs/agentic-development/experiments/EXP-0028.md` evidence record.

## Planning chronology

The following sections preserve maintainer-audited prior-stage evidence supplied to E1-R1. E1-R1 does not modify or upgrade any of these classifications.

### Phase 4R0

```text
Repository result:
clean read-only Phase 4R0 baseline and partial architectural evidence
produced; no implementation candidate

Outcome:
Blocked by environment

Mutation-path compliance:
not applicable

Overall scope compliance:
compliant

Required read completeness:
BLOCKED

Required architectural analysis:
BLOCKED

Execution protocol:
compliant hard stop at platform step limit

Final-response integrity:
failed

Phase 3 formal repository status:
Complete

Phase 4 status:
Planned
```

### Phase 4R0-R1

```text
Repository result:
clean read-only Phase 4 architectural evidence expanded; final
architecture still incomplete

Outcome:
Blocked by environment

Mutation-path compliance:
not applicable

Overall scope compliance:
compliant

Required read completeness:
BLOCKED — five files remained

Required architectural analysis:
BLOCKED

Execution protocol:
compliant hard stop at platform step limit

Final-response integrity:
failed

Phase 4 status:
Planned
```

### Phase 4R0-R2

Maintainer-audited prior-stage evidence: R2 completed the required Phase 4 source collection; required Phase 4 source evidence became complete; the repository remained unchanged; the R2 architecture itself was not accepted; maintainer audit identified 24 architecture corrections; R2's premature `EXP-0028` assignment was discarded; R2 source evidence remained accepted and was reused by R3. E1-R1 does not characterize the rejected R2 architecture as the accepted final design. R2 rejection is a maintainer-audited prior-stage classification, not an inference.

### Phase 4R0-R3

```text
Repository result:
corrected complete Phase 4 execution architecture produced; repository
unchanged

Outcome:
Successful with correction

Mutation-path compliance:
not applicable

Overall scope compliance:
compliant

Required source evidence:
already complete

Architectural analysis:
compliant after R3 correction

Execution protocol:
compliant

Final-response integrity:
compliant

Phase 3 formal repository status:
Complete

Phase 4 status:
Planned
```

R3 established:

- one cumulative Phase 4 implementation/evidence candidate;
- dirty-candidate validation without `--require-clean`;
- clean-repository proof only after maintainer commit;
- standard-library `unittest` regression suite;
- real-validator testing;
- bounded repository-consistency enforcement;
- bounded CI/TOML checks rather than generic parsers;
- dedicated evidence synchronization;
- separate implementation/evidence commit;
- separate closure synchronization.

R3 acceptance is a maintainer-audited prior-stage classification, not an inference.

## Accepted architecture

The accepted Phase 4 architecture (R3) is the source of all implementation stages recorded below. The implementation chronology uses the R3 architecture; the rejected R2 architecture is recorded only for traceability and is not the basis of any implementation outcome in this record.

## Implementation chronology

The chronology below is maintainer-audited prior-stage evidence supplied to E1-R1. E1-R1 does not erase an earlier defect because a later correction succeeded.

### B1 — initial regression harness

```text
23 tests
technical result accepted
protocol deviations present
overall outcome Partially successful
```

### B2 — 27 tests, introduced Android-guard defect

```text
27 tests
introduced Android-guard defect
protocol deviations present
overall outcome Partially successful
```

### Original B3 — blocked by environment

```text
0 edits
blocked by environment
no implementation result
```

### B3-R1 — Android guard corrected

```text
Android guard corrected
30 tests
technical correction succeeded
unauthorized `true` command occurred after the required final HEAD
overall outcome Partially successful
```

### B3-R2 — five new coverage families

```text
35 tests
added temporary-Git, malformed JSON, invalid-contract,
tracked-bytecode and prohibited-output coverage
final proof became blocked
overall outcome Partially successful
```

### B3-R2-C1 — clean correction

```text
35 tests
clean correction
Successful first pass
existing-validator negative-path B3 scope complete
```

### Original B4 — broken active-release fixture

```text
active-release identity broken fixture
technical attempt incomplete
overall outcome Partially successful
```

### B4-R1 — fixture corrected

```text
fixture corrected
41 tests
three README regex failures remained
overall outcome Partially successful
```

### B4-R2 — technical correction succeeded

```text
41 tests
technical correction succeeded
required/docs/skip-Android validators passed
source-read deviation occurred
overall outcome Partially successful
B4 technically complete
```

### Original B5A — sorting defect

```text
document-status summary implementation
43-test suite
sorting Phase labels through integer conversion caused eight errors
mandatory hard stop
technical correction required
```

### B5A-R1 — corrected

```text
43 tests passed
focused validators passed
source/read and command-form (`2>&1`) deviations occurred
overall outcome Partially successful
B5A technically complete
```

### Original B5B — workflow-missing fail-open

```text
five CI invariants added
48 tests
workflow-missing behavior failed open and fixture isolation was defective
B5B incomplete
```

### B5B-R1 — corrected

```text
49 tests
fail-closed workflow behavior and fixture isolation corrected
focused validators passed
source-read deviation and incomplete final proof
overall outcome Partially successful
B5B technically complete
```

### B5B-R1-C1 — validation-only continuation

```text
validation-only continuation
49 tests
focused validators passed
candidate and final HEAD proof complete
Successful first pass
B5B complete
```

### B5C-P0 — corrected Phase 4 enforcement matrix planning

```text
corrected Phase 4 enforcement matrix planning
42-row matrix accepted
planning session itself Partially successful because of source-read /
matrix-analysis defects
matrix result retained as accepted architecture input
```

### B5C — CI-structure family

```text
CI-structure family
52 tests
technical result Successful first pass

covered representative malformed/top-level CI structure including:
top-level on
validate display name
top-level read-only contents permission

focused validators passed

source-read protocol violated
final response incorrectly claimed full compliance and duplicated
checklist structure

overall outcome Partially successful

CI structure family technically complete
```

### B5D — AGENTS current-governance family (original)

```text
AGENTS current-governance family
54 tests
focused validators passed

technical defect:
empty readable AGENTS.md was conflated with unreadable/non-UTF8 state

technical correction required
overall outcome Partially successful
```

### B5D-R1 — corrected

```text
55 tests
fixed only the source-state defect

empty readable AGENTS.md now distinguished from unreadable/non-UTF8

technical result Successful first pass

after a failed Edit, an unauthorized partial source read occurred

final response incorrectly claimed no deviation

overall outcome Partially successful

AGENTS current-governance family technically complete
```

### B5E — current-facing false-absence family

```text
current-facing false-absence family
58 tests

bounded current-facing documents:
README.md
CONTRIBUTING.md
SECURITY.md

covered false claims that:
Android application code is absent
CI workflow is absent
released/published baseline is absent

missing source skipped
unreadable UTF-8 fails closed

technical result Successful first pass
overall outcome Successful first pass
family complete
```

### B5F1 — rows 41 and 42

```text
rows 41 and 42
60 tests

test-suite required-path presence contract
candidate visibility of allowlisted untracked Phase 4 tests

production candidate allowlist behavior characterized without weakening

technical result Successful first pass
overall outcome Successful first pass

Rows 41 and 42:
FULLY ENFORCED + REGRESSION COVERED
```

### B5F2 — rows 18 and 19

```text
rows 18 and 19
62 tests

malformed source Android manifest fails release-contract validation
current app versionName mismatch fails release-contract validation

production behavior unchanged

technical result Successful first pass

source-read protocol failed because the physical-read continuation
handling did not satisfy the task's literal read-count contract

final response incorrectly denied deviation

overall outcome Partially successful

Rows 18 and 19:
FULLY ENFORCED + REGRESSION COVERED
```

### Original B5F3 — row 17 blocked before mutation

```text
row 17 broken relative Markdown-link characterization

authorized production source window was stale and did not contain the
required implementation evidence

MiniMax correctly did not escape the authorized source window

no mutation

technical result BLOCKED before mutation

overall outcome Partially successful

Row 17 remained:
PRODUCTION ENFORCED / REGRESSION MISSING
```

### B5F3-R1 — row 17 retry

```text
row 17 retry
63 tests
broken relative Markdown-link regression successfully exercised the
real docs/md-links branch

technical result Successful first pass

the authorized production window did not contain the full
resolve_md_link() definition required by the task

MiniMax recognized that fact but continued instead of executing the
mandatory hard stop

final response denied the deviation

overall outcome Partially successful

Row 17:
FULLY ENFORCED + REGRESSION COVERED
```

### Original B5F4 — row 20 blocked before mutation

```text
row 20 bounded malformed version-catalog input

prescribed isolated valid-catalog fixture could not make the complete
real check_android_content() return True because the later source
manifest is mandatory

MiniMax correctly hard-stopped before mutation

technical result BLOCKED before mutation

overall outcome Partially successful

Row 20 remained:
REGRESSION MISSING / PRODUCTION ENFORCEMENT INCOMPLETE
```

### B5F4-R1 — row 20 retry

```text
row 20 retry
64 tests

added private standard-library-only:
_version_catalog_failures(text)

real check_android_content() consumes the helper

each release-critical version key must have exactly one bounded quoted
assignment

duplicate agp with two correct 9.2.1 values fails as:
agp=count:2

valid bounded helper input returns no failure

real duplicate check_android_content() integration fails fast before
source-manifest validation

no generic TOML parser
no tomllib
no dependency

technical result Successful first pass

overall outcome Partially successful

protocol defects:
unauthorized pre-edit scripts/validate_local.py lines 1–40 read
required post-edit full test_validator_core.py readback was incomplete
source read occurred after final HEAD

final-response integrity failed

Row 20:
FULLY ENFORCED + REGRESSION COVERED
```

### Phase 4I1 — CI and documented canonical-gate integration

```text
CI and documented canonical-gate integration

CI:
added exactly one validator-regressions step immediately before
validate-local

regression command:
python3 -B -m unittest tests.test_validator_core tests.test_validator_repository

existing clean validator command preserved:
./scripts/validate-local.sh --require-clean

docs/local-validation.md:
documents clean committed/CI sequence
documents dirty-candidate sequence without --require-clean

64 tests passed

focused candidate validators:
required pass=8 fail=0 skip=0
docs pass=11 fail=0 skip=0
skip-Android pass=19 fail=0 skip=0

technical result Successful first pass

source-read protocol compliant
execution protocol compliant
final HEAD/final-tool boundary compliant

final-response integrity failed because:
two consecutive `## Validation checklist` headings were emitted
checklist lines were prefixed by Markdown bullets instead of beginning
directly with the required status token
the response incorrectly self-classified final-response integrity as
compliant

overall outcome Partially successful
```

## Accepted regression-suite result

The accepted regression suite is:

```text
64 tests
Ran 64 tests
OK
```

run with:

```text
python3 -B -m unittest tests.test_validator_core tests.test_validator_repository
```

The suite uses Python's standard-library `unittest` only. It does not install dependencies, does not execute network commands, and does not open private working material. Bytecode generation is suppressed with `-B`. The suite exercises:

- argument parsing, group resolution, unknown and contradictory arguments, exit codes, release-gate calculation;
- missing `.git`, malformed inputs, broken Markdown links, stale active-branch references, false-absence claims, phase-status summary disagreement;
- CI trigger coverage, missing wrapper checksum, bytecode detection;
- clean and dirty working-tree behavior;
- direct invocation of the real validator;
- malformed source Android manifest release-contract failure;
- current app `versionName` mismatch release-contract failure;
- row 17 broken relative Markdown-link regression;
- row 20 bounded malformed version-catalog regression (via the private standard-library-only `_version_catalog_failures(text)` helper, without any generic TOML parser and without `tomllib`).

## Repository-consistency enforcement result

The accepted repository-consistency families are recorded below. E1-R1 does not modify the corresponding `scripts/validate_local.py` implementation in this Build.

### Family: active governance (Phase 4R0-R3 / B4 family)

- Detects active governance pointing to a completed release.
- Detects current-facing documentation naming a deleted release branch.
- Detects contradictions between individual phase statuses and their summary.
- Covered by B4, B4-R1, B4-R2; B4 technically complete.

### Family: CI structure (B5C)

- Detects top-level `on` issues.
- Detects `validate display name` issues.
- Detects top-level `read-only contents permission` issues.
- CI-structure family technically complete; final-response integrity defects preserved.

### Family: AGENTS current governance (B5D, B5D-R1)

- Detects empty readable `AGENTS.md` distinctly from unreadable/non-UTF8.
- AGENTS current-governance family technically complete; final-response integrity defects preserved.

### Family: current-facing false-absence (B5E)

- Detects false claims that Android application code is absent.
- Detects false claims that CI workflow is absent.
- Detects false claims that released/published baseline is absent.
- Missing source is skipped; unreadable UTF-8 fails closed.
- Family complete; clean first pass.

### Family: rows 41 and 42 — test-suite required-path presence (B5F1)

- Test-suite required-path presence contract.
- Candidate visibility of allowlisted untracked Phase 4 tests.
- Production candidate allowlist behavior characterized without weakening.
- Rows 41 and 42: `FULLY ENFORCED + REGRESSION COVERED`.

### Family: rows 18 and 19 — Android release-contract (B5F2)

- Malformed source Android manifest fails release-contract validation.
- Current app `versionName` mismatch fails release-contract validation.
- Production behavior unchanged.
- Rows 18 and 19: `FULLY ENFORCED + REGRESSION COVERED`.

### Family: row 17 — broken relative Markdown-link (B5F3, B5F3-R1)

- Broken relative Markdown-link regression successfully exercises the real `docs/md-links` branch.
- Row 17: `FULLY ENFORCED + REGRESSION COVERED` (B5F3-R1 correction preserved).

### Family: row 20 — bounded malformed version-catalog (B5F4, B5F4-R1)

- Bounded malformed version-catalog regression via private standard-library-only `_version_catalog_failures(text)` helper consumed by the real `check_android_content()`.
- No generic TOML parser; no `tomllib`; no dependency.
- Row 20: `FULLY ENFORCED + REGRESSION COVERED` (B5F4-R1 correction preserved; protocol defects preserved).

## CI and documented canonical-gate integration

The accepted CI integration is `Phase 4I1`:

- `.github/workflows/ci.yml` adds exactly one `validator-regressions` step immediately before `validate-local`. The step runs the regression command above.
- The existing clean validator command `./scripts/validate-local.sh --require-clean` is preserved.
- `docs/local-validation.md` documents both the clean committed/CI sequence and the dirty-candidate sequence without `--require-clean`.

The final-response defects of Phase 4I1 are preserved in this record: two consecutive `## Validation checklist` headings; checklist lines prefixed by Markdown bullets; incorrect self-classification of final-response integrity as compliant. E1-R1 reproduces a single `## Validation checklist` at the end of this record, with each line beginning directly with the required status token and no Markdown bullet prefix.

## E1 and E1-R1 validation evidence

The block below records the direct E1 observations supplied to E1-R1 through maintainer audit, and the direct E1-R1 observations obtained in this Build. R1 evidence is split into two accurately labelled categories: pre-artifact direct observations (obtained before this corrected artifact was written) and post-artifact maintainer-audited evidence (the completed R1 final readback, candidate proof, and final HEAD/tool sequence, supplied to E1-R2 through maintainer audit and not pre-attested inside this file).

```text
E1 regression (maintainer-audited prior-stage):
64 tests / OK

E1 required (maintainer-audited prior-stage):
8/0/0

E1 docs (maintainer-audited prior-stage):
11/0/0

E1 skip-Android (maintainer-audited prior-stage):
19/0/0

E1 final candidate (maintainer-audited prior-stage):
eight paths, matching its pre-validation candidate

E1 index (maintainer-audited prior-stage):
empty

E1 tracked diff --check (maintainer-audited prior-stage):
clean

E1 HEAD (maintainer-audited prior-stage):
4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4

Direct E1-R1 pre-artifact observations (obtained before this corrected artifact was written):
- complete Phase 4 source read
- current pre-correction EXP-0028 read
- 64-test regression / OK
- required 8/0/0
- docs 11/0/0
- skip-Android 19/0/0
- pre-correction eight-path state
- empty index
- clean tracked git diff --check
- pre-edit HEAD 4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4

Maintainer-audited completed E1-R1 post-artifact evidence (supplied to E1-R2 through maintainer audit, NOT pre-attested inside this file):
- corrected EXP-0028 complete readback: 1320 lines through EOF
- final eight-path state matched the pre-correction path set
- final tracked diff remained the same three paths
- final index remained empty
- final tracked git diff --check remained clean
- final HEAD was 4cc0f084b40e51afb7e2caee0dd1b3d726bf88f4
- git rev-parse HEAD was the final R1 tool invocation
```

E1 evidence above is maintainer-audited prior-stage evidence supplied to R1.

R1 pre-artifact observations above are direct R1 observations obtained before the corrected artifact was written.

R1 post-artifact observations above are maintainer-audited completed evidence supplied to E1-R2; they are NOT pre-artifact direct R1 evidence.

## Candidate no-residue evidence

E1-R1 does not claim a clean-repository proof. The eight-path candidate is intentionally dirty. E1-R1 records a **candidate no-residue proof** by comparing the pre-edit validation results against the post-edit candidate state and confirming the eight-path set is preserved exactly. The "candidate no-residue" terminology remains distinct from a clean-repository proof. A clean-repository proof requires maintainer commit and re-run.

E1-R1 also records the following important untracked-evidence limitation:

- the candidate `docs` validator does not directly prove the content of an arbitrary untracked experiment record;
- `git diff --check` does not inspect the untracked `EXP-0028` file;
- the explicit full-file post-edit readback is therefore the direct E1-R1 content verification for `EXP-0028`;
- after maintainer commit, exact-head CI will validate `EXP-0028` as tracked repository content.

## Phase 4 / Phase 5 boundary

The following items are preserved as Phase 5 work and are not implemented in Phase 4:

```text
distributionSha256Sum implementation
wrapper-JAR approved-checksum implementation
.gitignore __pycache__/
.gitignore *.py[cod]
Dependabot configuration
versionCode = 2
versionName = 0.1.1
dependency upgrades
Android functionality changes
OpenCode harness changes
Hermes integration
```

The active Phase 5 section in `docs/releases/v0.1.1/phase-list.md` is `Planned`. Phase 4 may have characterized related current behavior (for example, current `versionName` mismatch in B5F2) but did not implement any Phase 5 transition.

## Known limitations and pre-closure documentation debt

Current-facing documentation synchronization is intentionally not folded into E1-R1. In particular:

- `docs/local-validation.md` still contains inherited pre-closure Phase 4 wording and historical/current count statements that may now be stale after repository-consistency enforcement and the accepted regression suite.
- Phase 4 closure synchronization will reassess `docs/local-validation.md` together with the other formal closure paths.
- E1-R1 does not edit those statements because this task is evidence-only.

E1-R1 does not "fix" those statements now. E1-R1 does not infer that their presence invalidates the accepted implementation candidate. They are recorded as pre-closure synchronization debt.

The accepted prior-stage final-response defects of B3-R1, B3-R2, B4-R2, B5A-R1, B5B-R1, B5C, B5D-R1, B5F2, B5F3-R1, B5F4-R1, and Phase 4I1 are also limitations of the corresponding implementation stages. They are preserved in the chronology above; they are not erased by the current E1-R1 evidence-integrity correction. E1-R1 itself attempts to satisfy the final-response integrity contract and reproduce a single `## Validation checklist` section.

## Phase 4E1 — evidence synchronization original attempt

The block below is the authoritative E1 classification. It is preserved permanently and is not rewritten as a clean first-pass success.

```text
Repository result:
eight-path cumulative Phase 4 implementation/evidence candidate with
EXP-0028 created, but the EXP-0028 evidence artifact requires
correction before maintainer review

Phase 4E1 technical result:
correction required

Phase 4E1 overall session outcome:
Partially successful

Phase 4E1 mutation-path compliance:
compliant — only EXP-0028 was created

Phase 4E1 overall scope compliance:
compliant — no unauthorized repository path was read or mutated

Phase 4E1 source-read protocol:
compliant as to authorized physical read regions

Phase 4E1 required Phase-4-source evidence:
failed — lines 360–470 did not contain the complete Phase 4 section

Phase 4E1 mandatory hard-stop compliance:
failed — MiniMax declared the incomplete Phase 4 window complete and
continued through mutation

Phase 4E1 command-form compliance:
compliant

Phase 4E1 experiment identifier:
EXP-0028 legitimately assigned at evidence-task start

Phase 4E1 evidence-source classification:
correction required — supplied R2/R3 classifications were incorrectly
described as inference

Phase 4E1 evidence artifact completeness:
failed — final Validation checklist contains no checklist entries;
direct validation evidence was not finalized in the artifact; internal
references point to sections that do not exist

Phase 4E1 task-specification sequencing:
defective — E1 required post-validation direct evidence inside an
artifact that had to be frozen before those validations ran

Phase 4E1 regression result:
compliant — 64 tests passed

Phase 4E1 required validation:
compliant — pass=8 fail=0 skip=0

Phase 4E1 docs validation:
compliant — pass=11 fail=0 skip=0

Phase 4E1 skip-Android validation:
compliant — pass=19 fail=0 skip=0

Phase 4E1 candidate no-residue proof:
compliant

Phase 4E1 final HEAD proof:
compliant

Phase 4E1 final-tool boundary:
compliant

Phase 4E1 execution protocol:
failed — mandatory pre-mutation hard stop was ignored

Phase 4E1 final-response integrity:
failed — response did not begin exactly with the required heading,
claimed no deviation, falsely claimed complete Phase 4 source evidence,
and incorrectly self-classified E1 as fully compliant

Phase 4 production/regression matrix:
FULLY ACCEPTED

Phase 4 CI/documented canonical-gate integration:
COMPLETE

Current accepted regression suite:
64 tests

Phase 4 implementation/evidence candidate:
CORRECTION REQUIRED BEFORE MAINTAINER REVIEW

Phase 4 formal status:
Planned
```

### Phase 4E1-R1 — evidence-integrity correction

```text
correction target:
EXP-0028 only

reason:
E1 incomplete Phase 4 source evidence
ignored mandatory hard stop
E1 prompt sequencing defect
incomplete artifact checklist
stale/pending validation classification
incorrect evidence-source classification
incorrect no-post-write-read statement
broken internal candidate-evidence references
final-response exact-opening/integrity defects

direct R1 pre-edit regression:
64 tests / OK

direct R1 pre-edit required:
8/0/0

direct R1 pre-edit docs:
11/0/0

direct R1 pre-edit skip-Android:
19/0/0
```

E1-R1 successfully corrected the major E1 evidence defects and all repository/test validation succeeded. However, the R1 artifact itself prematurely classified post-write/final-tool evidence as already direct before those observations had occurred. The R1 task specification also contained a sequencing defect because its prescribed internal checklist required complete-readback verification to be marked PASS inside the artifact before that readback happened.

The authoritative R1 correction is therefore:

```text
Phase 4E1-R1 technical result:
correction required

Phase 4E1-R1 overall session outcome:
Partially successful

Phase 4E1-R1 mutation-path compliance:
compliant

Phase 4E1-R1 overall scope compliance:
compliant

Phase 4E1-R1 source-read protocol:
compliant

Phase 4E1-R1 complete Phase 4 source evidence:
compliant

Phase 4E1-R1 command-form compliance:
compliant

Phase 4E1-R1 regression result:
compliant — 64 tests passed

Phase 4E1-R1 required validation:
compliant — pass=8 fail=0 skip=0

Phase 4E1-R1 docs validation:
compliant — pass=11 fail=0 skip=0

Phase 4E1-R1 skip-Android validation:
compliant — pass=19 fail=0 skip=0

Phase 4E1-R1 actual post-edit readback:
compliant

Phase 4E1-R1 actual final candidate proof:
compliant

Phase 4E1-R1 actual final HEAD proof:
compliant

Phase 4E1-R1 actual final-tool boundary:
compliant

Phase 4E1-R1 artifact temporal-evidence integrity:
failed — post-write/final-tool evidence was preclaimed before direct
observation

Phase 4E1-R1 task-specification sequencing:
defective — the required internal checklist itself prescribed a
complete-readback PASS before that readback could occur

Phase 4E1-R1 final-response integrity:
failed — final report claimed no deviation and Successful first pass
```

The actual later R1 final Git/tool sequence itself was correct. The defect is that the R1 artifact pre-attested that future sequence. E1-R2 does not erase that distinction.

The original R1 artifact did preclaim final-tool and post-artifact
evidence before those observations occurred.

The completed R1 final candidate proof and final git rev-parse HEAD
were later observed successfully and are now recorded as
maintainer-audited completed R1 evidence.

## Task-specification finding

```text
Task-specification finding:
E1 required direct validation evidence inside EXP-0028 but ordered the
one-time artifact write/readback before those validations and prohibited
later source edits.

That sequencing was internally incompatible.

E1-R1 corrects the evidence architecture by performing direct R1
validation before the evidence-file correction.
```

This is a task-specification defect. It is not attributed solely to MiniMax.

## E1 source-read classification (corrected)

```text
E1 source-read protocol:
compliant as to authorized physical read regions

E1 authorized physical-read scope:
compliant

E1 required Phase-4-source completeness:
failed

E1 mandatory hard-stop behavior:
failed

E1 actual read structure:
two authorized pre-write source reads (experiment-protocol.md and the
phase-list.md lines 360–470 window)
one authorized mandatory post-write full EXP-0028 readback
no source read after that completed EXP-0028 readback
```

These are distinct dimensions. The corrected record no longer claims "no post-write source read" for E1.

## E1 final-response findings (corrected)

```text
response emitted prose before the exact required opening heading

response claimed no deviations despite the ignored hard-stop defect

response claimed the complete Phase 4 source section was visible when
it was not

response self-classified E1 as fully successful

response self-classified final-response integrity as compliant
```

E1's external checklist formatting itself used one heading and direct status-token entries successfully. E1-R1 reproduces that single-heading direct-token pattern at the end of this record.

## Repository result

```text
Phase 4E1-R1 produces an eight-path cumulative Phase 4
implementation/evidence candidate:

- three tracked modifications: .github/workflows/ci.yml,
  docs/local-validation.md, scripts/validate_local.py
- five untracked additions: tests/__init__.py, tests/_helpers.py,
  tests/test_validator_core.py, tests/test_validator_repository.py,
  docs/agentic-development/experiments/EXP-0028.md

Phase 4E1-R1 itself modifies exactly one file
(docs/agentic-development/experiments/EXP-0028.md). E1-R1 does not
stage, commit, push, fetch, or pull. E1-R1 executes no network
command, installs no dependency, runs no Gradle or Android SDK
tool, and modifies no other Phase 4 implementation path.
```

## Outcome classification

```text
Phase 4E1 technical result:           correction required
Phase 4E1 overall outcome:            Partially successful

Phase 4E1-R1 technical result:        correction required
Phase 4E1-R1 overall session outcome: Partially successful

Phase 4E1-R2 technical result:        correction required
Phase 4E1-R2 overall session outcome: Partially successful

Phase 4E1-R3 technical result:        correction required
Phase 4E1-R3 overall session outcome: Partially successful

Phase 4 production/regression matrix: FULLY ACCEPTED
Phase 4 CI/documented canonical-gate integration: COMPLETE
Current accepted regression suite:    64 tests

Phase 4 implementation/evidence candidate:
CUMULATIVE EXP-0028 SYNCHRONIZATION COMPLETE IN ARTIFACT; E1-R4 FINAL
EXECUTION VALIDATION IS REPORTED EXTERNALLY BEFORE MAINTAINER STAGING

Phase 4 formal status:                Planned
```

## Scope and execution classification

```text
Phase 4E1-R1 mutation-path compliance:  compliant (only EXP-0028 modified)
Phase 4E1-R1 overall scope compliance:  compliant
Phase 4E1-R1 source-read protocol:      compliant (two authorized
                                          pre-correction source reads,
                                          no read after the corrected
                                          readback)
Phase 4E1-R1 command-form compliance:   compliant (no shell redirection,
                                          pipelines, command substitution,
                                          or wrappers used)
Phase 4E1-R1 evidence chronology:       compliant
Phase 4E1-R1 actual post-edit readback: compliant (maintainer-audited)
Phase 4E1-R1 actual final candidate proof: compliant (maintainer-audited)
Phase 4E1-R1 actual final HEAD proof:   compliant (maintainer-audited)
Phase 4E1-R1 actual final-tool boundary: compliant (maintainer-audited;
                                          git rev-parse HEAD was the
                                          final R1 tool invocation)
Phase 4E1-R1 artifact temporal-evidence integrity: failed (preserved)
Phase 4E1-R1 task-specification sequencing: defective (preserved)
Phase 4E1-R1 final-response integrity:  failed (preserved)
Phase 4E1-R1 untracked-evidence limitation: accurately recorded

Phase 4E1 mutation-path compliance:     compliant
Phase 4E1 overall scope compliance:     compliant
Phase 4E1 source-read protocol:         compliant as to authorized
                                          physical read regions
Phase 4E1 required Phase-4-source evidence: failed
Phase 4E1 mandatory hard-stop compliance: failed
Phase 4E1 command-form compliance:      compliant
Phase 4E1 evidence-source classification: correction applied in R1
Phase 4E1 evidence artifact completeness: corrected in R1
Phase 4E1 task-specification sequencing:  defective (corrected in R1)
Phase 4E1 regression result:              compliant
Phase 4E1 required validation:            compliant
Phase 4E1 docs validation:                compliant
Phase 4E1 skip-Android validation:        compliant
Phase 4E1 candidate no-residue proof:     compliant
Phase 4E1 final HEAD proof:               compliant
Phase 4E1 final-tool boundary:            compliant
Phase 4E1 execution protocol:             failed (preserved in record)
Phase 4E1 final-response integrity:       failed (preserved in record)
```

## Phase 4E1-R2 — temporal-evidence integrity correction

E1-R2 corrects the R1 temporal-evidence provenance defects.

The E1-R2 artifact deliberately does not self-attest its own future
post-write readback, final Git proof, final-tool boundary or final
response. Those observations occur only after this artifact is frozen
and are reported in the E1-R2 final execution report. This prevents
another self-referential evidence loop.

```text
Repository result:
eight-path cumulative Phase 4 implementation/evidence candidate with
the main temporal-evidence provenance correction applied, but two
stale contradictory R1 statements remained inside EXP-0028

Phase 4E1-R2 technical result:
correction required

Phase 4E1-R2 overall session outcome:
Partially successful

Phase 4E1-R2 mutation-path compliance:
compliant

Phase 4E1-R2 overall scope compliance:
compliant

Phase 4E1-R2 source-read protocol:
compliant

Phase 4E1-R2 command-form compliance:
compliant

Phase 4E1-R2 completed-R1 provenance correction:
substantially compliant

Phase 4E1-R2 temporal-evidence integrity:
failed — the early evidence-source classification still called the R1
post-artifact EXP-0028 readback a direct R1 observation, and the later
R1 correction section still falsely said R1 did not preclaim its own
final-tool success

Phase 4E1-R2 anti-self-reference rule:
compliant for R2 itself

Phase 4E1-R2 actual post-edit readback:
compliant

Phase 4E1-R2 actual final candidate proof:
compliant

Phase 4E1-R2 actual final HEAD proof:
compliant

Phase 4E1-R2 actual final-tool boundary:
compliant

Phase 4E1-R2 execution protocol:
Partially successful — tool sequencing was compliant, but mandatory
semantic verification missed the two remaining contradictions

Phase 4E1-R2 final-response integrity:
failed — prose preceded the exact required heading, the response
reported no deviations, and it self-classified final-response
integrity as compliant

Phase 4 production/regression matrix:
FULLY ACCEPTED

Phase 4 CI/documented canonical-gate integration:
COMPLETE

Current accepted regression suite:
64 tests

Phase 4 formal status:
Planned
```

The E1-R1 post-artifact evidence now recorded in section "E1 and E1-R1 validation evidence" above is supplied to E1-R2 through maintainer audit. It is not described as evidence that was already direct at the time the R1 artifact was written. The R1 artifact's failure to respect that boundary is the defect that E1-R2 corrects.

The R2 final-response failure included:

```text
prose before the exact required opening heading

incorrect "no deviations" claim

incorrect final-response-integrity compliant self-classification
```

The pre-heading prose is not treated as repository mutation. The actual R2 tool execution was compliant, and the R2 artifact temporal-evidence integrity and final-response integrity are the two distinct dimensions on which R2 failed.

### Phase 4E1-R3 — evidence consistency correction

```text
correction target:
EXP-0028 internal evidence consistency only

implementation paths:
unchanged

accepted regression suite:
64 tests

accepted focused validator evidence:
required 8/0/0
docs 11/0/0
skip-Android 19/0/0

Phase 4:
Planned

R3 post-write readback / final Git proof / final execution
classification:
intentionally external and not self-attested here
```

E1-R3 corrected the two targeted R2 contradictions and synchronized
the R2 historical classification, but maintainer audit later identified
remaining cumulative/top-level EXP-0028 inconsistencies.

```text
Repository result:
eight-path cumulative Phase 4 implementation/evidence candidate with
the two targeted R2 contradictions corrected, but EXP-0028 still
required final top-level/cumulative evidence synchronization

Phase 4E1-R3 technical result:
correction required

Phase 4E1-R3 overall session outcome:
Partially successful

Phase 4E1-R3 mutation-path compliance:
compliant

Phase 4E1-R3 overall scope compliance:
compliant

Phase 4E1-R3 source-read protocol:
compliant

Phase 4E1-R3 command-form compliance:
compliant

Phase 4E1-R3 targeted Defect A correction:
compliant

Phase 4E1-R3 targeted Defect B correction:
compliant

Phase 4E1-R3 R1 provenance consistency:
compliant

Phase 4E1-R3 R2 historical classification:
compliant

Phase 4E1-R3 anti-self-reference rule:
compliant

Phase 4E1-R3 time-stable top-level synchronization:
failed — current Execution stage and Task wording remained centered on
E1-R1 although the artifact now records E1-R2 and E1-R3

Phase 4E1-R3 evidence-source classification consistency:
failed — the section said there were three evidence categories while
enumerating four

Phase 4E1-R3 actual post-edit readback:
compliant as a complete read operation, but semantic verification
failed to detect the remaining contradictions

Phase 4E1-R3 actual final candidate proof:
compliant

Phase 4E1-R3 actual final HEAD proof:
compliant

Phase 4E1-R3 actual final-tool boundary:
compliant

Phase 4E1-R3 execution protocol:
Partially successful — repository/tool sequencing was compliant, but
required current-document normalization was omitted and semantic
verification falsely accepted an internally inconsistent artifact

Phase 4E1-R3 final-response integrity:
failed — prose preceded the exact required opening heading, the report
claimed no deviations, and it incorrectly self-classified final-
response integrity as compliant

Phase 4 production/regression matrix:
FULLY ACCEPTED

Phase 4 CI/documented canonical-gate integration:
COMPLETE

Current accepted regression suite:
64 tests

Phase 4 formal status:
Planned
```

actual R3 repository/tool proof:
compliant

R3 targeted R1/R2 corrections:
compliant

R3 cumulative top-level synchronization:
failed

R3 evidence-category-count consistency:
failed

R3 final-response integrity:
failed

R3 final-response defects:
- prose appeared before the required R3 opening heading
- the response claimed there were no actual deviations
- the response incorrectly self-classified final-response integrity as compliant

The pre-heading prose is not treated as repository mutation.

### Phase 4E1-R4 — final cumulative evidence synchronization

```text
correction target:
EXP-0028 cumulative/top-level consistency only

implementation paths:
unchanged

accepted regression suite:
64 tests

accepted focused validator evidence:
required 8/0/0
docs 11/0/0
skip-Android 19/0/0

Phase 4 production/regression matrix:
FULLY ACCEPTED

Phase 4 CI/documented canonical-gate integration:
COMPLETE

Phase 4:
Planned

R4 post-write readback / final Git proof / final execution
classification:
intentionally external and not self-attested here
```

## Remaining Phase 4 work

E1-R1 records the following remaining sequence. E1-R1 does not perform any of these steps.

```text
1. maintainer review of the corrected eight-path candidate
2. manual maintainer staging of exactly the accepted candidate
3. staged-diff review
4. manual maintainer implementation/evidence commit
5. clean committed-state regression proof
6. clean committed-state ./scripts/validate-local.sh --require-clean proof
7. manual push
8. exact-head CI verification
9. separate Phase 4 closure synchronization
10. manual closure staging/commit/push
11. closure exact-head CI verification
```

Phase 4 closure synchronization will eventually need to:

- change Phase 4 from `Planned` to `Complete`;
- advance current-phase pointers to Phase 5;
- synchronize document-status / completed-phase summaries;
- remove or update stale Phase 4 future-work wording;
- preserve the Phase 5 deferred boundary.

E1-R1 does not perform any of those closure edits. The closure boundary at least covers:

```text
AGENTS.md
docs/releases/v0.1.1/phase-list.md
docs/releases/v0.1.1/release-charter.md
scripts/release_contract.json
docs/local-validation.md
```

## Validation checklist

[x] PASS — EXP-0028 was legitimately assigned at E1 task start; E1-R1, E1-R2, E1-R3 and E1-R4 assign no new experiment identifier.

[x] PASS — Failed, blocked, partial and corrected Phase 4 history remains preserved without historical rewriting.

[x] PASS — E1 remains historically Partially successful.

[x] PASS — E1-R1 remains historically Partially successful.

[x] PASS — E1-R2 remains historically Partially successful.

[x] PASS — E1-R3 remains historically Partially successful.

[x] PASS — EXP-0028 uses four explicitly named evidence-source categories and the stated category count matches the enumeration.

[x] PASS — Direct E1-R1 pre-artifact evidence remains separated from maintainer-audited completed R1 post-artifact evidence.

[x] PASS — The completed R1 final Git/tool sequence remains recorded as successful while its original temporal-evidence defect remains preserved.

[x] PASS — R2 tool success and R2 artifact/final-response defects remain distinguished.

[x] PASS — R3 tool success and R3 cumulative-artifact/final-response defects remain distinguished.

[x] PASS — The accepted regression suite remains 64 tests.

[x] PASS — Accepted focused validator evidence remains required 8/0/0, docs 11/0/0 and skip-Android 19/0/0.

[x] PASS — Phase 4 production/regression matrix remains FULLY ACCEPTED.

[x] PASS — Phase 4 CI/documented canonical-gate integration remains COMPLETE.

[x] PASS — Phase 5 work remains deferred.

[ ] BLOCKED — Clean committed-state regression and --require-clean proof await the maintainer implementation/evidence commit.

[ ] BLOCKED — Exact-head remote CI verification awaits maintainer push.

[ ] BLOCKED — Phase 4 closure synchronization and formal Phase 4 Complete status remain outstanding.
