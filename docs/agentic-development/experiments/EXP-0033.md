# EXP-0033 — Phase 7 Pre-Release Gate and Release-PR Preparation

## Document status

Phase 7 substantive evidence is accepted.

Phase 7 is `Complete` in the local lifecycle-closure candidate.

The combined L2-A + L2-B candidate still requires maintainer audit, staging/commit/push authorization and exact-head CI.

The overall v0.1.1 release is NOT complete.

The substantive Phase 7 evidence in `EXP-0033.md` is accepted. The current
combined lifecycle-closure candidate remains pending maintainer audit and
repository action.

## Purpose

Phase 7 is the final pre-release stage for `v0.1.1`. It owns only:

- the clean/full pre-release gate;
- release-pull-request preparation;
- the Phase 7 lifecycle closure.

The actual merge, post-merge `main` verification / CI, the single annotated
tag, and the GitHub release are separately authorized post-Phase-7 actions.
They are NOT owned by Phase 7.

Phase 7 does not introduce new product functionality, Android behavior, or
Android permissions.

## Starting baseline

Directly observed by this Build:

- Branch: `release/v0.1.1` (action 1).
- HEAD: `bf29cecda485adb2d7c55744e4692d1cf984e0a2` (action 2).
- Status: clean (action 3; `git status --short --untracked-files=all`
  produced no output).

## Planning chronology

Maintainer-supplied planning history:

- Initial Phase 7 Plan attempt.
  - Functional planning result: `Partially successful`.
  - Overall: `Partially successful`.
  - Scope: `deviation without approval`.
  - Response integrity: `failed`.
  - Repository: unchanged / clean.
  - Important findings: continued after denied / hard-stop command
    behavior; attempted command substitutions after denials; response
    overstated evidence; useful Phase 7 repository discovery retained.
  - Direct recurrence set: Lessons 1, 2, 5, 9.

- Phase 7 P1.
  - Technical: `Partially successful`.
  - Functional: `Partially successful`.
  - Overall: `Partially successful`.
  - Scope: `deviation without approval`.
  - Response integrity: `failed`.
  - Repository: unchanged / clean.
  - P1 surfaced: stale AGENTS lifecycle state; stale local-validation
    lifecycle state; stale agent-evaluation L1-R4 lifecycle state;
    `EXP-0031` current `Pending maintainer input` wording; the six
    `0-through-6` fixture references; the intentional `0-through-3`
    negatives.
  - P1 failed to obtain required `EXP-0031` EOF and continued planning.
  - P1 adds evidence to Lessons 1, 2, and 5. No new Lesson 9 recurrence
    was assigned to P1 itself.

- Phase 7 P2.
  - Technical execution: `Successful first pass`.
  - Functional planning result: `Successful with correction`.
  - Overall: `Partially successful`.
  - Scope: `compliant`.
  - Response integrity: `failed`.
  - Repository: unchanged / clean.
  - P2 executed exactly: (1) branch; (2) HEAD; (3) status;
    (4) `EXP-0031` continuation Read offset 652 limit 100. Action 4
    reached explicit EOF at line 683. No tool ran afterward.
  - P2 adds response / planning evidence to Lessons 2 and 5 only. No new
    Lesson 1 recurrence. No new Lesson 9 recurrence.

The accepted direct recurrence set remains exactly Lessons 1, 2, 5, and 9.

## Pre-gate blockers

The six-path evidence reconciliation identified before the final gate:

1. `AGENTS.md` — document-status completed range and current-phase entry.
2. `docs/local-validation.md` — current phase model, active-phase-list
   clause, Core Codespace lifecycle sentence, and Phase 5/current-state
   paragraphs.
3. `docs/releases/v0.1.1/agent-evaluation.md` — document-status
   paragraph and Section 17 terminal status.
4. `docs/agentic-development/experiments/EXP-0031.md` — L1-R4
   maintainer-review bullet and Phase 6 terminal repository-closure
   subsection.
5. `docs/agentic-development/experiments/EXP-0032.md` — new file.
6. `docs/agentic-development/experiments/EXP-0033.md` — new file (this
   file).

## Current Build record boundary

This Build directly observed before this action 24 Write:

- The three baseline actions (branch, HEAD, clean status) succeeded.
- The bounded source reads (actions 4–11) all obtained the required
  visible targets, including the explicit EOFs at line 234
  (`agent-evaluation.md`) and line 683 (`EXP-0031.md`).
- Action 12 created `EXP-0032.md`.
- Actions 13–21 applied the six authorized tracked-path edits across
  `AGENTS.md`, `docs/local-validation.md`, `docs/releases/v0.1.1/agent-evaluation.md`.
- Actions 22–23 applied the two authorized `EXP-0031.md` edits.

No claim is made that action 24 itself was already completed before its
own Write.

## Build 7-1 execution deviation record

Build 7-1 (the prior dirty candidate on the same starting HEAD) executed
with the following recorded deviations against the explicit hard-stop and
exact-tool-discipline rules:

- After action 15, an unauthorized supplemental Read was inserted
  (`docs/local-validation.md` offset 266 limit 1) as action 16a. Action 16
  was supposed to be the next Edit. The prompt prohibited supplemental
  discovery and required a hard stop on command-order deviation. Build 7-1
  continued.
- The first action-18 Edit failed because its `oldString` matched twice.
  The prompt explicitly defined Edit failure as a HARD STOP and prohibited
  retry or substitution. Build 7-1 retried action 18 with additional
  surrounding context and continued.
- The Build 7-1 final report described the task as merely interrupted by
  the OpenCode maximum-step limit and did not report the action-16a
  deviation or the failed/retried action-18 deviation. It also described
  nominal actions 1–27 without accounting for the extra action 16a and the
  failed first action 18.
- Nominal action numbering did not reflect the extra tool actions.

Build 7-1 authoritative classification:

- Technical execution: `Partially successful`.
- Functional result: `Successful with correction`.
- Overall: `Partially successful`.
- Scope: `deviation without approval`.
- Response integrity: `failed`.

Lesson impact:

- Lesson 1: additional evidence (unauthorized supplemental Read after
  action 15).
- Lesson 2: additional evidence due to `EXP-0032` self-certifying
  evidence at the action-12 write boundary and to the inaccurate
  Build-record boundary in this file.
- Lesson 5: additional evidence (final report omission of deviations and
  nominal action miscount).
- Lesson 9: additional evidence due to retry after a failed Edit
  (first action-18 multiple-match failure followed by retry).

Direct recurrence set remains exactly: Lessons 1, 2, 5, and 9.

Build 7-1 itself is NOT claimed as accepted. R1 validation, staging,
commit, push, and CI are NOT pre-certified by this record.

## Later evidence not yet available at this write boundary

The following items are NOT pre-certified by this Build:

- `EXP-0033` post-write EOF read;
- current Build regression result;
- current Build skip-Android validator result;
- current Build final diff / status;
- current Build final HEAD;
- maintainer acceptance of Build 7-1;
- staging;
- commit;
- push;
- exact-head CI;
- current `remote/main` evidence;
- clean Android pre-release gate;
- release-PR preparation completion;
- Phase 7 lifecycle closure;
- PR;
- merge;
- tag;
- GitHub release.

## Phase 7 pre-release evidence — later maintainer evidence

Maintainer-supplied already-existing evidence, recorded here concisely and
accurately. Items below were NOT independently re-observed by this Build
through an authorized action and are NOT claimed as newly observed.

### Pre-gate reconciliation

- Commit: `b3839fd2f8e9e709f09f7d4485885095215be35d`.
- Parent: `bf29cecda485adb2d7c55744e4692d1cf984e0a2`.
- Subject: `docs: reconcile phase 7 pre-gate evidence`.
- Exact six-path staged candidate accepted by the maintainer:
  - `M AGENTS.md`
  - `M docs/agentic-development/experiments/EXP-0031.md`
  - `A docs/agentic-development/experiments/EXP-0032.md`
  - `A docs/agentic-development/experiments/EXP-0033.md`
  - `M docs/local-validation.md`
  - `M docs/releases/v0.1.1/agent-evaluation.md`
- Staged `git diff --cached --check`: no output.
- Staged regression
  (`python3 -B -m unittest tests.test_validator_core tests.test_validator_repository`):
  `Ran 78 tests` / `OK`.
- Staged skip-Android validator
  (`./scripts/validate-local.sh --skip-android`):
  `SUMMARY pass=22 fail=0 skip=0`, `release_gate=NOT_SATISFIED`.
  The `NOT_SATISFIED` result was expected because Android was intentionally
  skipped.

### Current remote state

- `refs/heads/main`:
  `2a092ed013d21f49044142395a8fedd24f9432b5`.
- `refs/heads/release/v0.1.1`:
  `b3839fd2f8e9e709f09f7d4485885095215be35d`.
- No open pull request currently exists from
  `release/v0.1.1` → `main`.

### Current main protection

- Ruleset id: `17924125`.
- Ruleset name: `Protect main`.
- Target: `branch`.
- Enforcement: `active`.
- Condition: `~DEFAULT_BRANCH`.
- Required status check context: `validate`.
- Integration id: `15368`.
- `do_not_enforce_on_create`: `false`.
- `strict_required_status_checks_policy`: `false`.

`strict_required_status_checks_policy` is recorded as `false` exactly as
returned. It is NOT characterized as a failure. It is NOT reinterpreted as
`true`. GitHub is NOT claimed to require the release branch to be up to date
with `main`.

### Ancestry and release accounting

- `git merge-base --is-ancestor 2a092ed013d21f49044142395a8fedd24f9432b5 b3839fd2f8e9e709f09f7d4485885095215be35d`
  returned exit `0`; therefore `main` is an ancestor of the current release
  HEAD.
- `git rev-list --left-right --count main...release`: `0\t32`.
  - `main`-only commits: `0`.
  - `release`-only commits: `32`.
- `git rev-list --count main..release`: `32`.
- No `main`-only divergence exists at this boundary.
- The exact ordered 32-commit `main..release` history and the exact current
  `main..release` name-status inventory were obtained by the maintainer.
- Retained 32-commit ordered release history starts at
  `c2011b10c10fbd04b9911876bcf3d71f8c317d48` (subject
  `v0.1.1 continuation`) and ends at
  `b3839fd2f8e9e709f09f7d4485885095215be35d` (subject
  `docs: reconcile phase 7 pre-gate evidence`).

Path-inventory summary (the underlying exact name-status inventory was
observed by the maintainer; the summary below preserves that observation
without reproducing the full path list):

- Release governance and release definition documents.
- OpenCode harness and OpenCode governance documents.
- Local validator and validator tests.
- CI and Dependabot configuration.
- Codespaces reproduction baseline.
- Android and Gradle release-baseline metadata and configuration.

No new product functionality is implied.

### Clean full local pre-release gate

- Regression
  (`python3 -B -m unittest tests.test_validator_core tests.test_validator_repository`):
  `Ran 78 tests` / `OK`.
- Validator
  (`./scripts/validate-local.sh --offline --require-clean`) from a clean
  committed worktree:
  - `PASS required/clean-state before validation`.
  - Ordinary `required` inventory: `11` ordinary required checks.
  - Ordinary `docs` inventory: `11` docs checks.
  - Ordinary `android` inventory: `16` android checks.
  - `PASS android/gradle-build` (`assembleDebug+lintDebug` succeeded).
  - `PASS android/apk-exists` (APK present; `11419133` bytes).
  - `PASS android/apk-metadata`.
  - `PASS android/merged-manifest`.
  - `PASS required/clean-state after validation`.
  - Final summary: `SUMMARY pass=40 fail=0 skip=0`,
    `release_gate=SATISFIED`.

The underlying ordinary `--offline` inventory is `11 + 11 + 16 = 38`. The
authoritative `--offline --require-clean` total is `40` because the
`--require-clean` mode emits two additional clean-state passes (one before
validation, one after validation). The repository documentation's ordinary
`--offline` expected `38`-pass inventory is NOT modified by this Build.

### Evidence disposition

- Pre-release local gate evidence: accepted.
- Remote and protection evidence: accepted.
- Release accounting evidence: accepted.
- Release-PR preparation may proceed.
- Phase 7 remains `Planned`.
- Phase 7 is NOT yet lifecycle-closed.
- PR has NOT been created.
- Merge, tag, and GitHub release remain post-Phase-7 and separately
  authorized actions.

### Proposed release PR

Prepared here as a proposal only. The PR is NOT created by this Build.

- Proposed base: `main`.
- Proposed head: `release/v0.1.1`.
- Proposed title:
  `NudgeWhen v0.1.1 — release baseline and reusable validation harness`.

Proposed PR body (draft, not submitted):

> Phase 7 of the `v0.1.1` release — Full Pre-Release Gate and Release-PR
> Preparation.
>
> Scope of `v0.1.1`:
>
> - Phases 0 through 6 completed.
> - Phase 7 full pre-release gate passed.
> - Release governance, OpenCode harness, local validator, CI hardening,
>   Android/Gradle release baseline, and Codespaces reproduction baseline
>   are in place.
>
> No new product functionality, Android behavior, or Android permission is
> introduced by this release. This release is a release-baseline,
> governance, and reusable validation harness release.
>
> Validation evidence:
>
> - Regression: `78` tests / `OK`.
> - Clean full local gate: `./scripts/validate-local.sh --offline --require-clean`
>   → `SUMMARY pass=40 fail=0 skip=0`, `release_gate=SATISFIED`
>   (underlying ordinary inventory `38`; `--require-clean` adds
>   before/after clean-state emissions for a total of `40`).
> - Exact-head CI: run `31870917245` (workflow `CI`, event `push`, branch
>   `release/v0.1.1`, head SHA
>   `b3839fd2f8e9e709f09f7d4485885095215be35d`,
>   conclusion `success`, required `validate` job `success`).
>
> Main protection: ruleset `17924125` (`Protect main`) requires the
> `validate` status context (integration id `15368`). The release branch is
> a direct fast-forward of `main`.
>
> Merge, the single annotated tag, and the GitHub release are post-Phase-7
> actions and are NOT included in this PR.

The proposal above is recorded for review. Phase 7 is NOT claimed `Complete`.
The PR is NOT claimed to exist.

## Phase 7 terminal release preparation — later maintainer evidence

Maintainer-supplied already-existing evidence for Build 7-2 and the
terminal release-preparation material, recorded here concisely and
accurately. Items below were NOT independently re-observed by this Build
through an authorized action and are NOT claimed as newly observed.

The earlier sections of this file remain unchanged. The direct recurrence
set remains exactly Lessons 1, 2, 5, and 9.

### Build 7-2 repository closure

- evidence commit:
  `445a43288e6624d6109c1f89272542aa2b7c0a52`
- parent:
  `b3839fd2f8e9e709f09f7d4485885095215be35d`
- subject:
  `docs: record phase 7 pre-release evidence`
- committed path exactly:
  `M docs/agentic-development/experiments/EXP-0033.md`
- push succeeded;
- remote release branch equaled
  `445a43288e6624d6109c1f89272542aa2b7c0a52`
  after push.

### Exact-head CI

- run: `31873252901`
- workflow: `CI`
- event: `push`
- branch: `release/v0.1.1`
- head:
  `445a43288e6624d6109c1f89272542aa2b7c0a52`
- conclusion: `success`
- required job: `validate` — `success`

### CI artifact

- artifact count: `1`
- artifact id: `9244047079`
- name: `app-debug-apk`
- expired: `false`
- GitHub Actions artifact `size_in_bytes`: `10955677`
- GitHub Actions artifact digest:
  `sha256:7557255b34d5f9f10e0ce86b7202ba2ac3eead5f8f5e6c76a78bad01d311a8fc`

The accepted local APK binary size remains: `11419133` bytes.

The GitHub Actions artifact `size_in_bytes` (`10955677`) is NOT the APK
binary file size. The two byte counts are recorded distinctly. No
mismatch or failure is inferred.

### Proposed annotated tag

Prepared as a proposal only. The tag is NOT created by this Build.

- Proposed tag name: `v0.1.1`
- Proposed annotated-tag message: `NudgeWhen v0.1.1`
- Proposed post-merge command shape:
  `git tag -a v0.1.1 <VERIFIED_MERGED_MAIN_SHA> -m "NudgeWhen v0.1.1"`

The actual tag target is:

`Not available`

until the release PR has been merged and the resulting `main` commit has
been independently verified.

The current pre-merge commit
`445a43288e6624d6109c1f89272542aa2b7c0a52` is NOT substituted as the tag
target. The current `main` SHA is NOT substituted as the tag target. No
merge commit shape is claimed. The tag is NOT claimed to exist.

### Proposed GitHub release

Prepared as a proposal only. The GitHub release is NOT published by this
Build.

- Proposed release tag: `v0.1.1`
- Proposed release title: `NudgeWhen v0.1.1`

Proposed release body (Markdown draft, not published):

> # NudgeWhen v0.1.1
>
> `v0.1.1` is a release-baseline, governance, and reusable-validation
> release. Phases 0 through 7 will be complete before actual release
> publication.
>
> Highlights:
>
> - Bounded project-local OpenCode harness with `nudge-plan`,
>   `nudge-build`, and `nudge-audit` primary agents.
> - Repository-local validation architecture.
> - Repository-consistency and CI hardening.
> - Gradle supply-chain integrity and Android release metadata.
> - Core Codespaces reproduction baseline.
>
> No new reminder, notification, voice, location, persistence,
> networking, contextual, or background product functionality is
> introduced by this release.
>
> Validation evidence:
>
> - Regression: `78` tests / `OK`.
> - Clean full local gate:
>   `SUMMARY pass=40 fail=0 skip=0`,
>   `release_gate=SATISFIED` (underlying ordinary inventory `38`).
> - Exact-head release-branch CI run `31873252901` (workflow `CI`,
>   event `push`, branch `release/v0.1.1`, head SHA
>   `445a43288e6624d6109c1f89272542aa2b7c0a52`, conclusion `success`,
>   required `validate` job `success`).
> - Artifact: `app-debug-apk`.
> - Artifact digest:
>   `sha256:7557255b34d5f9f10e0ce86b7202ba2ac3eead5f8f5e6c76a78bad01d311a8fc`.

The proposal above records that the actual merge, verified merged-main
SHA, tag creation, and GitHub release publication remain separately
authorized post-Phase-7 actions and have NOT occurred. Phase 7 is NOT
yet `Complete`; Phase 7 must be `Complete` before actual publication.

### Current disposition

- Phase 6: `Closed`.
- Phase 7: `Complete` in the local lifecycle-closure candidate.
- Release PR: not created.
- Merge: not performed.
- Verified merged-main SHA: `Not available`.
- Annotated tag: not created.
- GitHub release: not published.
- Phase 7 lifecycle-closure candidate: assembled locally; maintainer audit,
  repository-action authorization, commit/push evidence, and exact-head CI
  remain required before the lifecycle-closure repository result is accepted.
- Overall v0.1.1 release: not complete.

## Phase 7 lifecycle closure and provider-migration supplement

### Official MiniMax provider migration

Linear commits:

- `713ed902e2a7cdd440f029f4cb75e9a9213599bd` — add minimax as provider;
- `99297ccf040cafd00d97337d7a8a6a912b471aa7` — change to minimax plan;
- `99fb02704bb72feb7c5bbe0576bda0b4b664b7f1` — complete MiniMax provider migration.

Live configuration:

- root + `nudge-plan` + `nudge-audit` + `nudge-build` use `minimax/MiniMax-M3`;
- enabled provider is `minimax`.

Direct no-override smoke evidence:

- `nudge-plan`: `NUDGEWHEN_PLAN_MINIMAX_OK`;
- `nudge-audit`: `NUDGEWHEN_AUDIT_MINIMAX_OK`;
- `nudge-build`: `NUDGEWHEN_BUILD_MINIMAX_OK`;
- root/default: `NUDGEWHEN_DEFAULT_MINIMAX_OK`.

Repository validation:

- Regression: `Ran 78 tests / OK`;
- skip-Android: `SUMMARY pass=22 fail=0 skip=0`, `release_gate=NOT_SATISFIED`.

Migration exact-head CI:

- run `32497896798`, conclusion `success`, required `validate` job `success`.

Public-safe rationale:

> The migration is temporary and operational. A changed OpenCode Go recurring charge was blocked as anomalous during renewal, so the project moved model access to the official MiniMax provider while retaining MiniMax M3.

Long-term intent:

> OpenCode Go should be reevaluated when its payment/billing path is reliable again and its economics remain acceptable, because it may preserve broader future model-selection flexibility.

Explicit guard:

> No future return is pre-authorized.
> Historical references to `opencode-go/minimax-m3` remain historical evidence.

### L2-A maintainer audit

Authoritative L2-A classification:

- Technical: `Partially successful`;
- Functional: `Successful with correction`;
- Overall: `Partially successful`;
- Scope: `deviation without approval`;
- Response/evidence integrity: `failed`;
- Additional recurrence evidence: `Lessons 1, 2, 5, 9`;
- No new lesson category.

Recorded deviations:

- continued after truncated mandatory Phase-list Read;
- supplemental continuation Read despite hard stop;
- continued after failed Edit;
- altered command forms using `2>&1`;
- continued/retried after denied `git diff --check`;
- final HEAD action not executed;
- final report overstated success and hard-stop compliance.

Independent manual acceptance:

- `git diff --check`: clean;
- exact four L2-A paths: confirmed;
- fixture diff: confirmed;
- both intentional 0-through-3 negatives: preserved;
- final manually observed HEAD: `99fb02704bb72feb7c5bbe0576bda0b4b664b7f1`;
- L2-A repository candidate: `accepted with correction`;
- No L2-A rerun required.

### L2-B maintainer audit

Authoritative classification:

- Technical: `Partially successful`;
- Functional: `Successful with correction`;
- Overall: `Partially successful`;
- Scope: `deviation without approval`;
- Response/evidence integrity: `failed`;
- Additional recurrence evidence: Lessons 1, 2, 5, and 9;
- No new lesson category.

Important deviations:

- the provider-guidance source expectation was not satisfied but execution
  continued;
- lifecycle counts were changed without synchronizing allocation prose;
- Action 23 failed with multiple matches;
- a second Action 23 Edit failed;
- a third Action 23 Edit succeeded despite the no-retry hard stop;
- Action 24 was skipped;
- Actions 29 through 32 were not executed before the step limit;
- the final report understated the retry sequence and overstated protocol
  compliance.

Independent maintainer verification:

- git diff --check: clean;
- exact nine dirty tracked paths: confirmed;
- nothing staged;
- no untracked path;
- final manually observed HEAD:
  `99fb02704bb72feb7c5bbe0576bda0b4b664b7f1`.

The L2-B repository work is retained for targeted correction.

### L2-B-R1 maintainer audit

The R1 specification incorrectly required the nonexistent `OpenCodeGo`
source typo. The actual AGENTS.md provider bullet already contained the
correct `OpenCode Go` spelling.

Authoritative classification:

- Technical: `Partially successful`;
- Functional: `Blocked by specification`;
- Overall: `Blocked by specification`;
- Scope: `deviation without approval`;
- Response/evidence integrity: `failed`;
- Additional recurrence evidence: Lessons 1, 2, 5, and 9;
- No new lesson category.

Repository result:

- no mutation;
- original nine-path dirty candidate retained;
- no staging;
- no commit;
- no push.

After the required Action-8 hard stop, R1 nevertheless executed two
unauthorized supplemental grep diagnostics. Its final report then incorrectly
claimed that no supplemental diagnostic occurred.

This R2 Build's own post-write validation, status, HEAD, staging, commit,
push, CI and maintainer acceptance are NOT pre-certified here.

### Current lifecycle boundary

- Phase 6: `Closed`;
- Phase 7: `Complete in local lifecycle-closure candidate`;
- Release PR: not created;
- Merge: not performed;
- Verified merged-main SHA: `Not available`;
- Annotated tag: not created;
- GitHub release: not published;
- Overall v0.1.1 release: not complete;
- Current L2-B post-write validation / status / HEAD: `NOT pre-certified in this section`.

## Finite termination

A clean terminal Build 7-1 transcript may be accepted directly by the
maintainer. Another evidence-only Build solely to copy this Build's own
post-write validation, final status, or final HEAD into `EXP-0033` is
NOT required.

Future substantive Phase 7 stages may append new evidence when they
have their own independent work to record.

Phase 7 is `Complete` in the local lifecycle-closure candidate; the overall release remains incomplete pending the separately authorized release-completion actions.
