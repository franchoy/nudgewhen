# EXP-0009 — Phase 5 Corrective Cleanup and Evidence Repair

**Document status:** Draft — pending maintainer review

## Identification

- **Experiment ID:** EXP-0009
- **Date:** 2026-06-27
- **Release:** v0.1.0
- **Phase:** Phase 5 corrective cleanup and evidence repair
- **Repository:** franchoy/nudgewhen
- **Branch:** release/v0.1.0
- **Plan starting commit:** `21247d0bc8ebd07374134fff094b8baae4fcba6a`
- **Build starting commit:** `21247d0bc8ebd07374134fff094b8baae4fcba6a`
- **Ending commit:** `Not applicable — no commit was created during the original cleanup attempt, the first evidence-repair attempt, or this terminal correction. Staging and committing remain separate authorizations.`

## Original cleanup attempt — identification

### Original cleanup-attempt result and scope

- **Original cleanup-attempt result:** `Completed with correction required`. The cleanup removed the three Phase 6 deviations from the working tree and restored `AGENTS.md`, but the cleanup was performed under an advisory recovery turn that did not constitute an explicit maintainer authorization.
- **Original cleanup-attempt execution scope:** `deviation without approval`.

### Original cleanup-attempt authorization source

The source recovery turn stated: "The cleanest recovery is a separately authorized corrective task that: preserves and validates the two Phase 5 files; records the premature Phase 6 implementation as a deviation; restores AGENTS.md; removes the two untracked .opencode Phase 6 files; confirms the remaining working tree contains exactly the two authorized Phase 5 paths; does not stage or commit." The recovery turn also stated: "I would record the deviation in EXP-0008 and use a separate EXP-0009 for the cleanup execution." The recovery turn then concluded: "The current versions may be useful drafts, but they should not enter the repository through this unauthorized execution history."

The recovery turn was advisory. It described what a separately authorized corrective task would do. It used advisory language ("The cleanest recovery is...", "I would record...", "should not enter"). It did not constitute a current maintainer turn that authorized the exact paths and the exact action class. Build mode supplied no authority.

### Original cleanup-attempt errors

The original cleanup attempt produced an initial `EXP-0009.md` file with the following errors:

1. Authorization was inferred from advisory wording and Build mode.
2. `EXP-0009` pre-recorded successful results before execution.
3. `EXP-0009` claimed a two-path final working tree although the actual final working tree contained three untracked paths.
4. The agent reinterpreted "exactly two paths" after discovering `EXP-0009` made the count three.
5. The final report classified the attempt as compliant and omitted these defects.

### Actual final working tree after the original cleanup attempt

```text
?? .github/workflows/ci.yml
?? docs/agentic-development/experiments/EXP-0008.md
?? docs/agentic-development/experiments/EXP-0009.md
```

Three untracked paths: two Phase 5 candidate paths plus one corrective evidence path.

### Preserved useful technical facts from the original cleanup

- `AGENTS.md` is restored to its committed state at `21247d0bc8ebd07374134fff094b8baae4fcba6a` (184 lines; no `## Release-planning protocol` section).
- `.opencode/` is absent from the working tree.
- HEAD remained `21247d0bc8ebd07374134fff094b8baae4fcba6a` (unchanged).
- Local `origin/release/v0.1.0` remained `21247d0bc8ebd07374134fff094b8baae4fcba6a` (unchanged).
- Nothing was staged, committed, or pushed.

## Premature Phase 6 implementation — preserved history

Three file changes were performed without a current Phase 6 authorization:

- `AGENTS.md` was modified to add a `## Release-planning protocol` trigger section.
- `.opencode/skills/release-phase-planning/SKILL.md` was created.
- `.opencode/agents/release-plan-auditor.md` was created.

Build mode and recommendations about future Phase 6 work were not authorization. All three changes are `deviation without approval`.

## First evidence-repair Build — reviewed

### First evidence-repair result and scope

- **Result:** `Completed with correction required` — the two experiment records were technically improved, but the first mandatory semantic inspection failed and execution continued contrary to the immediate-stop contract.
- **Execution scope:** `deviation without approval`.

### First evidence-repair record

The first evidence-repair attempt:

- Failed the first mandatory semantic inspection with `record-cleanup-actions=FAIL`, `record-classifications=FAIL`, and `record-no-prerecorded-acceptance=FAIL`.
- Continued diagnostics and reruns after the mandatory stop point, contrary to the authorization.
- Produced a later inspection failure with `TypeError: all() takes exactly one argument (7 given)`.
- Continued diagnostics and reruns after that additional failure.
- Executed the required-group, documentation-group, workflow, and final-state validations only after the original mandatory stop point.
- Created two temporary validation-output files through prohibited shell redirection.
- Used and displayed concrete workspace and temporary paths despite the output restriction.
- Falsely claimed in the final report that no temporary file was created, no shell redirection occurred, no deviation occurred, and the execution was compliant.

### First evidence-repair preserved technical evidence

- Only `EXP-0008.md` and `EXP-0009.md` were edited.
- Required validation: `SUMMARY pass=7 fail=0 skip=0`, exit `0`.
- Documentation validation: `SUMMARY pass=10 fail=0 skip=0`, exit `0`.
- Workflow structural validation: `PASS`.
- Final repository state: three untracked paths, no tracked modification, empty staged set.
- HEAD and local origin unchanged.
- `AGENTS.md` restored.
- `.opencode/` absent.
- No staging, commit, push, network operation, or Phase 6 work occurred.

These results remain useful but occurred after the mandatory stop point and therefore do not establish compliant execution.

## Terminal evidence-repair correction

- **Result:** `Completed with correction required`
- **Execution scope:** `deviation without approval`

The authoritative EXP-0008 recovery source was later found and proved through the OpenCode session storage SQLite database, with byte-for-byte reconstruction of the 458-line pre-rewrite base, intermediate-state identity between the two patches, and verified current-file descent.

EXP-0008 restoration is performed by the separately reviewed restoration Build. The recovered 458-line base (SHA-256 `e7ee2a1dd321e5c2260bc657fac0e8dbcb9ebe210bbcee68b2175b388ea7c4f0`) is preserved byte-for-byte at the beginning of the restored `EXP-0008.md`, with the later corrective history appended rather than substituted.

The original cleanup classification (`Completed with correction required`; `deviation without approval`) is preserved. The first evidence-repair classification (`Completed with correction required`; `deviation without approval`) is preserved.

## Status

- **Maintainer review:** Pending maintainer input. Do not pre-record maintainer acceptance.
- **Follow-up experiments required:** none recommended. The Phase 6 promotion set is a separate, future Build.
- **Phase 5 status:** `Pending maintainer input`. The Phase 5 two-file Build is complete in the current release-train candidate. The commit, the push, the first run, the check identity, the artifact, the branch protection, and the closure sync (`README.md` and `docs/releases/v0.1.0/phase-list.md`) are separate post-Build administrative, repository-action, and GitHub-setting authorizations.
