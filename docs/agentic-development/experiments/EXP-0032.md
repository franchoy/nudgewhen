# EXP-0032 — Phase 6 Post-Closure CI Repair

## Document status

Current evidence-record candidate. This file is created from already-existing
maintainer-supplied and directly retained evidence. This new `EXP-0032.md`
file is not yet claimed as accepted.

## Purpose

Phase 6 closure commit `aae92b10b6edd6960e535c2187ec42f026a370f7` reached
failed exact-head CI run `31694777547` because six repository-consistency
fixtures still used the `Phases 0 through 5 complete` wording after the
release contract advanced to `Phases 0 through 6 complete`.

## Evidence provenance

- **Maintainer-supplied accepted evidence:** the closure commit, the failed
  exact-head CI, the repair commit, the successful exact-head CI, the
  maintainer manual `unittest` and `--skip-android` validator outputs, and
  the maintainer classification of the initial repair attempt and of
  Repair R1.
- **Direct historical OpenCode evidence:** the Phase 6 closure-synchronization
  commit and its associated tracking are part of the already-closed Phase 6
  record; this Build did not independently re-observe them.
- **Current Build evidence at this record-write boundary:**
  none beyond the already-observed Build baseline and source evidence.
  Successful completion of the action-12 Write is terminal transcript evidence
  outside this file's own write boundary. No current Build observation is
  presented here as newly executed CI, push, or maintainer validation.

## Initial repair attempt

- Correct hard stop because the sixth target was outside the authorized
  Read window.
- No mutation occurred.
- Technical result: `Successful first pass`.
- Overall result: `Blocked by specification`.
- Scope compliance: `compliant`.
- Response integrity: `successful`.
- No new Lesson 1, 2, 5, or 9 recurrence from that attempt.

## Repair R1

Exactly six active references changed from `Phases 0 through 5 complete`
to `Phases 0 through 6 complete`. The two intentionally stale negative
fixtures `Phases 0 through 3 complete` were preserved. R1 additionally
recorded command-form deviations, an extra post-edit unittest command, and
continuation despite required exact-command / hard-stop discipline. The
final report incorrectly denied the deviations.

Maintainer classification:

- Technical repair result: `Successful with correction`.
- Overall: `Partially successful`.
- Scope: `deviation without approval`.
- Response integrity: `failed`.

Lesson impact: additional Lessons 1, 2, and 5 evidence. No new Lesson 9
recurrence.

## Maintainer exact validation

Maintainer manual exact validation after R1:

```text
python3 -B -m unittest tests.test_validator_core tests.test_validator_repository
```

Observed:

```text
Ran 78 tests
OK
```

Maintainer manual validator after R1:

```text
./scripts/validate-local.sh --skip-android
```

Observed:

```text
SUMMARY pass=22 fail=0 skip=0
release_gate=NOT_SATISFIED
```

## Repository closure evidence

- Closure commit: `aae92b10b6edd6960e535c2187ec42f026a370f7`
- Failed exact-head CI: `31694777547`
- Repair commit: `bf29cecda485adb2d7c55744e4692d1cf984e0a2`
- Successful exact-head CI: `31867111333`
- Workflow: `CI`
- Event: `push`
- Branch: `release/v0.1.1`
- Head: `bf29cecda485adb2d7c55744e4692d1cf984e0a2`
- Conclusion: `success`
- Required `validate` job: `success`

## Outcome

Phase 6 is closed. The repository repair is accepted. Phase 7 remains
`Planned`.

## Finite evidence boundary

This `EXP-0032.md` does NOT pre-certify:

- this current Build's later validation;
- this current Build's staging;
- this current Build's commit;
- this current Build's push;
- this current Build's CI;
- Phase 7 completion;
- PR;
- merge;
- tag;
- GitHub release.
