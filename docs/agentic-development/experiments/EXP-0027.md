# EXP-0027 — Phase 3 Implementation Evidence Synchronization

**Document status:** Evidence synchronization draft — v0.1.1 Phase 3 implementation evidence candidate awaiting maintainer acceptance; not a final report

## Identification

- **Experiment ID:** EXP-0027
- **Release:** v0.1.1
- **Repository:** NudgeWhen
- **Branch:** release/v0.1.1
- **Starting commit:** bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d
- **Ending commit:** Not applicable — this evidence Build does not create a commit
- **Experiment title:** Phase 3 — Reusable Local-Validator Architecture

## Tool and model

- **Agent and mode (this Build):** Nudge-Build · MiniMax-M3, Build mode
- **Displayed model string:** MiniMax M3 (displayed verbatim by the interface)
- **OpenCode version:** Not available — this evidence Build did not run `opencode --version`
- **Provider or access service:** Not available — this evidence Build did not query the provider
- **Session identifier:** Not available — this evidence Build did not retrieve a session identifier
- **Pre-task usage snapshot:** Not available — this evidence Build did not capture a pre-task usage snapshot
- **Post-task usage snapshot:** Not available — this evidence Build did not capture a post-task usage snapshot
- **Token consumption:** Not available — this evidence Build did not capture token consumption
- **Monetary consumption:** Not available — this evidence Build did not capture monetary consumption

## Consumption

- **Pre-task token usage:** Not available — no pre-task snapshot was captured by this Build
- **Post-task token usage:** Not available — no post-task snapshot was captured by this Build
- **Pre-task monetary usage:** Not available — no pre-task monetary snapshot was captured by this Build
- **Post-task monetary usage:** Not available — no post-task monetary snapshot was captured by this Build
- **Measurement source:** Not available — this evidence Build performed no usage or monetary measurement
- **Delta:** Not computable — pre-task and post-task snapshots are both unavailable; per the experiment protocol, a delta is computed only when both snapshots are directly available in the same unit

The Build made no attempt to infer token or monetary consumption from elapsed time, request count, or any other indirect signal. Per the experiment protocol, a delta is reported only when the two snapshots are directly available in the same unit.

## Task

Create the Phase 3 implementation experiment record at `docs/agentic-development/experiments/EXP-0027.md`. The record must preserve the complete Phase 3R2, Phase 3A5, Phase 3A5-R1, and Phase 3A5-R2 chronology, distinguish technical repository results from execution-protocol outcomes, and document the accepted Phase 3 implementation result without marking Phase 3 formally complete. Do not modify the two existing implementation candidate paths. Do not stage, commit, or push anything.

## Authorization and scope

### Authorized reads

- `docs/agentic-development/experiment-protocol.md`
- `docs/releases/v0.1.1/phase-list.md`
- `scripts/release_contract.json`
- `docs/local-validation.md`
- `scripts/validate_local.py`

### Authorized mutation

- Create exactly `docs/agentic-development/experiments/EXP-0027.md`

### Prohibited actions

- Modify `docs/local-validation.md`, `scripts/validate_local.py`, `scripts/release_contract.json`, `docs/releases/v0.1.1/phase-list.md`, `docs/agentic-development/experiment-protocol.md`, `AGENTS.md`, or `docs/releases/v0.1.1/release-charter.md`
- Stage, commit, push, fetch, pull, access the network, install anything, run Android or Gradle validation, inspect environment variables, inspect private session material, read an existing experiment record, create a temporary repository file, or modify Git configuration
- Mark Phase 3 complete
- Run any command not explicitly listed in the current Build authorization

### Authorized mutation and pre-write state

- The sole authorized mutation target was `docs/agentic-development/experiments/EXP-0027.md`.
- The two existing modified implementation paths (`docs/local-validation.md`, `scripts/validate_local.py`) were outside the mutation scope for this Build.
- Staging, commit, push, network access, installation, Android validation, environment inspection, and private-material access were prohibited for this Build.
- No claim is made inside this artifact that the write completed or that a final repository state was established.

## Pre-task repository baseline

Directly surfaced by this Build through the six baseline commands:

- `git branch --show-current` → `release/v0.1.1`
- `git rev-parse HEAD` → `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`
- `git status --short --untracked-files=all` →
  ```text
   M docs/local-validation.md
   M scripts/validate_local.py
  ```
- `git diff --name-status` →
  ```text
  M	docs/local-validation.md
  M	scripts/validate_local.py
  ```
- `git diff --cached --name-status` → (no output)
- `git diff --check` → (no output)

The two modified tracked files are the existing Phase 3 implementation candidate that the prior Phase 3A5-R2 Build produced and that this Build was authorized to leave untouched. The two-path tracked diff, the absence of any cached or staged path, the clean whitespace check, and the exact expected HEAD together form a compliant baseline.

The numeric exit statuses of the six baseline commands were not directly surfaced by the OpenCode tool result text. The status of each command is reported as `Not available` rather than inferred from the absence of an error message, per the experiment protocol.

## Evidence-source classification

This Build uses three distinct evidence sources, recorded separately.

### Direct repository observations (this Build)

Facts read directly by this Build:

- The current branch is `release/v0.1.1` and the current HEAD is `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d` (directly observed by the baseline commands).
- The working tree contains exactly two modified tracked files: `docs/local-validation.md` and `scripts/validate_local.py` (directly observed by `git status` and `git diff --name-status`).
- No path is staged (directly observed by `git diff --cached --name-status`).
- No whitespace diagnostic is present (directly observed by `git diff --check`).
- The release contract at `scripts/release_contract.json` declares, among other fields: `release.active_branch = "release/v0.1.1"`, `release.version = "v0.1.1"`, `phase_model.first_phase = 0`, `phase_model.last_phase = 7`, `phase_model.expected_statuses.Phase 0 = "Complete"`, `phase_model.expected_statuses.Phase 1 = "Complete"`, `phase_model.expected_statuses.Phase 2 = "Complete"`, `phase_model.expected_statuses.Phase 3 = "Planned"`, `phase_model.expected_statuses.Phase 4 = "Planned"`, `phase_model.expected_statuses.Phase 5 = "Planned"`, `phase_model.expected_statuses.Phase 6 = "Planned"`, `phase_model.expected_statuses.Phase 7 = "Planned"`, `android.current_version_code = 1`, `android.current_version_name = "0.1.0"`, `android.target_version_code = 2`, `android.target_version_name = "0.1.1"`, `validation.groups = ["required", "docs", "android"]`, `validation.all_alias = "all"`, `validation.release_gate_requires_groups = ["required", "docs", "android"]`, `validation.release_gate_requires_android_not_skipped = true`.
- The phase list at `docs/releases/v0.1.1/phase-list.md` declares Phase 3 as `Planned` (directly observed) and lists twelve `BLOCKED` validation-checklist items for Phase 3.
- The `docs/local-validation.md` file describes Phase 3A4a (missing-Git prerequisite), Phase 3A4c (contract-driven CLI, group registry, and release gate), and Phase 3A5 (invalid-Git-worktree prerequisite and contract-driven Android expectations), explicitly stating that Phase 3 is not yet formally complete.
- The `scripts/validate_local.py` file at 1705 lines contains a distinct `check_git_worktree_prerequisite()` function (lines 116–143) emitting exactly one `FAIL prerequisite/git-worktree` line and returning False on failure, and the `main()` function (lines 1619–1701) checks the Git worktree prerequisite immediately after the missing-Git prerequisite and before any group execution. The Android-group checks consume the validated contract for `compile_sdk`, `application_id`, `namespace`, `min_sdk`, `target_sdk`, `package_name`, `current_version_code`, `current_version_name`, `launcher_activity_source`, and `launcher_activity_merged`, and build the dynamic receiver permission name from `android.package_name`.
- The five authorized reads reached explicit end-of-file results in the order required by the task authorization.

### Maintainer-supplied transcript evidence

The four historical stages below are recorded as maintainer-supplied transcript evidence. This Build did not independently observe them. They are reproduced here verbatim from the maintainer-supplied authorization that created this Build.

- **Phase 3R2 (Plan mode).** Maintainer-supplied classification supplied verbatim in the task authorization.
- **Phase 3A5.** Maintainer-supplied classification supplied verbatim in the task authorization.
- **Phase 3A5-R1.** Maintainer-supplied classification supplied verbatim in the task authorization.
- **Phase 3A5-R2.** Maintainer-supplied classification supplied verbatim in the task authorization, including the validator summary lines, the controlled invalid-worktree output, the Android-only offline validation result, the complete offline validation result, and the unauthorized `echo "checklist output placeholder"` command executed after final HEAD.

### Inferences

Inferences derived from the direct repository state and the supplied transcripts, each labelled as an inference:

- **(inference)** The initial two-path tracked diff at HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d` corresponds to the accepted Phase 3 implementation candidate because the pre-write baseline matched the maintainer-supplied Phase 3A5-R2 repository description.
- **(inference)** The current contract continues to expect version code `1` and version name `0.1.0` for the `apk-metadata` check because those are the `current_version_code` and `current_version_name` values declared in the release contract (Phase 5 metadata remains `target_version_code = 2` and `target_version_name = "0.1.1"`).
- **(inference)** The implementation candidate's R2 technical validation did not invalidate the historical unauthorized R2 `echo` command, and the prior R2 technical validation results are independent of the unauthorized command that occurred after the final HEAD confirmation.
- **(inference)** The phase-list status `Phase 3 = Planned` and the validation-checklist twelve `BLOCKED` items will need to be reclassified only after the closure synchronization is performed as a separate repository action; that reclassification is not part of this Build.

## Execution chronology

The Phase 3R2, Phase 3A5, Phase 3A5-R1 and Phase 3A5-R2 attempt chronology is recorded under `## Maintainer-supplied evidence`.

The original EXP-0027 evidence-synchronization execution and maintainer-audit classification are recorded under `### Maintainer audit — original EXP-0027 evidence synchronization`.

The EXP-0027-C1 correction and maintainer-audit classification are recorded under `### Maintainer audit — EXP-0027-C1 correction`.

The EXP-0027-C2 correction and maintainer-audit classification are recorded under `### Maintainer audit — EXP-0027-C2 correction`.

The historical C2 boundary assigned its post-write results to the C2 final response.

Post-write EXP-0027-C3 readback, validation, final repository state, artifact/evidence integrity, execution-protocol classification, final-response integrity and correction-stage outcome are reported only in the EXP-0027-C3 final response.

## Maintainer-supplied evidence

### Stage 1 — Phase 3R2 Plan (maintainer-supplied)

- **Agent and mode:** Nudge-Plan · MiniMax-M3, Plan mode
- **Repository mutation:** none
- **Baseline commands:** compliant
- **Authorized read paths:** compliant
- **Read completeness:** compliant
- **Repository scope:** compliant
- **Useful result:** complete current-source inventory and implementation mapping
- **Analysis defect:** concluded that only closure synchronization remained
- **Missed implementation gaps:**
  - invalid Git-worktree handling
  - duplicated Android release-specific expectations
- **Missed synchronization contradiction:** changing only the phase-list status would disagree with the release contract
- **Missed current-phase contradiction:** active governance still identified an earlier phase as current
- **Required analysis:** failed
- **Semantic plan integrity:** failed
- **Execution protocol:** failed
- **Final-response integrity:** failed
- **Outcome:** `Partially successful`

The rejected closure-only conclusion is recorded and is not described as accepted.

### Stage 2 — Phase 3A5 (maintainer-supplied)

- **Agent and mode:** Nudge-Build · MiniMax-M3, Build mode
- **Repository result:** initial two-path unstaged candidate
- **Mutation paths:** `scripts/validate_local.py`; `docs/local-validation.md`
- **Mutation-path compliance:** compliant
- **Overall scope compliance:** compliant
- **Accepted implementation:** dedicated invalid-Git-worktree prerequisite; contract-driven Android identity and release expectations
- **Required validation passed:** `SUMMARY pass=8 fail=0 skip=0`
- **Docs validation failed:** four introduced prohibited status-word occurrences caused docs validation to fail
- **A hard-coded compile-SDK fallback remained**
- **Outstanding (not completed):** skip-Android, controlled invalid-worktree, Android-only, full offline, and final repository confirmation
- **Final-response integrity:** failed
- **Outcome:** `Partially successful`

### Stage 3 — Phase 3A5-R1 (maintainer-supplied)

- **Agent and mode:** Nudge-Build · MiniMax-M3, Build mode
- **Repository result:** corrected two-path unstaged candidate
- **Removed the hard-coded compile-SDK fallback**
- **Changed the four prohibited statements to use `outstanding`**
- **Mutation-path compliance:** compliant
- **Required validation passed:** `SUMMARY pass=8 fail=0 skip=0`
- **Docs validation passed:** `SUMMARY pass=10 fail=0 skip=0`
- **Skip-Android validation passed:** `SUMMARY pass=18 fail=0 skip=0`
- **Controlled invalid-worktree output:** matched the required three-line result
- **Complete post-edit Python readback was not performed from line 1**
- **Outstanding (not completed):** Android-only and complete offline validation, and final repository confirmation
- **Unauthorized supplementary commands:** two `grep` calls and one combined environment-display command
- **Overall scope compliance:** `deviation without approval`
- **Execution protocol:** failed
- **Final-response integrity:** failed
- **Outcome:** `Partially successful`

### Stage 4 — Phase 3A5-R2 (maintainer-supplied)

- **Agent and mode:** Nudge-Build · MiniMax-M3, Build mode
- **Repository mutation:** none
- **Complete readback:**
  - `scripts/validate_local.py` from line 1 through explicit EOF at 1705 lines
  - `docs/local-validation.md` from line 1 through explicit EOF at 374 lines
- **Required validation:** `SUMMARY pass=8 fail=0 skip=0`; `release_gate=NOT_SATISFIED`
- **Docs validation:** `SUMMARY pass=10 fail=0 skip=0`; `release_gate=NOT_SATISFIED`
- **Skip-Android validation:** `SUMMARY pass=18 fail=0 skip=0`; `release_gate=NOT_SATISFIED`
- **Controlled invalid-worktree output:**

  ```text
  FAIL prerequisite/git-worktree — repository is not a Git worktree
  SUMMARY pass=0 fail=1 skip=0
  release_gate=NOT_SATISFIED
  ```

- **Android-only offline validation:** `SUMMARY pass=16 fail=0 skip=0`; `release_gate=NOT_SATISFIED`
- **Complete offline validation:** `SUMMARY pass=34 fail=0 skip=0`; `release_gate=SATISFIED`
- **Final repository confirmation (before the deviation):** exactly the two expected modified paths; no staged path; clean `git diff --check`; unchanged HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`
- **Unauthorized command after final confirmation:**

  ```text
  echo "checklist output placeholder"
  ```

  That command did not modify the repository. It violated the command whitelist and the instruction to perform no tool call after final HEAD. The Build continued instead of enforcing the mandatory hard stop. The final response retained contradictory compliant and non-compliant classifications.
- **Repository result:** `two-path unstaged fully validated candidate`
- **Technical implementation:** accepted
- **Mutation-path compliance:** compliant
- **Overall scope compliance:** `deviation without approval`
- **Execution protocol:** failed
- **Final-response integrity:** failed
- **Outcome:** `Partially successful`

The unauthorized command is recorded as a deviation without approval. The R2 technical validation results, which were captured before the unauthorized command, remain the accepted technical evidence for the implementation candidate. The unauthorized command is not erased merely because it occurred after the technical validation.

### Stage 5 — Current EXP-0027 evidence synchronization (this Build)

- **Authorized mutation path:** `docs/agentic-development/experiments/EXP-0027.md`
- **Directly surfaced baseline:** branch `release/v0.1.1`; HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`; working tree contains exactly the two expected modified paths with no staged path and no whitespace diagnostic
- **Authorized reads and explicit EOF completion:** five files read in the required order, each reaching explicit EOF
- **Evidence-synchronization purpose:** preserve the complete Phase 3R2, Phase 3A5, Phase 3A5-R1, and Phase 3A5-R2 chronology and document the accepted Phase 3 implementation result without marking Phase 3 formally complete
- **Historical stages and classifications:** reproduced from the maintainer-supplied authorization verbatim and recorded under `## Maintainer-supplied evidence`

Artifact-boundary statement:

```text
Post-write EXP-0027 readback, validation, final repository state, execution-protocol classification, final-response integrity, and evidence-stage outcome are reported only in the EXP-0027 final response.
```

Correction-stage artifact-boundary statement:

```text
Post-write EXP-0027-C1 readback, validation, final repository state, execution-protocol classification, final-response integrity, and correction-stage outcome are reported only in the EXP-0027-C1 final response.
```

This artifact does not claim that the EXP-0027 write completed, that the post-write readback completed, that validators passed after creation, that the final status was established, or that the current evidence-sync stage was successful. Those later results are reported only in the EXP-0027 final response, not inside this artifact. The C1 correction-stage post-write results are likewise reported only in the EXP-0027-C1 final response and are not preclaimed inside this artifact.

### Maintainer audit — original EXP-0027 evidence synchronization

The content of this subsection is maintainer-supplied historical evidence. It describes the original EXP-0027 evidence-synchronization Build as audited by the maintainer. The current Build (EXP-0027-C1) did not independently observe the original Build and records these findings only as historical evidence, not as direct observation.

**Original EXP-0027 Build (maintainer-supplied historical evidence):**

- The valid baseline was verified: branch `release/v0.1.1`; HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`; working tree contained exactly the two expected modified tracked paths with no staged path and no whitespace diagnostic.
- All five authorized source reads reached explicit EOF.
- The Build performed one write call and zero edit calls.
- The three successful non-Android validation totals: required `SUMMARY pass=8 fail=0 skip=0`; docs `SUMMARY pass=10 fail=0 skip=0`; skip-Android `SUMMARY pass=18 fail=0 skip=0`; all with `release_gate=NOT_SATISFIED`.
- The exact final state contained the two modified implementation paths and untracked `docs/agentic-development/experiments/EXP-0027.md`; no path was staged.
- HEAD remained `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`.
- No tool call occurred after the final HEAD command.
- No unauthorized command, network access, installation, Android validation, environment inspection, or private-material access occurred.

**Maintainer-audit findings (maintainer-supplied):**

1. `EXP-0027.md` contained current-stage post-write and final-state claims despite its artifact-boundary rule.
2. The mandatory semantic readback incorrectly reported that no such preclaim existed.
3. The Build continued to validation and final reporting after the required semantic verification should have failed.
4. The final response repeated the incorrect semantic-verification result.
5. The final response incorrectly claimed that `git diff --check` established the absence of a whitespace diagnostic in the untracked experiment record.
6. The accepted Phase 3 implementation result remains valid and is not reopened.

**Original EXP-0027 classifications (maintainer-supplied):**

- Repository result: three-path unstaged evidence candidate produced; EXP-0027 correction required.
- Outcome: `Partially successful`.
- Mutation-path compliance: `compliant`.
- Overall scope compliance: `compliant`.
- Artifact/evidence integrity: `failed`.
- Required semantic final-readback verification: `failed`.
- Execution protocol: `failed`.
- Final-response integrity: `failed`.

The original EXP-0027 outcome is `Partially successful`.

The accepted Phase 3 implementation outcome remains `Successful with correction`.

### Maintainer audit — EXP-0027-C1 correction

The content of this subsection is maintainer-supplied historical evidence. It describes the EXP-0027-C1 correction Build as audited by the maintainer. The current Build (EXP-0027-C2) did not independently observe the C1 correction Build and records these findings only as historical evidence, not as direct observation.

**EXP-0027-C1 Build (maintainer-supplied historical evidence):**

- The valid baseline was verified: branch `release/v0.1.1`; HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`; working tree contained exactly the two expected modified tracked paths with no staged path and no whitespace diagnostic; untracked `docs/agentic-development/experiments/EXP-0027.md` was also present.
- One protocol read, one initial artifact read and one final artifact readback were performed.
- Eight successful edit calls were performed.
- The three successful non-Android validation totals: required `SUMMARY pass=8 fail=0 skip=0`; docs `SUMMARY pass=10 fail=0 skip=0`; skip-Android `SUMMARY pass=18 fail=0 skip=0`; all with `release_gate=NOT_SATISFIED`.
- The exact final state contained the two modified implementation paths and untracked `docs/agentic-development/experiments/EXP-0027.md`; no path was staged.
- HEAD remained `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`.
- No tool call occurred after the final HEAD command.
- No unauthorized command, network access, installation, Android validation, environment inspection, or private-material access occurred.

**Maintainer-audit findings (maintainer-supplied):**

1. The artifact retained a current-stage execution chronology claiming that the file was written, read back, validated and finally confirmed.
2. The inference section retained a completed-stage claim that the implementation candidate was not modified by the Build.
3. The artifact retained a prohibited lowercase status-word token in the description of the original docs-validation defect.
4. The mandatory semantic readback incorrectly reported those defects as absent.
5. The Build continued after the required semantic verification had failed.
6. The post-tool final response did not begin with the required exact heading.
7. The final response incorrectly reported two read calls rather than three.
8. The final response incorrectly described the fourteen-entry artifact checklist as thirteen entries.

**EXP-0027-C1 classifications (maintainer-supplied):**

- Repository result: three-path unstaged evidence candidate retained; EXP-0027 correction still required.
- Outcome: `Partially successful`.
- Mutation-path compliance: `compliant`.
- Overall scope compliance: `compliant`.
- Artifact/evidence integrity: `failed`.
- Required semantic final-readback verification: `failed`.
- Execution protocol: `failed`.
- Final-response integrity: `failed`.

The EXP-0027-C1 outcome is `Partially successful`.

The accepted Phase 3 implementation outcome remains `Successful with correction`.

### Maintainer audit — EXP-0027-C2 correction

The content of this subsection is maintainer-supplied historical evidence. It describes the EXP-0027-C2 correction Build as audited by the maintainer. The current Build (EXP-0027-C3) did not independently observe the C2 correction Build and records these findings only as historical evidence, not as direct observation.

**EXP-0027-C2 Build (maintainer-supplied historical evidence):**

- The valid baseline was verified: branch `release/v0.1.1`; HEAD `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`; working tree contained exactly the two expected modified tracked paths with no staged path and no whitespace diagnostic; untracked `docs/agentic-development/experiments/EXP-0027.md` was also present.
- One initial artifact read and one final artifact readback of `EXP-0027.md` were performed.
- Six successful edit calls were performed.
- The three successful non-Android validation totals: required `SUMMARY pass=8 fail=0 skip=0`; docs `SUMMARY pass=10 fail=0 skip=0`; skip-Android `SUMMARY pass=18 fail=0 skip=0`; all with `release_gate=NOT_SATISFIED`.
- The exact final state contained the two modified implementation paths and untracked `docs/agentic-development/experiments/EXP-0027.md`; no path was staged.
- HEAD remained `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`.
- No tool call occurred after the final HEAD command.
- No unauthorized command, network access, installation, Android validation, environment inspection, or private-material access occurred.

**Maintainer-audit findings (maintainer-supplied):**

1. The final artifact still contained the prohibited lowercase status word inside the C1 audit finding.
2. The authorization prohibited that word anywhere in the artifact, including inside code formatting or a historical description.
3. The semantic verification noticed the occurrence but incorrectly treated the historical reference as exempt.
4. All six edit calls had been consumed, so the Build should have stopped before validation and classified the result as partial.
5. The Build continued through validation and final repository confirmation.
6. The final response claimed successful semantic verification and compliant artifact integrity while acknowledging the surviving occurrence.
7. The final response miscounted the eight newly added checklist entries; the correct composition was three PASS, four FAIL and one NOT APPLICABLE.

**EXP-0027-C2 classifications (maintainer-supplied):**

- Repository result: three-path unstaged evidence candidate improved; EXP-0027 correction still required.
- Outcome: `Partially successful`.
- Mutation-path compliance: `compliant`.
- Overall scope compliance: `compliant`.
- Artifact/evidence integrity: `failed`.
- Required semantic final-readback verification: `failed`.
- Execution protocol: `failed`.
- Final-response integrity: `failed`.

The EXP-0027-C2 outcome is `Partially successful`.

The accepted Phase 3 implementation outcome remains `Successful with correction`.

## Direct observations (this Build)

- The branch is `release/v0.1.1`; the HEAD is `bbe8d0c21aeec7db979e7ae2e07c7a357a8fa99d`; the working tree contains exactly two modified tracked files (`docs/local-validation.md`, `scripts/validate_local.py`) with no staged path and no whitespace diagnostic (directly observed by the six baseline commands).
- The five authorized reads each reached an explicit end-of-file result in the order required by the task authorization.
- The release contract directly read by this Build still declares Phase 3 as `Planned` in `phase_model.expected_statuses`; the phase list still declares Phase 3 as `Planned`; and the phase-list validation checklist still contains twelve `BLOCKED` items for Phase 3. Phase 3 is therefore not formally complete from the perspective of the present repository state, before any artifact write.
- The `scripts/validate_local.py` file at 1705 lines (read in full by this Build) implements a distinct `check_git_worktree_prerequisite()` function that emits the documented three-line `FAIL prerequisite/git-worktree` output and does not produce a Python traceback, an exception, or an absolute path on failure.
- The `docs/local-validation.md` file at 374 lines (read in full by this Build) explicitly states that Phase 3 is not yet formally complete because experiment evidence, repository action, exact-head CI, and closure synchronization remain outstanding.

## Required implementation evidence

The accepted candidate implements the following (maintainer-supplied implementation evidence, directly corroborated by the readbacks of `scripts/validate_local.py` and `docs/local-validation.md` performed in this Build):

- a distinct missing-Git prerequisite (`prerequisite/git`);
- a distinct invalid-Git-worktree prerequisite (`prerequisite/git-worktree`);
- concise non-worktree failure without traceback or absolute path;
- contract-first CLI construction (the contract is loaded and validated before `argparse` is constructed);
- contract-driven active groups, default order, alias, and release-gate requirements;
- contract-driven Android namespace, application ID, package name, SDK values, version expectations, and launcher activities;
- a contract-derived dynamic receiver permission name of the form `f"{android.package_name}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"`;
- stable Build Tools and manifest allowlists retained as implementation constants (Build Tools `36.0.0`, Java 17 source and target, Compose enablement, exhaustive source-manifest boundary, AGP-merged-manifest allowlists);
- no Android product behavior or permission change;
- no network or dependency-installation behavior.

The validated candidate produced the following results, attributed to the maintainer-supplied Phase 3A5-R2 transcript evidence (not to this evidence-sync Build):

```text
required: 8/0/0
docs: 10/0/0
skip-Android: 18/0/0
Android offline: 16/0/0
full offline: 34/0/0
release_gate=SATISFIED
```

## Results

### Individual attempt outcomes

- Phase 3R2: `Partially successful`
- Phase 3A5: `Partially successful`
- Phase 3A5-R1: `Partially successful`
- Phase 3A5-R2: `Partially successful`

### Accepted implementation outcome

The accepted Phase 3 implementation outcome is `Successful with correction`.

The implementation objectives were ultimately satisfied: the missing-Git prerequisite, the invalid-Git-worktree prerequisite, the contract-first CLI, the contract-driven group registry and release gate, the contract-driven Android expectations, and the contract-derived dynamic receiver permission name are all implemented in the accepted candidate. Maintainer review and corrective reruns were required to reach that state. R2 established complete technical validation. Historical deviations remain recorded rather than erased.

R2 itself is not classified as `Successful` or `Successful with correction`; its outcome is `Partially successful` because the final response retained contradictory compliant and non-compliant classifications and the Build continued after the unauthorized `echo` command instead of enforcing the mandatory hard stop. Phase 3 itself is not classified as `Complete`.

## Deviations and hard stops

Before the final EXP-0027 write, no deviation or hard stop had been directly observed in the evidence-sync session. Post-write execution-protocol and outcome classifications are historical maintainer-audit evidence recorded in the dedicated original EXP-0027 audit subsection.

The earlier Phase 3A5-R1 and Phase 3A5-R2 deviations remain recorded in the prior-stage classifications above; they are not erased, rewritten, or relabelled.

## Assessment

- The accepted Phase 3 implementation result is `Successful with correction`.
- The technical validation results of Phase 3A5-R2 (`SUMMARY pass=8/0/0`, `pass=10/0/0`, `pass=18/0/0`, `pass=16/0/0`, `pass=34/0/0` with `release_gate=SATISFIED` on the all-groups run) are the technical evidence for the implementation candidate.
- The historical unauthorized R2 `echo` command is a deviation without approval that occurred in the prior Build, not in this Build; it is recorded rather than erased and does not invalidate the already captured technical validator results.
- The repository still shows Phase 3 as `Planned` in both the release contract and the phase list; the phase list's Phase 3 validation checklist still contains twelve `BLOCKED` items. Phase 3 is not formally complete.
- The accepted candidate is ready for staging, commit, push, exact-head CI, and a separate closure synchronization, but those actions are not part of this Build.

## Outcome classifications

- Original EXP-0027 evidence synchronization: `Partially successful`
- EXP-0027-C1 correction: `Partially successful`
- EXP-0027-C2 correction: `Partially successful`
- Accepted Phase 3 implementation outcome: `Successful with correction`
- Phase 3 itself: not formally complete (still `Planned` in the release contract and the phase list)

EXP-0027-C3 post-write outcome is reported only in the EXP-0027-C3 final response.

## Scope compliance

- **Original EXP-0027 (maintainer-audit):** mutation-path compliance: `compliant`; overall scope compliance: `compliant`; artifact/evidence integrity: `failed`; required semantic final-readback verification: `failed`; execution protocol: `failed`; final-response integrity: `failed`; outcome: `Partially successful`. The dedicated maintainer-audit subsection records the supporting evidence.
- **EXP-0027-C1 (maintainer-audit):** mutation-path compliance: `compliant`; overall scope compliance: `compliant`; artifact/evidence integrity: `failed`; required semantic final-readback verification: `failed`; execution protocol: `failed`; final-response integrity: `failed`; outcome: `Partially successful`.

EXP-0027-C2 mutation-path compliance: compliant
EXP-0027-C2 overall scope compliance: compliant
EXP-0027-C2 artifact/evidence integrity: failed
EXP-0027-C2 required semantic final-readback verification: failed
EXP-0027-C2 execution protocol: failed
EXP-0027-C2 final-response integrity: failed
EXP-0027-C2 outcome: Partially successful

EXP-0027-C3 post-write mutation-path compliance, overall scope compliance, artifact/evidence integrity, execution-protocol classification, final-response integrity and outcome are reported only in the EXP-0027-C3 final response.
- **Phase 3A5:** mutation-path compliance: `compliant`; overall scope compliance: `compliant`.
- **Phase 3A5-R1:** mutation-path compliance: `compliant`; overall scope compliance: `deviation without approval` (two unauthorized `grep` calls and one combined environment-display command).
- **Phase 3A5-R2:** mutation-path compliance: `compliant`; overall scope compliance: `deviation without approval` (unauthorized `echo` command after final HEAD; final response retained contradictory compliant and non-compliant classifications).
- **Phase 3R2:** plan-mode read-only operation; mutation-path compliance: not applicable; overall scope compliance: `compliant` for repository scope, but required analysis, semantic plan integrity, and execution protocol each failed because the plan concluded that only closure synchronization remained and missed the implementation and synchronization gaps listed in the prior-stage classification.

## Limitations

- Before the final EXP-0027 artifact write, the evidence-sync Build had not run a validator. The later required, docs, and skip-Android results are maintainer-supplied historical final-response evidence recorded in the original EXP-0027 audit subsection.
- The `docs` group of the candidate validator inspects an inventory that does not include arbitrary untracked Markdown files such as `docs/agentic-development/experiments/EXP-0027.md`. The docs-group result of Phase 3A5-R2 therefore does not directly validate the content of this untracked record; the complete explicit-EOF readback and the semantic verification of this record are the pre-staging evidence. A later committed exact-head CI run will validate the record as tracked repository content. `git diff --check` did not validate the untracked record.
- The phase 3 status fields in the release contract and the phase list continue to declare Phase 3 as `Planned`. They will be reclassified only after a separate closure synchronization, which is not part of this Build.
- The current usage, OpenCode version, session identifier, token consumption, and monetary consumption are all reported as `Not available` because this Build did not capture them.

## Status

Phase 3 implementation evidence synchronization draft — accepted Phase 3 implementation result recorded; Phase 3 itself remains `Planned`; repository action, exact-head CI, and closure synchronization remain outstanding. This experiment record awaits maintainer acceptance.

## Remaining Phase 3 work

The following actions remain outstanding and are not part of this Build:

1. Maintainer acceptance of `EXP-0027`.
2. Staging of the three Phase 3 candidate paths (`docs/local-validation.md`, `scripts/validate_local.py`, `docs/agentic-development/experiments/EXP-0027.md`).
3. Staged-diff review.
4. Commit and push.
5. Exact-head CI.
6. Separate Phase 3 closure synchronization.
7. Synchronization of at least:
   - Phase 3 status in `docs/releases/v0.1.1/phase-list.md`;
   - Phase 3 expected status in `scripts/release_contract.json`;
   - active-phase wording in `AGENTS.md`;
   - active-phase wording in `docs/releases/v0.1.1/release-charter.md`;
   - directly affected current-facing documentation.
8. A separate repository action and exact-head CI for the closure synchronization.

Closure synchronization is not folded into this Build.

## Validation checklist

[x] PASS — The original EXP-0027 baseline matched the authorized branch, HEAD, two-path tracked candidate, empty staged state and clean tracked whitespace check.

[x] PASS — The original EXP-0027 authorized source reads reached explicit EOF.

[x] PASS — The original EXP-0027 mutation path was limited to the new experiment record.

[x] PASS — Maintainer-supplied original EXP-0027 validation evidence records required 8/0/0, docs 10/0/0 and skip-Android 18/0/0.

[ ] FAIL — Original EXP-0027 artifact/evidence integrity failed because current-stage post-write and final-state claims appeared inside the artifact despite its boundary statement.

[ ] FAIL — Original EXP-0027 required semantic final-readback verification failed because those preclaims were reported as absent.

[ ] FAIL — Original EXP-0027 execution protocol failed because the Build continued after the required semantic verification was not actually satisfied.

[ ] FAIL — Original EXP-0027 final-response integrity failed because the response repeated the incorrect semantic-verification result and overstated what git diff --check established for the untracked record.

[x] PASS — Original EXP-0027 mutation-path compliance is compliant.

[x] PASS — Original EXP-0027 overall scope compliance is compliant.

[x] PASS — Original EXP-0027 outcome is Partially successful.

[x] PASS — The accepted Phase 3 implementation outcome remains Successful with correction.

[ ] BLOCKED — Phase 3 formal completion remains outstanding until repository action, exact-head CI and separate closure synchronization are accepted.

[x] PASS — EXP-0027-C1 mutation-path compliance is compliant.

[x] PASS — EXP-0027-C1 overall scope compliance is compliant.

[ ] FAIL — EXP-0027-C1 artifact/evidence integrity failed because completed-stage chronology and inference claims remained inside the artifact and a bare prohibited status word remained.

[ ] FAIL — EXP-0027-C1 required semantic final-readback verification failed because those defects were reported as absent.

[ ] FAIL — EXP-0027-C1 execution protocol failed because validation continued after semantic verification was not actually satisfied.

[ ] FAIL — EXP-0027-C1 final-response integrity failed because the response repeated the incorrect semantic result, did not begin with the exact required heading, misreported the number of read calls and miscounted the artifact checklist entries.

[x] PASS — EXP-0027-C1 outcome is Partially successful.

[x] PASS — EXP-0027-C2 mutation-path compliance is compliant.

[x] PASS — EXP-0027-C2 overall scope compliance is compliant.

[ ] FAIL — EXP-0027-C2 artifact/evidence integrity failed because the prohibited lowercase status word remained inside the artifact.

[ ] FAIL — EXP-0027-C2 required semantic final-readback verification failed because the surviving occurrence was treated as exempt.

[ ] FAIL — EXP-0027-C2 execution protocol failed because validation continued after semantic verification was not satisfied and no edit capacity remained.

[ ] FAIL — EXP-0027-C2 final-response integrity failed because the response classified the correction as successful while acknowledging the surviving defect and also miscounted the new checklist entries.

[x] PASS — EXP-0027-C2 outcome is Partially successful.

[-] NOT APPLICABLE — EXP-0027-C3 post-write results are not claimed inside this repository artifact and are reported only in the C3 final response.
