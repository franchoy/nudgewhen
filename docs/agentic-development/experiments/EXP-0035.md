# EXP-0035 — v0.1.2 Candidate-B Synchronization and Bootstrap Closure

## Objective

Record the complete Candidate-B synchronization chronology, the original
B4c validation failure, the diagnosis and release-neutral corrective slice,
the corrective repository boundary, the exact-command B4c-R2 synchronization
proof, and the bootstrap-exception termination eligibility for the v0.1.2
release.

## Scope and boundaries

Candidate B's frozen six-path synchronization set is:

- `scripts/release_contract.json`
- `docs/releases/v0.1.2/release-charter.md`
- `docs/releases/v0.1.2/phase-list.md`
- `AGENTS.md`
- `README.md`
- `docs/agentic-development/experiments/EXP-0035.md`

The corrective paths:

- `scripts/validate_local.py`
- `tests/_helpers.py`
- `tests/test_validator_repository.py`

were a SEPARATE corrective slice and did not redefine Candidate B. The
corrective slice was driven by the failure of the original B4c validation
on the v0.1.2 bootstrap baseline and was committed independently as a
release-neutralization fix before this evidence record was produced.

This experiment covers the original Candidate-B work, the separately
committed corrective work, the validation evidence, the repository actions,
the historical agent and harness deviations, and the current state.

## Candidate-B chronology

Candidate B synchronized the inherited v0.1.1 current-release pointers to
v0.1.2 in the following substeps:

- **B1** — release charter `docs/releases/v0.1.2/release-charter.md`
  synchronization accepted.
- **B2** — phase list `docs/releases/v0.1.2/phase-list.md` synchronization
  accepted.
- **B3** — `scripts/release_contract.json` synchronization accepted.
- **B4a** — `AGENTS.md` synchronization accepted.
- **B4b** — `README.md` synchronization accepted.
- **B4c** — full validator regression executed. The full regression
  reported `Ran 98 tests` with `FAILED (failures=45)`. B4c hard-stopped
  correctly.
- **D1/D2/D3** — diagnosis identified the release-coupling defects
  responsible for the 45 failures.
- **C1/C2a/C2b1/C2b1-R1/C2b2a/C2b2b** — corrective implementation produced
  a release-neutral validator test surface.
- **C3** — corrective validation accepted the targeted and full regressions.
- **C4a/C4b/C4c** — corrective repository boundary committed, pushed, and
  CI-green.
- **B4c-R1** — repeated all four validation commands wrapped by a shell
  wrapper; functionally produced the expected results but was not the
  authoritative closure.
- **B4c-R2** — repeated all four validation commands exactly, without
  wrappers, and produced the authoritative Candidate-B synchronization
  proof.

## Original B4c failure

The original B4c validation executed the full validator regression after the
five non-experiment Candidate-B paths had been materialized. The result was:

- Validator regression: `Ran 98 tests` and `FAILED (failures=45)`.

B4c correctly hard-stopped at that point. The failures were concentrated in
the validator test surface rather than in the Candidate-B synchronization
content itself.

## D1/D2/D3 diagnosis

The D1/D2/D3 diagnosis produced the following directly proven findings:

- **D1.** `tests/_helpers.py` copied the live contract but hard-coded stale
  active release-document roots and current Android fixture metadata,
  pinning the test surface to the inherited v0.1.1 release identity rather
  than the active v0.1.2 contract.
- **D2.** `tests/test_validator_repository.py` contained release-coupled
  v0.1.1 positive fixtures, negative fixtures, AGENTS fixtures, ProductScope
  charter header and path assertions, and related assertions that fixed the
  test expectations to the inherited v0.1.1 release identity.
- **D3.** `scripts/validate_local.py` had a release-neutral-path
  implementation but a production semantic defect for the zero Complete
  phases case. The expected display was `(no phases complete)` while the
  observed display was `(no completed-range claim)`, and the validator
  incorrectly compared those two display strings rather than comparing the
  underlying semantic completed-claim values.

## Corrective implementation

The corrective slice produced the following changes:

- **C1.** `tests/_helpers.py` became contract-driven, reading the active
  release identity and document roots from `scripts/release_contract.json`
  rather than hard-coding them.
- **C2a.** `scripts/validate_local.py` changed its completed-range
  presentation from a string comparison to a semantic completed-claim
  comparison, returning a `tuple[int, int] | None` for completed ranges
  and `None` for zero Complete phases. `None == None` now passes for zero
  Complete phases.
- **C2b1.** `tests/test_validator_repository.py` gained contract-derived
  fixture identity, positive repository fixtures, and early README tests.
  The C2b1 historical execution deviation is preserved in the
  Agent and harness observations section below.
- **C2b1-R1.** A subsequent retry directly verified the three edited
  methods that the original C2b1 post-edit readback had not surfaced, and
  accepted the C2b1 artifact.
- **C2b2a.** Six repository-consistency negative tests became
  release-neutral.
- **C2b2b.** ProductScope `CHARTER_HEADER` and charter-path lookup became
  contract-driven.

## Corrective validation

Corrective C3 validation results:

- **C3 targeted (affected classes).** `Ran 45 tests` and `OK`.
- **C3 full regression.** `Ran 98 tests` and `OK`.

The corrective validation proved the release-neutralization fix produced a
green test matrix on the v0.1.2 bootstrap baseline before any repository
action was taken.

## Corrective repository boundary

The corrective repository boundary was established as follows:

- **C4a.** OpenCode attempted the exactly authorized `git add` once. The
  OpenCode permission ceiling denied `git add`. The agent correctly
  hard-stopped with zero retry and zero substitute staging mechanism.
- Then the maintainer manually staged exactly:
  - `scripts/validate_local.py`
  - `tests/_helpers.py`
  - `tests/test_validator_repository.py`
- **C4b.** The manual corrective commit was produced:
  - Commit: `9c10d5b2750f2d84479efbfa3d105da8da8c5a8a`
  - Parent: `0b9ceedc46c487d909c576d24c7fb66881681386`
  - Message: `fix: make release validation release-neutral`
  - Exact three corrective paths, no others.
- **C4c.** The commit was pushed to `origin/release/v0.1.2` and the exact
  head CI ran green:
  - CI run: `32643727537`
  - Conclusion: `success`

The OpenCode `git add` denial at C4a was a harness permission ceiling
event, not a corrective-code failure. The corrective code itself was
green against the C3 validation before any staging attempt.

## B4c synchronization retry

After the corrective commit was CI-green, B4c synchronization retry
executed the four prescribed validation commands against the synchronized
v0.1.2 baseline.

- **B4c-R1.** Functionally produced the expected results but wrapped
  Actions 5 through 8 with `2>&1; echo "EXITSTATUS=$?"`. The functional
  evidence was retained but B4c-R1 was NOT the authoritative synchronization
  closure because the wrapper altered the command surface from the
  prescribed form.
- **B4c-R2.** Repeated all four validation commands exactly, with no
  wrappers. Observed evidence:
  - Validator regression: `Ran 98 tests` and `OK`.
  - Required group: `SUMMARY pass=11 fail=0 skip=0`.
  - Docs group: `SUMMARY pass=11 fail=0 skip=0`.
  - Decisive synchronization proof: `PASS docs/repository-consistency —
    active release identity consistent (version='v0.1.2',
    branch='release/v0.1.2')`.
  - Skip-Android integrated: `SUMMARY pass=22 fail=0 skip=0`.

B4c-R2 is the authoritative Candidate-B synchronization proof.

All isolated and integrated validation gates in this evidence remained
`release_gate=NOT_SATISFIED` as expected, because Android and full
release-gate conditions were not being satisfied by these bounded
synchronized-validation runs.

## Bootstrap-exception closure

The repository-consistency termination prerequisite for the temporary
v0.1.2 bootstrap exception is satisfied by B4c-R2. The temporary v0.1.2
bootstrap exception is now `TERMINATED` in `AGENTS.md` via this evidence
reconciliation. The historical v0.1.1 bootstrap record remains preserved
unchanged.

This evidence reconciliation does NOT make Phase 0 `Complete`. Phase 0
remains `Planned` until an explicit maintainer closure step is authorized.
This evidence reconciliation does NOT itself commit Candidate B. Candidate B
remains uncommitted in the working tree and the Git staging area at the end
of this evidence reconciliation.

## Agent and harness observations

The following historical agent and harness deviations are preserved here as
evidence and are not erased by the corrective or synchronization outcomes.

- **C2b1 historical deviation.** Original C2b1 post-edit readback did not
  show all three complete edited methods but the agent continued. C2b1-R1
  later directly verified the missing methods and accepted the artifact.
  - Lesson 1: YES (continuation after a required hard-stop condition when
    the authorized readback did not show all required complete modified
    methods).
  - Lesson 2: YES (expected/inferred completion claimed as observed).
  - Lesson 5: YES (intermediate final-report overstatement relative to
    readback state).
  - Lesson 9: NO.
- **C2b2a reporting.** Six repository-consistency negative tests became
  release-neutral.
  - Lesson 1: NO.
  - Lesson 2: YES.
  - Lesson 5: YES.
  - Lesson 9: NO.
- **C2b2b reporting.** ProductScope `CHARTER_HEADER` and charter-path
  lookup became contract-driven.
  - Lesson 1: NO.
  - Lesson 2: YES.
  - Lesson 5: YES.
  - Lesson 9: NO.
- **B4c-R1.** Repeated validation with a wrapper that altered the command
  surface from the prescribed form.
  - Lesson 1: NO.
  - Lesson 2: NO.
  - Lesson 5: YES (functional-evidence claim presented as authoritative
    closure despite wrapper).
  - Lesson 9: NO.
- **B4c-R2.** Repeated all four validation commands exactly with no
  wrappers and produced the authoritative synchronization proof.
  - Lesson 1: NO.
  - Lesson 2: NO.
  - Lesson 5: NO.
  - Lesson 9: NO.
- **C4a.** OpenCode's permission ceiling denied `git add` despite the
  explicitly authorized staging operation. The agent correctly
  hard-stopped with no retry and no substitute staging mechanism. The
  denial is not characterized as a corrective-code failure.

## Current repository state

- `HEAD`: `9c10d5b2750f2d84479efbfa3d105da8da8c5a8a`.
- Corrective commit `9c10d5b2750f2d84479efbfa3d105da8da8c5a8a`: already
  committed, pushed, and CI-green.
- Candidate B: still uncommitted in the working tree and not staged.
- Phase 0: still `Planned`.
- Bootstrap exception (v0.1.2): `TERMINATED` by this evidence
  reconciliation.

## Outcome

- `CANDIDATE_B_SYNCHRONIZATION`: `PASS`.
- `REPOSITORY_CONSISTENCY`: `PASS`.
- `BOOTSTRAP_EXCEPTION`: `TERMINATED`.
- `PHASE_0`: `STILL PLANNED`.
- `CANDIDATE_B_COMMIT`: `NOT YET PERFORMED`.

This evidence does NOT claim production readiness and does NOT claim
v0.1.2 release completion.

## Post-Candidate-B repository overlay

This overlay supplements the pre-Candidate-B-commit evidence above. It does
not rewrite the earlier `Current repository state`, `Outcome`, chronology,
or historical agent/harness observations. Statements above that describe
Candidate B as uncommitted or Phase 0 as still Planned remain accurate
snapshots of the point at which the original Candidate-B evidence was
authored.

### Final Candidate-B staged validation

The accepted B4e staged-candidate validation established:

- exactly six staged Candidate-B paths;
- `git diff --cached --check` produced no output;
- no unstaged tracked difference;
- validator regression: `Ran 98 tests` / `OK`;
- required group: `SUMMARY pass=11 fail=0 skip=0`;
- docs group: `SUMMARY pass=11 fail=0 skip=0`;
- repository consistency: `PASS` for `version='v0.1.2'` and
  `branch='release/v0.1.2'`;
- skip-Android integrated: `SUMMARY pass=22 fail=0 skip=0`;
- bounded runs retained `release_gate=NOT_SATISFIED` as expected.

The B4e final report later made a narrow unsupported final-HEAD inference.
The maintainer audit therefore records B4e independently as:

- Lesson 1: NO.
- Lesson 2: YES — narrow final-HEAD inference without a final HEAD
  observation.
- Lesson 5: YES — narrow final-report overstatement.
- Lesson 9: NO.

The staged-candidate validation itself remains accepted.

### Candidate-B commit and remote boundary

MAINTAINER-SUPPLIED ACCEPTED EVIDENCE records:

- Candidate-B commit:
  `4fa042c30223ca0d451501220e42cc18b115fc42`
- Parent:
  `9c10d5b2750f2d84479efbfa3d105da8da8c5a8a`
- Subject:
  `docs: synchronize v0.1.2 candidate b`
- Commit path set:
  exactly the frozen six Candidate-B paths.
- Push:
  success to `origin/release/v0.1.2`.
- Remote exact SHA:
  `4fa042c30223ca0d451501220e42cc18b115fc42`.

Exact-head CI:

- Run: `32649781738`
- Event: `push`
- Head SHA:
  `4fa042c30223ca0d451501220e42cc18b115fc42`
- Status: `completed`
- Conclusion: `success`
- `validate` job: `success`
- `validator-regressions`: `success`
- `validate-local`: `success`
- `upload-debug-apk`: `success`

Therefore:

- `CANDIDATE_B_COMMIT`: `PASS`.
- `CANDIDATE_B_PUSH`: `PASS`.
- `CANDIDATE_B_EXACT_HEAD_CI`: `PASS`.
- `CANDIDATE_B_REPOSITORY_BOUNDARY`: `CLOSED`.

### Post-creation evidence correction

B4d-R1 corrected the C2b1 historical description so that Lesson 1 records
continuation after a required hard-stop condition rather than a "soft"
readback issue.

The later maintainer audit of the B4d-R1 report records:

- Lesson 1: NO.
- Lesson 2: YES — narrow final-HEAD inference without a final HEAD
  observation.
- Lesson 5: YES — narrow contradictory final-report overstatement.
- Lesson 9: NO.

The evidence wording correction itself remains accepted.

### Phase 0 closure position

At the Candidate-B repository boundary:

- Candidate-B synchronization is complete.
- The temporary v0.1.2 bootstrap exception is `TERMINATED`.
- Candidate-B repository consistency is accepted.
- Candidate-B repository boundary is `CLOSED`.
- Android target metadata has not been delivered and is not a Phase 0
  closure blocker.
- Phase 0 remains formally `Planned` until the separately authorized formal
  Phase 0 status-transition Build is completed and accepted.
- Phase 1 has not started.

This overlay does not claim production readiness, Phase 0 completion,
v0.1.2 release completion, or a Phase 1 implementation.

## Formal Phase 0 closure-candidate overlay

This additive overlay records the later formal Phase 0 transition. It does
not rewrite the earlier Candidate-B-boundary evidence above. The earlier
statements that Phase 0 was still `Planned` and Phase 1 had not started
remain historically accurate for that earlier repository boundary.

### Pre-transition reconciliation

Accepted pre-transition closure synchronization:

- F2A: `PASS` — post-Candidate-B repository evidence was added
  cumulatively to this experiment record.
- F2B1: `PASS` — the terminated v0.1.2 bootstrap declaration in
  `AGENTS.md` was reconciled to historical/past-tense state.
- F2B2: `ACCEPTED_WITH_CORRECTION`.
- F2B2-R1: `PASS` — the remaining release-charter phase-list
  cross-reference was corrected.
- F2B3: `PASS` — the phase list was reconciled to accepted Candidate-B,
  `EXP-0035`, repository-boundary, and bootstrap-termination state while
  Phase 0 remained formally `Planned`.

### Formal transition chronology

- F2C1: `PASS` — `scripts/release_contract.json` and the v0.1.2 phase list
  transitioned Phase 0 from `Planned` to the `Complete` closure-candidate
  state. The semantic completed claim is `(0, 0)`. Phase 1 remained
  `Planned`.
- F2C2: `PASS` — the release charter transitioned to Phase 0 `Complete`
  candidate state and Phase 1 as the current lifecycle phase with formal
  status `Planned`.
- F2C3A: `PASS` — `AGENTS.md` transitioned its current-release context to
  Phase 0 `Complete` candidate state and Phase 1 — Reminder Architecture
  Contract — as the current lifecycle phase with formal status `Planned`;
  Phase 1 implementation was explicitly not started.
- F2C3B: `PASS` — `README.md` transitioned its two current-state surfaces to
  Phase 0 `Complete` candidate state and Phase 1 current / `Planned`;
  product implementation remains not started.

### Current six-path closure candidate

The formal Phase 0 closure candidate spans exactly:

- `AGENTS.md`
- `README.md`
- `docs/agentic-development/experiments/EXP-0035.md`
- `docs/releases/v0.1.2/phase-list.md`
- `docs/releases/v0.1.2/release-charter.md`
- `scripts/release_contract.json`

Current semantic state:

- Phase 0: `Complete` closure candidate.
- Completed-range claim: `(0, 0)`.
- Phase 1 — Reminder Architecture Contract: current lifecycle phase.
- Phase 1 formal status: `Planned`.
- Phase 1 implementation: not started.
- Phases 1 through 7: `Planned`.
- Candidate-B repository boundary: `CLOSED`.
- Temporary v0.1.2 bootstrap exception: `TERMINATED`.
- Android target metadata: not delivered and not a Phase 0 closure blocker.
- Reminder and persistence product implementation: not started.

This is a synchronized CONTENT candidate only.

Integrated six-path validation has not yet been performed after the formal
transition.

Therefore:

- `FORMAL_PHASE_0_TRANSITION`: `SYNCHRONIZED_CANDIDATE`.
- `FULL_PHASE_0_CLOSURE`: `PENDING_INTEGRATED_VALIDATION`.
- `PHASE_1_IMPLEMENTATION`: `NOT_STARTED`.

This overlay does not claim production readiness, v0.1.2 release completion,
or that Phase 1 implementation has begun.

### Formal-closure MiniMax lesson disposition

Maintainer-audit lesson classifications for the formal closure work:

- F2A: Lesson 1 `NO`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `NO`.
- F2B1: Lesson 1 `NO`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `NO`.
- F2B2: Lesson 1 `YES`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `YES`.
- F2B2-R1: Lesson 1 `NO`; Lesson 2 `NO`; Lesson 5 `NO`; Lesson 9 `NO`.
- F2B3: Lesson 1 `NO`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `NO`.
- F2C1: Lesson 1 `NO`; Lesson 2 `NO`; Lesson 5 `NO`; Lesson 9 `NO`.
- F2C2: Lesson 1 `NO`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `NO`.
- F2C3A: Lesson 1 `NO`; Lesson 2 `YES`; Lesson 5 `YES`; Lesson 9 `NO`.
- F2C3B: Lesson 1 `NO`; Lesson 2 `NO`; Lesson 5 `NO`; Lesson 9 `NO`.

These dispositions supplement the historical execution records; they do not
rewrite them.

## Integrated Phase 0 closure-candidate validation

Phase 0F-2D1 validated the synchronized six-path closure candidate after the
formal Phase 0 transition.

### D1 validator regression

Command:

`python3 -B -m unittest tests.test_validator_core tests.test_validator_repository`

Observed unittest result:

- `Ran 98 tests`
- `OK`
- no unittest `FAILED`
- no unittest `ERROR`

The unittest stdout also emitted validator PASS/FAIL fixture messages from
negative validator test scenarios. Those fixture `FAIL ...` strings are test
scenario output and are not failing unittest cases. The unittest process
itself completed `OK`.

### D1 required-group validation

Command:

`./scripts/validate-local.sh --group required`

Observed:

- `SUMMARY pass=11 fail=0 skip=0`
- `release_gate=NOT_SATISFIED`
- no validator FAIL.

### D1 docs-group validation

Command:

`./scripts/validate-local.sh --group docs`

Observed:

- `PASS docs/phase-status — contiguous phase status: 1 Complete, 7 Planned`
- `PASS docs/repository-consistency — active release identity consistent
  (version='v0.1.2', branch='release/v0.1.2')`
- `SUMMARY pass=11 fail=0 skip=0`
- `release_gate=NOT_SATISFIED`
- no validator FAIL.

The phase-status evidence establishes one phase `Complete` and seven phases
`Planned`, consistent with the semantic completed-range claim `(0, 0)`.

### D1 skip-Android integrated validation

Command:

`./scripts/validate-local.sh --skip-android`

Observed:

- `SUMMARY pass=22 fail=0 skip=0`
- `release_gate=NOT_SATISFIED`
- no validator FAIL.

### D1 closure-candidate disposition

The D1 validation establishes:

- `PHASE_0F2D1`: `PASS`.
- `VALIDATOR_REGRESSION`: `PASS`.
- `REQUIRED_GROUP`: `PASS`.
- `DOCS_GROUP`: `PASS`.
- `REPOSITORY_CONSISTENCY`: `PASS`.
- `SKIP_ANDROID_INTEGRATED`: `PASS`.
- `EXPECTED_COMPLETED_CLAIM`: `(0, 0)`.
- `OBSERVED_PHASE_STATUS`: `1 Complete / 7 Planned`.
- `FORMAL_PHASE_0_TRANSITION`: `VALIDATED_CANDIDATE`.
- `FULL_PHASE_0_CLOSURE`: `READY_FOR_STAGED_VALIDATION`.
- `CURRENT_PHASE`: `Phase 1 — Reminder Architecture Contract`.
- `PHASE_1_STATUS`: `PLANNED`.
- `PHASE_1_IMPLEMENTATION`: `NOT_STARTED`.
- `BOOTSTRAP_EXCEPTION`: `TERMINATED`.

The candidate remains uncommitted and unpushed.

Exact-head CI for the final Phase 0 closure candidate has not yet occurred.

The next repository boundary is exact-six-path staging followed by complete
staged-candidate validation.

### D1 MiniMax lesson disposition

Maintainer audit of D1:

- Lesson 1: `NO`.
- Lesson 2: `NO`.
- Lesson 5: `NO`.
- Lesson 9: `NO`.

This D1 evidence supplements the earlier synchronized-content candidate
state. It does not rewrite the historically correct pre-D1
`PENDING_INTEGRATED_VALIDATION` record.
