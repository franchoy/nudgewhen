# EXP-0046 — v0.1.3 Persistence Compatibility Proof

## 1. Experiment identity

- Experiment: `EXP-0046`
- Release: `v0.1.3`
- Phase: `Phase 3 — Persistence Compatibility Proof`
- Status: Phase 3 implementation evidence complete; closure-synchronization candidate being authored separately
- Repository branch: `release/v0.1.3`
- Implementation repository boundary: `LANDED_AND_EXACT_HEAD_CI_ACCEPTED`
- Release readiness: `NO`

## 2. Objective and exact Phase 3 scope

Phase 3 proves persistence compatibility for the already-landed domain edit operation.

Exact implementation path:

`app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt`

Production persistence change expected/observed: `NONE`.

Out of scope:

- Compose editing UX
- `MainActivity` changes
- Android identity
- dependencies
- validator architecture
- CI architecture
- new persistence backend
- `NWR1` migration

## 3. Frozen Phase 1 persistence contract

- `NWR1_FORMAT`: `NO_MIGRATION`
- `PHYSICAL_SAVE_BEHAVIOR`: `EXISTING_COMPATIBLE_WHOLE_FILE_REWRITE`
- Header: `NWR1`
- Record grammar: `<id><TAB><base64url-encoded-utf-8-text>`

Required Phase 3 proof responsibilities:

- changed edit round-trip
- same `id` after reload
- same list index after reload
- neighbors/order/content unchanged
- new store/controller restores edited text
- existing valid NWR1 supports load/edit/save/reload
- NWR1 header unchanged
- record grammar unchanged
- Unicode edit round-trip
- no migration

## 4. Authoritative Phase 3 implementation path

- single implementation path: `app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt`
- existing tests preserved: `P3_01` through `P3_29`
- new edit-persistence tests added: `P3E_01` through `P3E_10`
- source-level `FileReminderStoreTest` total: `39`
- production `FileReminderStore` change: `NONE`
- production `ReminderController` change during Phase 3: `NONE`

## 5. A0 persistence reconnaissance

`A0` (RETAINED DIRECT EVIDENCE):

- clean read-only persistence reconnaissance;
- 29 existing persistence tests reconciled;
- 10 Phase 3 responsibilities required new edit-specific proof;
- `NWR1` expectation compatible with the observed `FileReminderStore` implementation;
- one-test-file implementation boundary sufficient;
- production change required: `NO`;
- A0 report-format trailing separator noted as report-format-only and not material to evidence.

A0 lessons:

- `L1:NO`
- `L2:NO`
- `L5:NO`
- `L9:NO`

## 6. B1 persistence-test implementation and validation

`B1` (RETAINED DIRECT EVIDENCE):

- exact one-path implementation authorized by Build scope;
- `P3_01` through `P3_29` preserved;
- `P3E_01` through `P3E_10` implemented;
- source-level `FileReminderStoreTest` count: `39`;
- direct JVM validation: `BUILD SUCCESSFUL`;
- Android validator: `17 / 0 / 0`;
- `android/jvm-tests`: `PASS`;
- `release_gate`: `NOT_SATISFIED`;
- production changes: `NONE`;
- B1 report-format trailing separator noted as report-format-only and not material to evidence.

B1 lessons:

- `L1:NO`
- `L2:NO`
- `L5:NO`
- `L9:NO`

## 7. B2 semantic audit

`B2` (RETAINED DIRECT EVIDENCE):

- `P3_01` through `P3_29` preserved;
- `P3E_01` through `P3E_10` all semantically `PASS`;
- `NWR1` compatibility: `SEMANTICALLY_ACCEPTED_AND_RUNTIME_GREEN`;
- `NWR1` migration: `NOT_REQUIRED_BY_PHASE_3_TEST_EVIDENCE`;
- production change required: `NO`;
- blocking findings: `NONE`;
- B2 report-format trailing separator noted as report-format-only and not material to evidence.

B2 lessons:

- `L1:NO`
- `L2:NO`
- `L5:NO`
- `L9:NO`

## 8. B3 final validation and byte freeze

`B3` (RETAINED DIRECT EVIDENCE):

- direct JVM Gradle invocation: `BUILD SUCCESSFUL`; the task returned `:app:testDebugUnitTest UP-TO-DATE` (retained direct evidence shows UP-TO-DATE rather than a fresh re-execution of every JVM test);
- Android validator: `17 / 0 / 0`;
- `android/jvm-tests`: `PASS`;
- `release_gate`: `NOT_SATISFIED`;
- `git diff --check`: no output;
- frozen implementation path: `app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt`;
- frozen candidate SHA-256: `74e46f94fcfbc823911faf971124c4600f70f5d02002b5dc4e6f2a1d623aa4a7`;
- candidate byte freeze: `COMPLETE`.

B3 lessons:

- `L1:NO`
- `L2:NO`
- `L5:NO`
- `L9:NO`

## 9. Maintainer Category-C staging history

(MAINTAINER-SUPPLIED DECISION/OBSERVATION)

The required pre-stage B3 SHA-256 recheck was omitted before `git add`.

Classification: `PROTOCOL_DEVIATION_WITH_COMPLETE_STAGED_INDEX_PROOF`.

Later staged-index proof established:

- `CACHED_PATH_SET`: `EXACT_ONE_PATH`;
- `CACHED_WHITESPACE`: `PASS`;
- `STAGED_SHA256`: `74e46f94fcfbc823911faf971124c4600f70f5d02002b5dc4e6f2a1d623aa4a7`;
- `STAGED_BYTES`: `EXACT_B3_FROZEN_CANDIDATE`;
- `UNSTAGE_REQUIRED`: `NO`;
- `RESTAGE_REQUIRED`: `NO`.

No OpenCode lesson codes are assigned to this maintainer repository-action history.

## 10. Implementation commit / remote / exact-head CI evidence

(MAINTAINER-SUPPLIED DECISION/OBSERVATION)

- implementation commit: `b77af048950a720482c4ec279762d51f7f65ca5f`
- parent: `fa387ece5eeb972f4344ea4bd04582749b6bbf02`
- subject: `test: prove reminder edit persistence compatibility`
- exact commit path: `app/src/test/kotlin/io/github/franchoy/nudgewhen/data/FileReminderStoreTest.kt`
- committed SHA-256: `74e46f94fcfbc823911faf971124c4600f70f5d02002b5dc4e6f2a1d623aa4a7`
- remote SHA: `b77af048950a720482c4ec279762d51f7f65ca5f`
- exact-head CI run: `33245690596`
- workflow: `CI`
- event: `push`
- head SHA: `b77af048950a720482c4ec279762d51f7f65ca5f`
- status: `completed`
- conclusion: `success`
- repository boundary: `LANDED_AND_EXACT_HEAD_CI_ACCEPTED`

The remote-branch SHA equality with the local commit, the hosted exact-head CI result, the successful push, and the entire Section 10 record are maintainer-supplied evidence. This Build does not independently re-observe the push or the hosted exact-head CI.

## 11. Phase 3 technical outcome

Established Phase 3 evidence (mixed provenance; detailed provenance recorded in Sections 4–10):

- existing `P3_01` through `P3_29` preserved;
- `P3E_01` through `P3E_10` semantically accepted;
- source-level `FileReminderStoreTest` count: `39`;
- changed edit round-trip persists;
- same `id` survives reload;
- same list index survives reload;
- neighbors/order/unrelated content preserved;
- new store/controller restores edited text;
- existing valid NWR1 supports load/edit/save/reload;
- NWR1 header unchanged;
- record grammar unchanged;
- Unicode edited text round-trips;
- existing compatible whole-file rewrite remains sufficient;
- `NWR1` migration: `NOT_REQUIRED_BY_PHASE_3_TEST_EVIDENCE`;
- no production `FileReminderStore` change;
- no production `ReminderController` change during Phase 3;
- no Compose editing UX change;
- no Android identity alignment;
- no dependency, validator architecture, CI architecture, or OpenCode configuration change.

## 12. Phase 3 closure-synchronization candidate lifecycle state

This section describes the dirty C1 target state as a closure-synchronization candidate. The candidate lifecycle state is:

- Phase 0: `Complete`;
- Phase 1: `Complete`;
- Phase 2: `Complete`;
- Phase 3: `Complete` in this closure-synchronization candidate;
- Phase 4: `Planned` / next / not started;
- Phases 5 through 7: `Planned`;
- phase model: `4 Complete / 4 Planned`;
- editing domain implementation: `LANDED_AND_EXACT_HEAD_CI_ACCEPTED`;
- persistence compatibility proof: `LANDED_AND_EXACT_HEAD_CI_ACCEPTED`;
- Compose editing UX: `NOT_STARTED`;
- Android current identity: `versionCode 3 / versionName 0.1.2`;
- Android target identity: `versionCode 4 / versionName 0.1.3`;
- release readiness: `NO`.

This is candidate lifecycle content. It is NOT a claim that this C1 authoring, this C1 validation, this C1 landing, or any later exact-head CI has already succeeded. The committed Phase 3 persistence test, its remote SHA, and its exact-head CI are independently recorded above from the Phase 3 implementation landing; this candidate lifecycle section describes only the closure-synchronization candidate authored for Phase 3-C1.

## 13. Provenance principle

Current or future execution outcomes are established only after their actual execution.

Evidence labels used in this document follow the convention:

- `DIRECT OBSERVATION` — directly observed by a Phase 3 Build execution and retained as evidence;
- `RETAINED DIRECT EVIDENCE` — directly produced by a Phase 3 Build execution and retained as evidence;
- `MAINTAINER-SUPPLIED DECISION/OBSERVATION` — supplied by the maintainer outside the Build execution, including the maintainer repository-action history and the successful push of the Phase 3 implementation commit;
- `INFERENCE` — strongly supported by direct evidence but not directly observed (no `INFERENCE` labels are used in this document).

Specifically, future execution outcomes are not pre-written into this evidence record:

- the outcome of the C1 authoring and local validation pass is established only by the C1 execution report after completion and is not pre-written here;
- the C1 lesson disposition is established only by the C1 execution report after completion and is not pre-written here;
- the C1 closure-candidate audit result is established only by the C2 execution report after completion and is not pre-written here;
- the future C3 byte-freeze result is established only by the C3 execution report after completion and is not pre-written here;
- the future closure candidate hashes are not pre-written here;
- the future closure staging, commit, push, and exact-head CI are not pre-written here;
- the future Phase 4 implementation is not pre-written here.
